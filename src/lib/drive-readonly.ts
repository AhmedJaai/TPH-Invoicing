/**
 * حارسٌ يمنع الكتابة على الدرايف في بيئةٍ ليست الإنتاج.
 *
 * أرشيفُ الدرايف واحدٌ لا يتفرّع: قاعدةُ المعاينة نسخةٌ من الإنتاج
 * ويُكتَب فيها بلا خطر، أمّا الدرايف فلا نسخةَ له — وإعادةُ تسميةٍ
 * تجري في معاينةٍ تقع على **الملفّ الحقيقيّ** الذي يراه أحمد.
 *
 * والأمرُ ليس افتراضياً: للتسمية أثرٌ وقع من قبل على أربعة ملفّاتٍ
 * حقيقيّة يوم ١٤ سبتمبر ٢٠٢٦، بنصّه في سجلّ `DRIVE_FILE_RENAMED`.
 *
 * فما لم تكن البيئةُ إنتاجاً صريحاً، تُمنَع الكتابة ويُقال السبب.
 * والقراءةُ والمزامنةُ والمعاينةُ تعمل كما هي — المنعُ على التسمية
 * وحدها، وهي العمليّةُ الكتابيّة الوحيدة على الدرايف في النظام كلّه.
 */
export interface DriveWriteEnv {
  VERCEL_ENV?: string;
  NODE_ENV?: string;
  /** يفتح الكتابة في التطوير لمن يعمل على المسار نفسه عن قصد. */
  DRIVE_ALLOW_WRITE?: string;
}

export function driveWritesAllowed(env: DriveWriteEnv): boolean {
  if (env.VERCEL_ENV === "production") return true;
  // خارج Vercel: البناءُ الإنتاجيّ وحده — وإلّا فقرارٌ صريح بالمتغيّر.
  if (!env.VERCEL_ENV && env.NODE_ENV === "production") return true;
  return env.DRIVE_ALLOW_WRITE === "true";
}

export const DRIVE_READONLY_MESSAGE =
  "إعادة التسمية موقوفة في هذه البيئة: الأرشيف في جوجل درايف واحدٌ لا نسخةَ له، "
  + "فالتسمية من معاينةٍ تقع على الملفّ الحقيقيّ. المعاينة تعمل، والتنفيذ من الإنتاج وحده.";
