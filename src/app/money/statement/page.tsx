import { permanentRedirect } from "next/navigation";

/**
 * تحويلٌ دائم إلى «المال».
 *
 * كانت تحمل شيئين: **التدفّق النقديّ** — وهو نافع، فانتقل إلى «المال» —
 * و**قائمة الدخل**، وخمسةٌ من سبعة بنودٍ فيها «غير متاح» لأنّها تحتاج
 * مصدرَ مبيعاتٍ غير موصول. وهيكلٌ ينتظر مصدراً ليس صفحةً تُفتَح كلّ شهر.
 *
 * وجدولُ «المصروف حسب الباب» كان معروضاً فيها وفي «المال» بالأرقام
 * نفسها.
 */
export default function LegacyStatement(): never {
  permanentRedirect("/money");
}
