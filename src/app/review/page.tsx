import { permanentRedirect } from "next/navigation";

/**
 * تحويلٌ دائم إلى «يحتاج قرارك».
 *
 * كانت `/review` تُسمّى «طابور المراجعة» — أي مكان العمل الباقي —
 * وتُفتَح على بيانات المقهى فتقول **«لا شيء ينتظرك»**، بينما تسعةُ
 * بنودٍ تنتظر في شاشةٍ أخرى وخمسةَ عشرَ مستنداً في ثالثة. فالمكانُ
 * المخصَّص للعمل كان المكانَ الوحيد الفارغ منه.
 *
 * والسبب أنّها بابٌ واحد من سبعةَ عشر: حركاتُ بنكٍ لم يُعرَف بابُها.
 * فصارت لوحَ فعلٍ داخل بندها، ويُفتَح بهذا الرابط.
 */
export default function LegacyReview(): never {
  permanentRedirect("/attention?item=unclassified-bank");
}
