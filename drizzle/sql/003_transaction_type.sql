-- ═══════════════════════════════════════════════════════════════════
--  نوع الحركة البنكية يُحفظ مع الحركة
-- ═══════════════════════════════════════════════════════════════════
--
--  كشف الأهلي يحمل عمود «نوع العملية» منفصلاً عن الوصف، وكنّا نقرؤه
--  للمطابقة ثمّ نطرحه. وكشف تصنيف الحركات يعتمد عليه: «نقاط بيع» و«رسوم»
--  ترد في النوع لا في الوصف. فبقيت مئات الحركات «غير مصنَّفة» لأنّ ما
--  يصنّفها لم يُحفظ.
--
--  الحركات القديمة لا يُسترجع نوعها إلا بإعادة استيراد كشفها — والعمود
--  يقبل الفراغ ليقول ذلك صراحةً بدل أن يُملأ بنصّ مخترع.

ALTER TABLE bank_transactions ADD COLUMN IF NOT EXISTS transaction_type text;

CREATE INDEX IF NOT EXISTS bank_tx_type_idx ON bank_transactions (transaction_type);
