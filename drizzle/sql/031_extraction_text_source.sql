-- ═══════════════════════════════════════════════════════════════
-- من أين قُرئ المستند: نصّاً أم صورةً مضمَّنة أم صورةً مباشرة
--
-- `documents.text_source` موجودٌ ولم يُملأ قطّ (١٦٦ من ١٦٦ فارغ)، فلا
-- يُعرف كم من الأرشيف ممسوح — وهو ما يتوقّف لو سُحب نموذج الرؤية.
-- والقراءة تقع في `/api/analyze` قبل الأرشفة، فيُحفَظ مصدرها مع ما
-- قُرئ ثمّ يُنقل إلى المستند. عمودٌ جديد فارغ — لا يمسّ بياناً.
-- ═══════════════════════════════════════════════════════════════

ALTER TABLE extraction_cache ADD COLUMN IF NOT EXISTS text_source text;
