-- ═══════════════════════════════════════════════════════════════
-- قيدٌ واحد للهويّة، لا قيدان بتوحيدين مختلفين
--
-- `bank_tx_natural_uniq` كان يوحّد الوصف بـSQL (الفراغات وحالة
-- الأحرف)، و`identity_key` يوحّده بـ`identityText` (الترقيم والهمزة
-- والتاء المربوطة كذلك). فصار للحركة الواحدة تمثيلان يقيّدانها، وكلٌّ
-- يرى ما لا يراه الآخر.
--
-- وهذا هو نفسه العطب الذي أدخل الكشف مرّتين: **كاتبٌ يوحّد وقارئٌ
-- يوحّد بغيره**. فيُترَك واحد، وهو المخزَّن الذي يكتبه المستورِد نفسه.
--
-- ولا يُترَك المجالُ فارغاً: `bank_tx_identity_uniq` قائم قبل هذا،
-- و`bank_tx_operation_ref_uniq` يحرس مرجع العمليّة.
-- ═══════════════════════════════════════════════════════════════

drop index if exists bank_tx_natural_uniq;
