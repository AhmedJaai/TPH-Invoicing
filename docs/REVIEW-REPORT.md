# تقرير الجولة — ٦ سبتمبر ٢٠٢٦

مراجعةٌ خارجية بأربعين بنداً، حاكمُها جملةٌ واحدة:

> *«لا تُضِف ميزةً حتى تصير البيانات الخام والاستنتاج والاقتراح والتأكيد
> والقيد المالي والتدقيق والتعلّم **حلقةً واحدة مغلقة**.»*

وهذا التقرير مرتَّبٌ على الأقسام التي طلبها المراجع.

**الحال:** ١٬١٣٦ اختباراً في ٧٧ ملفاً · ٣٩ جدولاً · ١٧ هجرة ·
`typecheck` و`lint` و`build` نظيفة · مدفوعٌ إلى `main`.

---

## أ · ما وُجد

سبعُ كسورٍ في الحلقة، كلّها من نوعٍ واحد: **عملٌ يُحسَب صحيحاً ولا يبلغ
المال**.

| المكان | الحال |
|---|---|
| `UNKNOWN_HIGH_VALUE` | يُنشئ حالةً بلا مرشّحين — والحَكَم يرفضها |
| `splitBankFee` | مكتوبةٌ ومختبَرةٌ ولا يستدعيها أحد |
| `schemas-by-kind.ts` | مكتوبٌ ومختبَرٌ ولا يستدعيه أحد |
| `detect.ts` | يعرف البنك ثمّ يُرمى اسمُه في عمودِ عرض |
| `exact` من المحسِّن | يُحسَب ولا يُعرَض |
| `coverage.gaps` | تُحسَب عند الاستيراد ثمّ تُنسى |
| ثقة النموذج | تُقرأ ولا تؤثّر — ٠٫٢ و٠٫٩٩ سواء |

وثلاثة عيوبٍ في البيانات نفسها لا في استعمالها: هويّةٌ بلا نطاق، ودليلٌ
فرادتُه واحدة لكلّ الأنواع، وحدثُ مصروفٍ يُقيَّد مرّتين من مصدرين.

---

## ب · ما كان خطأً

مرتَّبٌ بأثره الماليّ، الأشدّ أوّلاً.

### ١. المال الداخل يُطابَق بفاتورة — ويُحسَم تلقائياً

الاتّجاه لم يكن يُفحَص أصلاً. فحركةٌ **واردة** بابُها مجهول تمرّ إلى
تعريف المستفيد ثمّ إلى توليد المرشّحين، فإن وافق مبلغُها فاتورةً مفتوحة
حُسمت تلقائياً — فيُنشَأ سدادٌ لمورّدٍ من مالٍ **دخل** الحساب، وتُقفَل
فاتورةٌ لم تُدفَع.

وأثرُه مضاعف: **إيرادٌ يُقرأ مصروفاً، ومستحقٌّ يختفي.**

كشفته مصفوفة التسوية من أوّل تشغيل.

### ٢. حوالتان متطابقتان في حسابين — تُمحى إحداهما

بصمة الحركة فريدة في النظام كلّه لا في حسابها. والإيجار يُدفَع من
حسابين. فتُقبَل الأولى وتُرَدّ الثانية «مستوردةً مسبقاً» — مالٌ خرج فعلاً
ولا أثر له.

### ٣. فراغٌ مزدوج يبتلع حركة

العدّ بالوصف الخام والبصمة بالوصف الموحَّد. فحركتان لا يفرّقهما إلّا
فراغ تأخذان الترتيب صفراً كلتاهما، ثمّ يوحّدهما التطبيع فتخرج لهما
بصمةٌ واحدة — فتُبتلَع الثانية.

### ٤. الدفعة المردودة تُحسَب مدفوعة

للدفعة حالٌ واحد ضمنيّ: «موجودة». فلا فرق بين ما لم يُخصَّص وما رُدَّ
مالُه — يُحسبان معاً في «المدفوع».

### ٥. الدفعة المقدَّمة تختفي

من دفع قبل وصول الفاتورة تُوسَم دفعتُه «متجاهَلة»، فيختفي ألفُ ريالٍ خرج
— ثمّ تُدفَع الفاتورة ثانيةً حين تصل.

### ٦. التراجع يحذف الدفعة

فتعود الفاتورة مستحقّةً بلا سببٍ ظاهر، ولا يبقى أثرٌ لأنّ مالاً خرج
ونُسب ثمّ رُدّ نسبُه.

### ٧. الرسم يُنسَب إلى المورّد

`splitBankFee` تُحسب ولا تبلغ التخصيص: تُقسَّم الدفعة كاملةً بما فيها
رسمُ التحويل. وأسوأ: الخصم الزائد بقدر الرسم يُسمّى `OVERPAYMENT` فيُرفَع
إلى المراجعة — فيُراجَع يدوياً ما يعرفه النظام يقيناً.

### ٨. حدثُ المصروف يُقيَّد مرّتين

القيود تمنع تكرار السجلّ عن **نفس** الحركة أو **نفس** الفاتورة. والحدث
الواحد يصل من مصدرين لا يعرف أحدهما الآخر — كشفِ البنك ومستندٍ رُفع —
فيمرّ سجلّان، ويعلو مصروف الشهر عمّا صُرف.

### ٩. «مؤسسة الرياض للتجارة» لا توجد مرّتين

الدليل فريدٌ في نوعه كلِّه، فصار الاسم حكراً على أوّل من سُجّل به.
والأسوأ في القراءة: `memory.set` يكتب فوق سابقه، فيفوز آخرُ صفٍّ بلا
ترتيبٍ مضمون — فتُصنَّف حركةُ هذه باسم تلك.

### ١٠. «سرد للتجارة» بلا كلمةٍ مميّزة

«سرد» ثلاثة أحرف فتُسقَط، و«للتجاره» عامّة فتُستبعَد. وهذا سبب عجز
مطابقة الـ١١٬٦٠٠ الحقيقية. وأسماء التجارة العربية كثيرٌ منها ثلاثيّ.

### ١١. «زاتكا» بالعربية تفوت التصنيف

القاعدة تعرف `ZATCA` لاتينيةً و«هيئه الزكاه والضريبه» كاملةً، لا النطق
العربيّ المختصر الذي يكتبه الأهليّ.

### ١٢. الزمن بلا اتّجاه

القياس بالقيمة المطلقة يُسوّي بين فاتورةٍ قبل الدفعة وفاتورةٍ بعدها.
فتُرجَّح فاتورةٌ لم تكن قد صدرت يوم الدفع.

### ١٣. بركة البحث تُقصّ بمعيارٍ لا صلة له بالسؤال

تُرتَّب بالمتبقّي تنازلياً وتُقصّ عند أربعين. فمورّدٌ له ستّون فاتورة
ودفعةٌ تسدّد ثلاثاً صغيرةً قريبة: تزاحمها الأربعون الكبرى فلا تدخل
البحث أصلاً.

### ١٤. «طوبقت ٣٠١ حركة» ليست «الحساب مضبوط»

لا معادلة أصلاً. وبدونها تمرّ ثلاثة أخطاء صامتة: حركةٌ لم تُقرأ، ومبلغٌ
قُرئ خطأً، وكشفٌ ناقص الطرف — وكلّها تُنتج مطابقةً تامّةً على ناقص.

---

## ج · ما أُصلح

| # | العلّة | الإصلاح |
|---|---|---|
| ١ | الوارد يُطابَق | `direction !== "DEBIT"` يخرج قبل الترشيح |
| ٢ | الهويّة بلا نطاق | `unique (coalesce(bank_account_id,'~'), external_id)` |
| ٣ | تطبيعان | تمثيلٌ واحد يُعدّ به ويُبصَم به |
| ٤ | حالُ الدفعة | سبعُ حالاتٍ تُشتقّ لا تُكتَب، وقيدٌ يمنع مخالفة العمود لأثره |
| ٥ | المقدَّمة | `ADVANCE` — مالٌ خرج ورصيدٌ ينتظر فاتورته |
| ٦ | التراجع يحذف | `REVERSED` بسببها ومن ردّها، والتخصيصات تُفَكّ |
| ٧ | الرسم | يُفصَل في `planned`، ويُحفَظ في `payments.fee_minor`، ويخرج من القسمة |
| ٨ | ازدواج المصروف | `event_key` — بصمةُ ما وقع، وقيدٌ على المشتقّ وحده |
| ٩ | فرادة الدليل | القاطع يُحتكَر، والظنّيّ يُشترَك — وإن اشتُرك سقط من الذاكرة |
| ١٠ | الاسم الثلاثيّ | `MIN_TOKEN_FALLBACK = 3` حين لا أطول منه |
| ١١ | «زاتكا» | أُضيفت إلى نمط `GOVERNMENT` |
| ١٢ | الزمن | نافذةٌ أمامية سبعة أيام بنصف درجة |
| ١٣ | البركة | تُرتَّب بالصلة، ويخرج ما جاوز الدفعة — حذفُ المستحيل |
| ١٤ | المعادلة | `balance-equation.ts` — مانعُ إقفالٍ وبندُ انتباه |

**وأُغلقت الحلقة في ثلاثة مواضع كانت مقطوعة:**

- **المسار الميّت** — غموضُ الجهة صار نوعاً مستقلاًّ له طبقةُ ترشيحٍ خاصّة.
- **الأسباب** — رموزٌ تُقابَل بما حسبه النظام؛ ومن ادّعى دليلاً لم يقع رُدّ سببُه بنصّه.
- **الأثر** — `adjudications` و`decision_history`، و`ruleId` لم يعد يُكتَب `null` عمداً.

**وأُضيف ما كان ناقصاً في الحلقة:**

- طابور مراجعةٍ موحَّد بثلاثة أعمالٍ مفصولة، وإقرارٌ جماعيّ يُعيد الحساب في الخادم.
- قراءةٌ بصرية للكشف المصوَّر بشروطها.
- محوِّلٌ لكلّ بنك، و`verified` يُعرَض للمستخدم.
- ملامحُ سدادٍ لكلّ مورّد، ترجّح ولا تحسم.
- تحويلُ وحداتٍ صريح، ولا جسر بين وزنٍ وحجم.
- مصفوفةُ تسويةٍ باثنتين وعشرين حالةً مسمّاة.
- مقاييسُ نظامٍ تُحسَب على ما فعله الإنسان وحده.

---

## د · ما بقي

### يمنع الإنتاج

1. **الهجرات ١٤–١٧ لم تُشغَّل بعد.** والمطبَّق على القاعدة اليوم
   `001`–`013` كاملةً — بما فيها `007`. وشروطُ الأربع الباقية فُحصت على
   بيانات الإنتاج فعلاً: صفرُ حركاتٍ مكرّرة يمنع `014`، وصفرُ دليلٍ
   قاطعٍ لجهتين، وصفرُ دفعةٍ سالبة يمنع `015`. فتُشغَّل بـ`db:migrate`
   ثمّ يُتحقَّق بـ`db:verify`.
2. **`GEMINI_API_KEY` و`GOOGLE_CLIENT_SECRET` ظهرا في محادثة** — يُدوَّران
   بيد أحمد؛ لا سبيل لي إلى ذلك.
3. **بيئة المعاينة في Vercel** — لم يُتحقَّق أتشارك قاعدة الإنتاج. إن
   كانت تشاركها فكلّ نشرٍ تجريبيّ يكتب في البيانات الحقيقية.

### لا يمنع، ويجب أن يُعرَف

4. **`xlsx` فيه ثغرتان عاليتان بلا إصلاحٍ في npm** — والتطبيق يقرأ بها
   ملفّات يرفعها المستخدم.
5. **خمسةٌ من ستّة محوِّلات بنوك لم تُجرَّب على كشفٍ حقيقيّ** — يُعرَض
   ذلك عند الاستيراد، ولا يُدَّعى غيرُه.
6. **`transaction_type` لنحو ٥٤٨ حركة قديمة** لا يُستعاد إلّا بإعادة
   استيراد الكشف.
7. **المقاييس بلا عيّنة بعد** — `db:measure` سيقول «لا يُقاس» حتى
   يتراكم عملُ إنسان. وهذا صحيح لا عطب.

---

## هـ · تغييرات القاعدة

| الهجرة | ما تفعل |
|---|---|
| `013_decision_provenance.sql` | `classification_source`؛ ثلاثة أعمدةٍ على الحركة؛ جدولا `adjudications` و`decision_history` |
| `014_identity_scoping.sql` | فرادة الحركة مقيَّدة بالحساب؛ `bank_imports.bank_account_id`؛ فرادة الدليل بحسب نوعه |
| `015_payment_lifecycle.sql` | `payment_status`؛ الرسم والمقدَّمة والردّ والإلغاء؛ أربعة قيود؛ وتصنيفُ القديم من تخصيصاته |
| `016_lifecycle_and_expense_events.sql` | `tx_lifecycle`؛ `expenses.event_key` — **بلا قيدٍ ولا حذف**، انظر أدناه |
| `017_sales_identity_and_units.sql` | `sales.branch_id`؛ فرادةُ سطر البيعة ودفعتها ودفعةِ التسوية؛ `content_unit` و`content_quantity` مع قيدَيهما |

**الجدولان الجديدان:** `adjudications` (٣٩ جدولاً الآن) و`decision_history`.

### قيدٌ كُتب ثمّ سُحب — وهذا أهمّ ما في القسم

`016` كان يحمل قيدَ فرادةٍ على `event_key` مسبوقاً بحذف المكرّر. وقبل
تشغيله فُحص أثرُه على بيانات الإنتاج، فتبيّن أنّه سيحذف **ثلاثة مصروفات
حقيقية**: فاتورتَي كهرباء وفاتورةَ اتصالات في يومٍ واحد بمبالغ متطابقة.

والسبب المباشر أنّ البصمة كانت تقتطع الوصف عند ستّين حرفاً، ولا يفرّق
بين هذه الفواتير إلّا **مرجعُ السداد** — وهو يقع بعد الحرف الستّين في
وصف الأهليّ.

والسبب الأعمق أنّ حدثين لهما **أثران مختلفان** — حركتان بنكيّتان
ببصمتين — هما حدثان، مهما تطابق كلُّ ما عداهما.

فأُصلح الأمران: البصمة تُلبَّد ولا تُقتطَع، والكشف يستثني ما اختلف أثرُه.
وسُحب القيد والحذف، وصار الازدواج **بنداً في «ما يحتاج انتباهك»** يقرّر
فيه إنسان.

**والقاعدة المستخلَصة:** قيدٌ يحذف بيانات مالية يُفحَص أثرُه على الإنتاج
**قبل** كتابته، لا بعد تشغيله.

---

## و · المسارات والخدمات

**جديدة:**
`src/lib/bank/` → `balance-equation` · `lifecycle` · `review-queue` ·
`evidence-uniqueness` · `supplier-profile` · `vision-statement` · `metrics` ·
`parsers/adapters`
`src/lib/` → `payment-state` · `unit-conversion`
`src/services/` → `bank-account.service` · `supplier-profile.service` ·
`statement-vision.service`
`src/app/` → `api/match-confirm-bulk/route.ts` · `review/page.tsx`
`src/components/review-workspace.tsx` · `scripts/measure-system.ts`

**مغيَّرة:**
`reconcile.service` (الاتّجاه · الرسم · الملامح · مصدر القراءة) ·
`payment.service` (الحال · الردّ · الرسم في التخصيص) ·
`counterparty.service` (فرادة الدليل · الذاكرة الملتبسة) ·
`statement-file.service` (البصريّ · حدود القراءة) ·
`expense.service` (بصمة الحدث) ·
`api/bank-import` · `api/match-confirm` · `api/match-undo` ·
`lib/bank/candidates` · `lib/bank/identity` · `lib/bank/classification` ·
`lib/extraction/extract` · `lib/extraction/schemas-by-kind` ·
`lib/month-close` · `lib/attention` · `lib/expenses` · `lib/nav` · `db/schema`

---

## ز · أثر الذكاء الاصطناعيّ

يُحفَظ في `adjudications` لكلّ حكم: المزوّد، والنموذج، ونسخةُ الموجِّه،
ونسخةُ المخطّط، والزمن، وثقةُ النموذج، وسببُه بنصّه، والرموزُ المدَّعاة،
والمقبولةُ منها، **والمردودة**.

وثلاثة قيود:

1. **حكمُه اقتراحٌ لا مطابقة** — لا يبلغ `AUTO` أبداً، وحكمُ الجهة `REVIEW` دائماً.
2. **لا يخترع مرشّحاً** — يختار من قائمةٍ حُسبت، ويُتحقَّق من اختياره في الخادم.
3. **ثقتُه إشارةٌ من ستّ** — لا حُكم.

والمزوّد منفصلٌ عن الاستخراج: `ADJUDICATOR_PROVIDER` (gemini · claude ·
deepseek · qwen)، و`EXTRACTION_PROVIDER`، و`VISION_PROVIDER` — ثلاثةُ
أعمالٍ مختلفة الحساسيّة، وربطُها بمتغيّر واحد يجعل تحسين أحدها يُقلق
الآخرين.

---

## ح · سلوك التسوية

`canonical → classify → resolve entity → candidates → global reconcile →
decide → adjudicate → planned`

- **الوارد يخرج قبل الترشيح.**
- **الحسم التلقائيّ بشرطين**: الدرجة فوق الحدّ **والفارق** عن التالي فوق حدّه.
- **لا يُحسَم تلقائياً**: الحلُّ التقريبيّ (`exact:false`)، وما قُرئ بصرياً.
- **التوزيع عالميّ** — تفريعٌ وتحديد على الفترة كلّها، فلا تُحجَز فاتورةٌ لحركتين.
- **الملامح تُرجّح ولا تحسم** — ٨٪ سقفاً، ولا تُبنى على أقلّ من خمس سابقات.
- **الرسم يُفصَل**، والمبلغ المسجَّل ما خرج فعلاً — وإلّا اختلّت المعادلة بمقدار الرسوم.

---

## ط · سلوك الكشف

ثلاثةُ طرق، وترتيبها مقصود:

1. **جدول** (Excel/CSV) → حسابيّاً.
2. **PDF نصّيّ** → نصٌّ ومواضع، ثمّ حسابيّاً.
3. **PDF مصوَّر** → بصرياً **إن هُيّئ مزوّد**، بشروطٍ أربعة: كلّ مبلغٍ
   يُقرأ نصّاً بالهللات، وكلّ تاريخٍ يُفحَص، **والمعادلةُ تردّ الكشف كلّه
   إن اختلّت**، ولا يُحسَم منه شيء تلقائياً. وإن لم يُهيَّأ مزوّد أُعلن
   السبب — ولا تُرجَع صفوفٌ فارغة تقول «الكشف فارغ».

وحدود القراءة تُعرَض **دائماً** لا عند الخطأ.

---

## ي · سلوك الحساب البنكيّ

- الحساب كيانٌ يُعرَّف عند أوّل ظهور من رقمٍ موحَّد؛ والمجهول يبقى مجهولاً.
- `bank_imports.bank_account_id` و`bank_transactions.bank_account_id` يُملآن.
- الفرادة `(coalesce(bank_account_id,'~'), external_id)` — والمجهول نطاقٌ
  واحد كي يبقى منعُ التكرار عاملاً قبل أن تُعرف الحسابات.
- المعادلة تُفحَص لكلّ فترة، والفجوة تُحسب، وكلاهما مانعُ إقفال.

---

## ك · مصفوفة الاختبارات

اثنتان وعشرون حالةً مسمّاة في `src/services/reconcile-matrix.test.ts`:
المبلغ (٦) · الجهة (٥) · التنازع (٤) · الزمن (٤) · ما ليس سداداً (٣).

وتفحص **السلوك** لا الدوالّ، و**القرار المكتوب** (`planned`) لا الدرجة.
وكشفت عطبين حقيقيّين من أوّل تشغيل: الوارد يُطابَق، و«زاتكا» تفوت.

**المجموع:** ١٬١٣٦ اختباراً في ٧٧ ملفاً.

---

## ل · جاهزية الإنتاج

**الشيفرة جاهزة. القاعدة ليست.**

`typecheck` و`lint` و`build` نظيفة، و١٬١٣٦ اختباراً تمرّ. لكنّ الهجرات
١٣–١٧ لم تُشغَّل، و`007` فشلت من قبل. فما لم تُشغَّل، الشيفرة تكتب أعمدةً
لا وجود لها.

**الترتيب:** `db:migrate` → `db:verify` → `db:measure`. وشروطُ الهجرات
الأربع فُحصت على الإنتاج ولا مانع فيها.

---

## م · جاهزية Foodics

**الأساس مبنيّ، والموصل غير مبنيّ — عمداً.**

الجداول قائمة وفارغة: `sales` `sale_lines` `sale_payments` `refunds`
`refund_lines` `settlement_batches` `pos_products` `sales_sources`.
وواجهة `SalesConnector` معرَّفة بلا تطبيق، و`notConnected` **يرمي**
`NotConnectedError` ولا يُرجع أصفاراً.

وبعد هذه الجولة صار الأساس يحتمل الوصل: فرعٌ للبيعة، وهويّةٌ لسطرها
ولدفعتها ولدفعة التسوية، وتحويلُ وحداتٍ للأصناف.

---

## ن · ما يجب قبل Foodics

1. **تشغيل الهجرات ١٤–١٧** — الشرط الأوّل.
2. **دورةُ تسويةٍ كاملة على كشفٍ حقيقيّ**: استيراد → طابور → إقرار →
   معادلةٌ متّزنة → إقفالٌ يمرّ. حتى تُقاس `db:measure` بعيّنةٍ حقيقية.
3. **تدوير المفتاحين، وحسمُ سؤال قاعدة المعاينة.**
4. **فرعٌ واحد على الأقلّ في `branches`** — وإلّا نُسبت المبيعات إلى
   `null` ثمّ صُحّحت بالحدس.
5. **مطابقةُ دفعات التسوية بحركات البنك** على بيانات حقيقية — فهي الجسر،
   وكسرُها يجعل المبيعات رقماً لا يُقابله شيء.

---

## س · ما يجب ألّا يُبنى بعد

بنصّ المراجع، وأُبقيه:

- ❌ موصل Foodics
- ❌ «المدير المالي» الذكيّ
- ❌ المخزون والوصفات والتنبّؤ
- ❌ ميزات لوحةٍ جديدة
- ❌ تبديل النماذج
- ❌ أيّ ميزةِ منتَج جديدة

**والسبب واحد:** الحلقة أُغلقت الآن — لكنّها لم تدُر بعد على بياناتٍ
حقيقية دورةً كاملة. وكلّ ما يُضاف قبل ذلك يُبنى على حلقةٍ لم تُختبَر.
