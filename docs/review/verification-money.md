> تقرير وكيل التحقّق المستقلّ الأوّل (المال والبيانات)، نُقل آلياً من ردّه.

# تقرير التحقّق المستقلّ

كلّ ما يلي قراءةٌ فقط. لم يُعدَّل ملفّ، ولم يُكتب في القاعدة، ولم يُشغَّل نصّ ولا خادم. ولكلّ ادّعاء أربعة: الحكم، والدليل، والدرجة، ثمّ ما جرّبتُه لنقضه.

**أهمّ ما خرج:**
- **صمدت أربعة ادّعاءات كما هي:** FIN-003 وFIN-005 وSCN-015 وUX-001.
- **صمدت أربعة أخرى، ومعها تفاصيل تغيّر فهمها:** FIN-002 وFIN-007 وSCN-003/004 وادّعاء المنسّق.
- **ثلاثة جزئيّة:** FIN-004 وFIN-006 وFIN-008.
- **لم ينتقض ادّعاءٌ واحد.**
- **أخطر ما وجدتُه:**
  - في FIN-002: الازدواج في أطلس وقع عبر المسار الذي **يستدعي** `findPaymentTwin` فعلاً. فالخلل في الدالّة نفسها لا في غيابها عن المسارات الأخرى.
  - في FIN-008: «شرط السباق» في `match-confirm-bulk` لا يمنع الدفعة الثانية أصلاً.

---

## 1. FIN-002: دفعتان لواقعةٍ واحدة
**الحكم: صمد، مع تصحيحٍ لموضع الخلل.**

**الدليل:**
```
Lava 2026-08-07 94500:
  e8772684  APPLIED → فاتورة 05 (2026-08-01)، لها حركة بنك BANK_REF:120484659، أُنشئت 09-03 17:59
  15bd5c9f  APPLIED → فاتورة 003 (2026-06-02)، بلا حركة ولا مستند، أُنشئت 09-04 00:52
Atlas 2026-08-13 57500:
  e578c109  APPLIED → INV-2026-05297، بلا حركة ولا مستند، أُنشئت 09-04 01:10
  746e7f22  UNAPPLIED، لها حركة BANK_REF:100965156، أُنشئت 09-07 23:19
```
- في كلّ بيانات البنك (من 05-08 إلى 09-03، ١٤٤٠ حركة) حركةٌ واحدة فقط بمبلغ 94500، وواحدة فقط بمبلغ 57500.
- فحصتُ كلّ المجموعات (مورّد · يوم · مبلغ) في الدفعات غير الملغاة: لا يتكرّر منها إلّا هاتان.

**مسارات الإنشاء بلا فحص توأم:** كلّها كما قيل.
- `archive/route.ts:265`
- `bank-import/route.ts:768`
- `mark-paid/route.ts:85`
- `match-confirm-bulk/route.ts:246`
- `match-confirm/route.ts:118` و`:310` و`:431`

والاستدعاء موجودٌ في `match-confirm/route.ts:562` و`drive-sync/route.ts:312` وحدهما.

**التصحيح المهمّ:** دفعة أطلس الثانية (746e7f22) أُنشئت من `settleAccounts`، أي من المسار الذي يستدعي `findPaymentTwin`.
- الدليل في `decision_history`: «سدادٌ على حساب المورّد — لا فاتورة مفتوحة تقابله».
- وجدت الدالّة التوأمَ e578c109، لكنّ شرط التبنّي في `match-confirm/route.ts:565` يشترط أن يكون التوأم **بلا تخصيص**: `!twin.hasBankRow && twin.allocatedMinor === 0`. والتوأم كان مخصَّصاً، فأُنشئت دفعةٌ جديدة.
- إذن **إضافة الاستدعاء إلى بقيّة المسارات لا تكفي**. الحالة التي أنتجت الخطأ فعلاً (توأمٌ مخصَّص بلا حركة بنك) لا يغطّيها التصميم.
- مصدر الدفعتين اللتين بلا حركة (09-04 بين 00:52 و01:10): **لا يُحسم**، فلا أثر لهما في `audit_logs`. أمّا `mark-paid` فمستبعَد، لأنّه يضع تاريخ الدفع تاريخَ الفاتورة، و003 مؤرّخة 06-02.

**الدرجة:** P1، والمبلغ المكرَّر ١٬٥٢٠ ريالاً اليوم.

**ما جرّبتُه لنقضه:** بحثتُ عن حركة بنكٍ ثانية تبرّر الدفعة، وعن قيد فرادة في `payments`. لا يوجد إلّا `document_id` فريداً، ولا مؤثِّر على `payments`.

## 2. FIN-003: المسدَّد لا يرى الدفعات غير المخصَّصة
**الحكم: صمد.**

**الدليل:**
- في صفحة المورّد `suppliers/[slug]/page.tsx`:
  - `:57-58` المسدَّد = `payment_allocations` وحدها.
  - `:80` `balance = billed - paid`.
  - `:91` `buildSupplierAccount({paidMinor: n("paid")})`.
- في القائمة `suppliers/page.tsx`: `:39-44` والرصيد في `:156`.
- و`supplier-account.ts:81` يحسب ما يُمرَّر إليه فقط.

**غاناش بالأرقام:** المفوتر 308660، والمخصَّص 308660، ومجموع الدفعات 3218160، وغير المخصَّص **2909500**.
- المستحقّ المعروض = صفر.
- آخر كشف (08-31) رصيده 543260، فالفرق = +5,432.60، والجملة المعروضة «يطالب بأكثر ممّا نعرف» (`supplier-account.ts:135`).
- والحقيقة: دفعنا له ٢٩٬٠٩٥ لا يراها الحساب.

**الدرجة:** P1، لأنّه يوجّه صاحب العمل إلى دفعٍ زائد.

**ما جرّبتُه لنقضه:** بحثتُ عن طرحٍ للدفعات غير المخصَّصة أو للرسوم في الصفحة، فلا شيء. رسوم غاناش صفر، فلا تفسّر الفرق.

## 3. FIN-004: الكوب الذهبي
**الحكم: جزئيّ.** الوقائع صحيحة، لكنّ نصفها سياسةٌ موثّقة.

**الدليل:**
```
INV263287 (2026-05) 13,506.75 ← 7500 + 4503.13 + 1150 + 201.25 = 13,354.38 (بقي 152.37)
INV264916 (2026-07) 12,003.13 ← لا تخصيص
كشف واحد: 07-01..07-15 closing 1200313، بلا أسطر
```
- **النصف الأوّل سياسة:** وضعُ كلّ الحوالات على أقدم فاتورة هو «الأقدم أوّلاً»، مع نافذة سبعة أيّام (`allocation.ts:94-114`). وهذا موثّق في `CLAUDE.md` قراراً مقصوداً، فهو ليس عطباً في ذاته.
  - ويبقى تناقضٌ لافت: الكشف يقول المستحقّ 12,003.13، أي فاتورة يوليو وحدها، ممّا يعني أنّ المورّد عدّ مايو مسدَّدة في 07-15. بينما ثلاث من حوالاتنا جاءت بعد ذلك التاريخ.
- **النصف الثاني عيبٌ حقيقيّ:** الصفحة تأخذ آخر رصيد ختاميّ بلا اعتبارٍ لتاريخه (`[slug]/page.tsx:68-70`)، وتقارنه بالرصيد اليوم (`:91`).
  - الحساب: 25,509.88 − 13,354.38 = 12,155.50، والفرق = 12,003.13 − 12,155.50 = **−152.37**، وهو مطابق للادّعاء.

**الدرجة:** P2 للمقارنة الزمنيّة. أمّا التخصيص فليس خطأً ما لم تُراجَع السياسة.

**ما جرّبتُه لنقضه:** تأكّدتُ أنّ الدفعة المردودة (7500 REVERSED) بلا تخصيص، فلا تدخل الحساب.

## 4. FIN-005: قائمة الدخل تحسب PERSONAL مصروفاً
**الحكم: صمد.**

**الدليل:**
- `money/statement/page.tsx:62-69` و`money/page.tsx:50-54` تستثنيان `INTERNAL` و`UNKNOWN` و`SUPPLIER` فقط.
- `expenses.ts:55-57` تستثني `PERSONAL` أيضاً، و`:75-85` تستثني «شراء بضاعة».
- `cashflow.ts:167-185` تجمع ما يصلها بلا تصفية.

**بالأرقام:**
- `PERSONAL` = ٣٨ حركة بمبلغ **112,722.18**، من مصروفٍ تشغيليّ كلّيّ قدره 222,098.40 (٥١٪).
- حركات «بضاعة» داخل المصروف التشغيليّ: ١٢ حركة بمبلغ 14,863:
  - `SALARY` أربع بمبلغ 5,678
  - `PERSONAL` ستّ بمبلغ 8,685 (داخلةٌ أصلاً في رقم `PERSONAL`)
  - `OTHER` اثنتان بمبلغ 500

**الدرجة:** P1، فالرقم التشغيليّ متضخّم إلى نحو الضعف.

**ما جرّبتُه لنقضه:** بحثتُ عن تصفيةٍ داخل `buildProfitLoss`، فلا شيء.

## 5. FIN-006: جدول `expenses` لا يتبع إعادة التصنيف
**الحكم: جزئيّ.** الشقّ الأوّل صحيح، والثاني أغلبه استثناءٌ مقصود.

**الدليل من الشيفرة:** `expense.service.ts:61-77` يستبعد كلّ حركةٍ لها صفّ مسبق (`alreadyRecorded`). ولا مسار يحذف صفّاً أو يعيد تصنيفه غير الحذف اليدويّ في `deleteExpense:220`. وآخر إنشاءٍ للصفوف كان في 09-06 04:40.

**الشقّ الأوّل صحيح:** في الجدول أربعة صفوف لحركاتٍ صارت اليوم `PERSONAL`، مجموعها **22,724**:
- ثلاثة رواتب لأحمد (5,500 و5,500 و10,724)
- وواحد «تحويل الى الاهل» بمبلغ 1,000
- وفوقها ٢٥ صفّاً `BANK_FEE` صارت حركاتها `BANK_VAT` (5.03 ريالات، انحرافُ بابٍ لا نقص).

**الشقّ الثاني أغلبه مقصود:** الحركات الناقصة المصنَّفة `SALARY` ثمانٍ بمبلغ 5,758، منها أربع بمبلغ 5,678 وصفها «شراء بضاع».
- وهذه يستثنيها `looksLikeGoodsPurchase` عمداً، وكذلك `OTHER` الاثنتان (500).
- الناقص فعلاً: رواتب أربعة بعشرين ريالاً لكلٍّ منها ووصفها فارغ (80)، ورسوم صغيرة (`BANK_FEE` 17.78 · `POS_FEE` 102.87 · `POS_VAT` 15.42 · `BANK_VAT` 0.23).

**الدرجة:** P1 للصفوف الشخصيّة، وP2 للباقي.

## 6. FIN-007: إقفال الشهر بلا كشف ولا معادلة
**الحكم: صمد.**

**الدليل:**
- `month-close.ts:174-180`: غياب الكشف `WARN`.
- `:189`: إن غاب الكشف لا يُضاف بندا الفجوة والمعادلة أصلاً.
- `:211-216`: المعادلة المجهولة `WARN`.
- `month-close/route.ts:91` لا يمنع إلّا `BLOCK`.
- `reconciliation_periods` = **٠ صفوف**، ولا كاتب لها. المواضع الوحيدة قراءات: `month-close-facts.ts:135` و`attention-facts.ts:193` و`production-gate.ts:158`.
- إذن المعادلة `UNKNOWN` دائماً، فهي `WARN` دائماً.

**الدرجة:** P1.

**ما جرّبتُه لنقضه:** الفجوة `BLOCK` فعلاً، لكنّها لا تُفحص إلّا بعد وجود حركة في الشهر. ولا مؤثِّر في القاعدة على `month_closes`.

## 7. FIN-008: سباق طلبين على الحركة نفسها
**الحكم: جزئيّ.** القاعدة تمنع حالةً واحدة فقط.

**الدليل:**
- الفحص خارج المعاملة في `match-confirm/route.ts:87` و`:228`.
- التحديثات بلا شرط `is null` في `:127-138` و`:321-331` و`:442-452` و`:596-607`.

**ما في القاعدة (قرأتُه من `pg_trigger` و`pg_indexes` و`pg_constraint`):**
- لا مؤثِّر على `bank_transactions` ولا على `payments`.
- لا قيد فرادة على `matched_payment_id`.
- المؤثِّر الوحيد `payment_allocations_bounds`، ونسخة 026 مطبَّقة (`prosrc` فيه `for update`). يقفل الفاتورة والدفعة ويرفض التخصيص الزائد.

**النتيجة:**
- **تُمنع** الدفعة المزدوجة حين يملأ التخصيص المتبقّي على الفاتورة: الطلب الثاني يُرفض، والرفض 500 لا 409.
- **لا تُمنع** في ثلاث حالات، فتُنشأ دفعتان والحركة تشير إلى آخرهما:
  - دفعة مقدَّمة `ADVANCE` (بلا تخصيص)
  - `settleSupplier` بلا فاتورة مفتوحة، أو مع باقٍ غير مخصَّص
  - تخصيص جزئيّ على فاتورةٍ متبقّيها ضعف المبلغ فأكثر

**اكتشافٌ إضافيّ:** `match-confirm-bulk/route.ts:270-275` يضيف `is null` إلى التحديث، لكن بلا `.returning()` وبلا فحص عدد الصفوف المتأثّرة. فالدفعة والتخصيص يُودَعان ولو لم يتأثّر أيّ صفّ. أي أنّ «شرط السباق» يمنع الكتابة فوق الحركة فقط، **ولا يمنع الدفعة الثانية**.

**في الواجهة:** `review-workspace.tsx` يعطّل الزرّ بـ`busy` داخل التبويب نفسه فقط.

**الدرجة:** P1.

## 8. SCN-003 وSCN-004: القشرة و`/review` صفر، والرئيسة تعرض بنوداً حرجة
**الحكم: صمد.**

**الدليل:**
- شرط `pendingDecision()` (`pending.ts:41-73`) يعطي **0**. فالقشرة (`page-shell.tsx:46`) و`/review` (`review/page.tsx:59`) تعرضان صفراً.
- `attention-facts` تحسب ثلاث مجموعات سداد مزدوج في 07-15 (مرافق):
  - STC 287.50 + كهرباء 363.88 + كهرباء 1,699.39 = **2,350.77**
  - مراجع السداد مختلفة، والبند `CRITICAL` (`attention.ts:260-275`).
- والدفعات «بلا فاتورة» **14 دفعة بمبلغ 38,605.00**، والبند `HIGH` (`:285-298`).
- **SCN-004 صمد:** البند الحرج يحيل إلى `/bank` (`attention.ts:271`)، و`double-paid` لا يُستورَد إلّا في `attention-facts.ts` و`double-paid.ts`. صفحة البنك لا تعرضه.

**ملاحظة إنصاف:** «العمل الباقي» معرَّفٌ على الحركات لا على الدفعات، فالعدّان يقيسان شيئين مختلفين. لكنّ `CLAUDE.md` يصف `countPendingWork()` بأنّه «مصدرٌ واحد للعمل الباقي»، فالتناقض قائم.

**الدرجة:** P1 لـSCN-004 (بندٌ حرج بلا موضع إصلاح)، وP2 لـSCN-003.

## 9. SCN-015: الكتابة في شهرٍ مقفل
**الحكم: صمد.** والخطر كامنٌ اليوم لا واقع.

**الدليل:**
- `assertMonthOpen` لا يرد في المصدر وlا في النصوص إلّا في `archive/route.ts:170`.
- لا مؤثِّر في القاعدة يفحص `month_closes`.
- `month_closes` اليوم **فارغ**، فلا شهر مقفلاً فعلاً.

**الدرجة:** P1، ويصير أثره واقعاً مع أوّل إقفال.

## 10. UX-001: بطاقة البنك تعرض ٢٢ و`/review` فارغ
**الحكم: صمد.** مع ملاحظة أنّ نصّ البطاقة الحاليّ ليس «تحتاج».

**الدليل:**
- `bank/page.tsx:50-51` يعدّ `match_disposition='REVIEW' or category='UNKNOWN'` فيعطي **22**.
- البطاقة في `:290-298`، وعنوانها «مجهولة أو متقاربة المرشّحين»، ووصفها «جزءٌ من طابور المراجعة»، ورابطها `/review`.
- التقاطع مع `pendingDecision` = **0**.
- الاثنتان والعشرون كلّها قراراتٌ قديمة لم تُطوَ:
  - MATCHED/POSTED: ١٤ (39,724.89)
  - MATCHED/CONFIRMED: ٧ (9,971.25)
  - IGNORED/INFERRED: ١ (1,100)
- ولا حركة `UNKNOWN` واحدة.

**الدرجة:** P2.

## 11. ادّعاء المنسّق: السيناريو ٨ في `certify-flow.ts`
**الحكم: صمد.** ولا أثر تدقيقٍ يبقى بعده.

**الدليل:**
- رأس الملفّ `:5` يقول «لا يكتب شيئاً»، وسطر الطباعة `:146` يقول «لا يُكتَب شيء»، و`CLAUDE.md` يقول «كلّ سيناريو يُلغى أثرُه».
- السيناريو ٨ (`:386-455`) يخالف ذلك:
  - يكتب بـ`db` مباشرةً: مورّداً، ومستنداً وفاتورة عبر `makeInvoice(db, …)`، ودفعتين في معاملتين حقيقيّتين مودَعتين.
  - ثمّ يحذفها بـ`delete` صريح (`:434-438`).
  - وتعليقه في `:388` يعترف بذلك.
- **لا يبقى أثرٌ في سجلّ التدقيق:**
  - `createPayment` (`payment.service.ts:103-124`) لا يكتب تدقيقاً.
  - لا مؤثِّر على `payments` يكتب في `audit_logs`.
  - البحث في `audit_logs` عن «certify» أو «تزاحم» = لا شيء.
  - ولا بقايا اليوم: مورّدون `certify%` = 0، مستندات = 0، فواتير `RACE-`/`CERT-` = 0.
- **الخطر الحقيقيّ:** لو انقطع التشغيل بين الإدراج والحذف لبقي مورّدٌ نشط وفاتورة ودفعة في قاعدة الإنتاج. وخلال التشغيل يظهر «مورّد تزاحم» للمستخدمين.

**الدرجة:** P2، فهو تناقضٌ في الوثيقة وكتابةٌ في الإنتاج بلا أثرٍ دائم.

---

## ملفّات مرجعيّة
- `/Users/aj/Desktop/TPH Invoicing/src/app/api/match-confirm/route.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/app/api/match-confirm-bulk/route.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/services/payment.service.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/app/suppliers/[slug]/page.tsx`
- `/Users/aj/Desktop/TPH Invoicing/src/app/suppliers/page.tsx`
- `/Users/aj/Desktop/TPH Invoicing/src/lib/supplier-account.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/app/money/statement/page.tsx`
- `/Users/aj/Desktop/TPH Invoicing/src/app/money/page.tsx`
- `/Users/aj/Desktop/TPH Invoicing/src/lib/expenses.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/services/expense.service.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/lib/month-close.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/lib/month-close-facts.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/lib/bank/pending.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/lib/attention.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/lib/attention-facts.ts`
- `/Users/aj/Desktop/TPH Invoicing/src/app/bank/page.tsx`
- `/Users/aj/Desktop/TPH Invoicing/scripts/certify-flow.ts`
- `/Users/aj/Desktop/TPH Invoicing/drizzle/sql/026_allocation_bounds_serialize.sql`