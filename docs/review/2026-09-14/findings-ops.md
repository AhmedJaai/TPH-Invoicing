# مراجعة التشغيل والأداء (OPS): ١٤ و١٥ سبتمبر ٢٠٢٦

**ما فُحص:** فرع `main` عند `375f0d7`، في النسخة المعزولة وحدها (`sbx tph_ops`، وخادم إنتاجيّ على `127.0.0.1:3600`). لم يُلمَس الإنتاج، ولا DeepSeek الحقيقيّ، ولا الدرايف الحقيقيّ.

**التنظيف في النهاية:** أوقفتُ الخادم. أعدتُ `log_min_duration_statement` إلى `-1`. حذفتُ قاعدة `tph_restore`. وأعدتُ `tph_ops` إلى لقطة الإنتاج بـ`reset-db.mjs`، لأنّ تجربة الاستيراد أضافت إليها ١٣١٨ حركة.

**الملفّات:**
- `docs/review/2026-09-14/coverage-ops.md` كُتب.
- `findings-ops.md` رفضت الأداة كتابته، فالتقرير الكامل أدناه لينقله المنسّق.

**العدد:** P0: ٠ · P1: ٣ · P2: ٥ · P3: ٤ · مشتبَه بها: ٤.

**لا أُعيد إثبات ما ثبت عند غيري، وأذكره بمعرّفه:**
- SEC-106: الأرشفة ترفع إلى الدرايف قبل القيد، ثمّ تردّ ٥٠٠.
- SEC-109: مبلغ ضخم يُسقط الطلب بـ٥٠٠ من تجاوز `integer`.
- SEC-110: `instrumentation.ts` يسجّل params.
- FIN-013: `sum(...)::int` في ٢٢ موضعاً.
- FIN-107: `payment_month` معرَّفة IMMUTABLE وهي تتبع المنطقة الزمنيّة.
- SCN-103: الاستيراد يكتب في الشهر المقفل.
- SCN-106: الإعادة بعد الانقطاع تنادي الذكاء ثانيةً رغم `extraction_cache`.
- SCN-109: استيراد فارغ يكتب صفّاً في `bank_imports`.
- SCN-111: المزامنة بالاسم لا تحفظ البصمة.
- `ops:certify` يكتب ثمّ يحذف، ويكتب `certify-result.json` داخل المستودع.

---

## ٠. حال ملاحظات المراجعة السابقة

| القديم | الحال | الدليل |
|---|---|---|
| OPS-001: «عرّف الجهة» يسقط لأنّه يكتب بـ`db` داخل المعاملة | **أُصلحت** | `counterparty/route.ts:167-261` يكتب بـ`t` دفعاتٍ مجمَّعة، ويحرسه `src/lib/code-guards.test.ts:59-72` |
| OPS-002: مهلة الذكاء أطول من عمر المسار | **جزئيّة** | `withDeadline(55_000)` مُثبَت: `/api/analyze` توقّف عند 52.08s و`/api/statement-reconcile` عند 52.07s. الناقص: `/api/ai-analysis` استمرّ **144.2s** (OPS-101)، والكتابة بعد التحكيم في الاستيراد بلا ميزانيّة (OPS-104) |
| OPS-003: `res.json()` خامّ في الشاشات | **أُصلحت** | `src/lib/http-client.ts`، ويحرسها `code-guards.test.ts:107` |
| OPS-004: لا مراقبة | **جزئيّة** | `instrumentation.ts` يكتب إلى `console.error`، و`/api/health` يسأل المزوّد والدرايف للمخوَّل. لكن لا تنبيه، والفحص يردّ HTTP 200 وجسمه `healthy:false` (OPS-105) |
| OPS-005: لا نسخ احتياطيّ مُجرَّب | **جزئيّة** | بند `backup_restore` صار في البوّابة ويبقى `?`. لا إجراء في المستودع. والاستعادة الساذجة تسقط (OPS-103) |
| OPS-006: النشر يدويّ، والتطوير على قاعدة الإنتاج | **قائمة** | إعداد `tph-invoicing` في `.claude/launch.json` (OPS-106) |
| OPS-007: المزامنة تعيد تسمية الملفّات | **أُصلحت** | `drive-sync/route.ts:447-460`، و`code-guards.test.ts:89` يمنع `renameFile` خارج `/api/drive-rename` |
| OPS-008: البوّابة تقول PASS على دليل قديم | **جزئيّة** | أُضيف `STALE_DAYS = 7`، لكن لا ربط بالكوميت، فيبقى PASS على شهادة عمرها ٦ أيّام (OPS-107) |
| OPS-009: الاستيراد رحلة لكلّ صفّ | **قائمة، ومُثبَتة** | ١٤٣٦ صفّاً جديداً = **٢٧٧٥ استعلاماً** في معاملة واحدة (OPS-104) |
| OPS-010: حدود الحجم فوق حدّ المنصّة | **جزئيّة** | analyze ٤ م.ب، وarchive ٣، وbank-import ٤. لكن `statement-reconcile/route.ts:35` ما زال **٢٥ م.ب** (OPS-112) |
| OPS-011: الهجرات بلا قفل | **أُصلحت (مُتتبَّعة)** | `scripts/migrate.ts:43` يطلب `pg_try_advisory_lock`، و`:30-33` يشترط `--reapply`. لم أشغّله لأنّ مصنِّف الأدوات رفض |
| OPS-012: سحب نموذج `-exp` بلا تنبيه | **جزئيّة** | `models.ts:36-40` بلا `-exp`، و٤٠٤ يُعلَن «النموذج deepseek-flash غير متاح لهذا المفتاح» (مُثبَت). لكن `text_source` فارغ في ١٦٨ من ١٧٥ مستنداً، و`/api/health` لا يفحص وجود اسم النموذج |
| OPS-013: خطّة Vercel Hobby | **قائمة** | `docs/SETUP.md` §٦ يقرّها ويحيلها إلى أحمد |
| OPS-014: الدرايف قبل القاعدة | **قائمة** | ⟵ SEC-106 (`archive/route.ts:253` يرفع ثمّ `:263` يفتح المعاملة) |
| OPS-015: القراءة قبل «أهو مرفوع؟» | **أُصلحت، ومُثبَتة** | رفع الملفّ الأصليّ ردّ **409 في 5ms** بلا أيّ نداء ذكاء (`analyze/route.ts:106-119`) |
| OPS-016: فهارس ناقصة | **أُصلحت** | `029_ops_indexes.sql` |
| OPS-017: `middleware` مهجور | **أُصلحت** | `src/proxy.ts` موجود، و`src/middleware.ts` غائب، ومخرَج البناء يقول «ƒ Proxy (Middleware)» |
| OPS-018: `product.service` صفّاً صفّاً | **لم يُفحَص** | — |

---

## ١. خطّ الأساس ومطابقة أرقام الوثائق

| البند | الواقع | ما في CLAUDE.md | الحكم |
|---|---|---|---|
| typecheck / lint / build / audit | ✓: 5s / 5s / 9s / 0 ثغرات | — | سليم |
| الاختبارات | 1801 في 102 ملفّاً، 2.38s | «١٨٠١ اختباراً · ١٠٢ ملفاً» | مطابق |
| الجداول | ٤٢ في `public`، وهي ٤١ جدولاً مع `schema_migrations`. و٩ في `neon_auth` | «٤١ جدولاً» | مطابق لـ`schema.ts` (٤١ `pgTable`). `neon_auth` لا يرد في الشيفرة ولا الوثائق، وكلّ جداوله فارغة إلّا `project_config` (صفّ واحد). هو أثر ميزة Neon Auth، ولا يستعمله النظام (OPS-110) |
| الهجرات | ٣١ في المجلّد = ٣١ مطبَّقة | ٣١ | مطابق |
| جدول «المكتبات التي يجب معرفتها» | كلّ الملفّات المذكورة موجودة | — | مطابق |
| أوامر `package.json` | ٩ أوامر لا تذكرها CLAUDE.md: `db:remove-dupe`، `db:missing-invoices`، `db:backfill-invoices`، `bench:extraction`، `drive:migrate`، `try:bank`، `try:statement`، `db:repair-months`، `ops:real-bank` | — | OPS-110 |
| حجم القاعدة | 16 MB | — | — |

### بوّابة الإنتاج (`sbx tph_ops npm run ops:gate`، خرجت بالرمز 1)

| البند | النتيجة | التفصيل |
|---|---|---|
| سلامة الهجرات | PASS | 31، وهي عين ما في المستودع |
| سلامة القاعدة | PASS | 9 أعمدة موجودة |
| الثوابت المالية | PASS | 5 من 5 |
| تسوية البنك | ? | 1440 حركة، بلا رصيد افتتاحيّ ولا ختاميّ |
| دورة الفاتورة | PASS | 141 فاتورة |
| دورة الدفعة | PASS | — |
| كشوف المورّدين | FAIL | 3 من 14 بلا مطابقة |
| إقفال الشهر | ? | لم يُقفَل شهر |
| مسار المراجعة | PASS | 21 إقراراً، وردّ واحد، و47 حركة أقدم من السجلّ |
| سجلّ التدقيق | PASS | 295 سجلّاً، و3 مؤثِّرات |
| سلامة الأرشيف | PASS | 175 مستنداً، ولا شيء بلا رابط |
| عزل المعاينة | ? | لا إقرار |
| الفحص الأمنيّ | ? | لا إقرار بتدوير المفاتيح |
| النسخ والاستعادة | ? | لا إقرار |
| الدورة كاملةً | **PASS** | «11 من 11 · قبل 6 أيّام» (OPS-107) |
| التحقّق على بيانات حقيقيّة | FAIL | مفقود 5، وعمره 8 أيّام |

الحكم الذي تطبعه البوّابة: «غير جاهز: بندان فشل و5 لم يُفحَص».

**النصوص الآمنة التي شُغّلت:**

| النصّ | الخروج | الزمن | ما قاله |
|---|---|---|---|
| `check-isolation` | 0 | <1s | — |
| `audit-data` | 0 | 1s | 141 فاتورة بـ138,803.65 ريال، غير المسدَّد منها 6,851.36 |
| `measure-system` | 0 | <1s | 87 حركة لم تبلغ قراراً |
| `diagnose-unpaid` | 0 | 1s | — |
| `find-split-suppliers` | 0 | <1s | لا مورّد مسجَّل مرّتين بدليلٍ ماليّ |

ورفض مصنِّف الأدوات تشغيل: `migrate`، و`verify-invariants`، و`identity-report`، و`link-counterparties`، و`missing-invoices`.

---

## ٢. المهلات لكلّ مسار

**التجربة:** رتّبتُ للمزيَّف طابوراً من ١٢ ردّاً متأخّراً ٧٠ ثانية، واستدعيتُ ثلاثة مسارات معاً، مع عيّنة من `pg_stat_activity` كلّ ٤ ثوانٍ.

**احتجاز اتّصال القاعدة:** طوال الانتظار لم تظهر حالة `idle in transaction`، ولا اتّصال محجوز واحد. العيّنات من الثانية 16 إلى 92 فارغة (`[]`). فالنداء الخارجيّ لا يقع داخل معاملة في أيّ من المسارات الثلاثة.

| المسار | `maxDuration` | أطول مسار داخليّ (مُتتبَّع) | الزمن المقيس | الحكم |
|---|---|---|---|---|
| `/api/analyze` | 60 | `withDeadline(55s)`: محاولة واحدة حتى 52s، ثمّ لا يُبدأ ما بقي | **52.08s، وردّ 502** | سليم. لكنّ الرسالة تُسقط السبب (OPS-109) |
| `/api/statement-reconcile` | 60 | تنزيل من الدرايف، ثمّ `withDeadline(55s)`، ثمّ إدراج لكلّ سطر | **52.07s، وردّ 502** | سليم زمناً. حدّ الحجم 25MB (OPS-112) |
| `/api/ai-analysis` | 60 | **خارج `withDeadline`**. `timeoutMs = 48s` و`AbortSignal.timeout(48s)`، × ٣ محاولات | **144.2s، وردّ 502** | **P1 · OPS-101** |
| `/api/drive-sync` | 60 | المشي 20s، والقراءة تُسأل عند الباب حتى 28s، ثمّ `withDeadline(55s)`. أسوأ حالة نحو 52s ثمّ الكتابة | لم يُجرَّب حيّاً | مُتتبَّع، سليم |
| `/api/bank-import` | 60 | التحكيم حتى 25 نداءً تحت 55s، **ثمّ معاملة الإدراج بلا ميزانيّة** | الكشف المعروف 0.22s؛ 1436 صفّاً جديداً 0.59s محلّياً | OPS-104 |
| `/api/archive` | 60 | رفع إلى الدرايف ثمّ معاملة | لم يُقَس | ⟵ SEC-106 |
| `/api/drive-rename` | 60 | حتى 25 تسمية، و`googleapis` بلا مهلة | لم يُقَس | مشتبَه S-2 |
| `/api/health` | 30 | قاعدة، ثمّ `/models` بمهلة 5s، ثمّ تجديد رمز الدرايف بلا مهلة | 30ms | OPS-105 |
| `counterparty` · `mark-paid` · `match-confirm-bulk` · `month-close` · `ai-findings` | 60 | قاعدة وحدها | — | سليم |
| `match-confirm` · `match-undo` · `bank-rule` · `payment-run` · `expense` · `expense-actual` · `product` · `supplier` · `search` · `document-status` · `ops/db-identity` | افتراضيّ الخطّة | قاعدة وحدها | — | مشتبَه S-3 |

**برهان OPS-101 خارج المسار** (`$SP/ops/proof-signal.ts`): خادم محلّيّ يتأخّر 5s، و`callDeepseek` بـ`timeoutMs: 2000` و`signal: AbortSignal.timeout(2000)`، وهما ما يمرّره `analyzeSupplier`. النتيجة:
`{"totalMs":8420,"hits":3,"ok":false,"attempts":3}`. أي ثلاث محاولات كاملة، لا ثانيتان.

---

## ٣. الاستعلامات لكلّ صفحة

**الطريقة:** جلسة المالك، وطلب إحماء قبل القياس، ثمّ عدّ أسطر `statement` و`execute` في سجلّ Postgres لكلّ طلب.

| الصفحة | HTTP | الزمن محلّياً | الاستعلامات | مجموع زمن القاعدة | أثقل استعلام |
|---|---|---|---|---|---|
| `/` | 200 | 40ms | **23** | 20ms | 2.9ms |
| `/bank` | 200 | 22ms | 9 | 9ms | 4.6ms |
| `/review` | 200 | 15ms | 5 | <1ms | 0.2ms |
| `/suppliers` | 200 | 21ms | 4 | 1ms | 1.1ms |
| `/suppliers/OliveLeaves` | 200 | 19ms | 11 | 1ms | 0.3ms |
| `/payments` | 200 | 14ms | 5 | 1ms | 0.9ms |
| `/purchases/invoices` | 200 | 27ms | 7 | 3ms | 1.9ms |
| `/documents` | 200 | 28ms | 10 | 1ms | 0.3ms |
| `/attention` | 200 | 25ms | 18 | 8ms | 1.6ms |
| `/close` | 200 | 13ms | 12 | 2ms | 1.2ms |
| `/analysis` | 200 | 24ms | 4 | 1ms | 0.3ms |
| `/money` | 200 | 12ms | 8 | 3ms | 1.2ms |

**الخلاصة:** لا N+1 في الصفحات. العدد ثابت ولا يكبر بعدد الصفوف. وأثقل استعلام 4.6ms على هذا الحجم.

**تفاصيل ذات صلة:**
- **`EXPLAIN`:** «ما يحتاج قرارك» يستعمل `Bitmap Index Scan on bank_tx_category_idx`. ونافذة التاريخ في الاستيراد `Seq Scan` على 2131 صفّاً بكلفة 327، وهو مقبول اليوم. و`payment_allocations` بـ`invoice_id` مسحٌ متسلسل لأنّ الجدول صغير (4.65)، والفهرس موجود من `029`.
- **مقاسٌ عند المنسّق:** زمن أوّل بايت 3–8ms على بناء الإنتاج. وأحجام HTML: `/documents` 432KB، و`/purchases/invoices` 354KB.
- **حزمة المتصفّح:**
  - ٣١ ملفّاً مجموعها 1008KB، أكبرها 234KB و174KB و113KB، وهي إطار العمل وReact.
  - لا `googleapis` ولا `pdfjs` ولا `drizzle` في العميل. ملفّ واحد (36KB) يحمل كلمة «xlsx» نصّاً فقط.
  - ٢٧ ملفّاً `"use client"`.
  - لا `use cache` ولا `cacheComponents`، فالصفحات ديناميكيّة. وهذا مقبول على هذا الحجم، وأبقى الحكم كما في OPS-017 القديمة.

---

## ٤. استيراد كشف الأهلي الحقيقيّ

**الملفّ:** `E-Statement_12600000942005_260505_260903_752471_1-1.xlsx` (1436 صفّاً).

| التجربة | HTTP | الزمن | الاستعلامات | نداءات الذكاء | النتيجة |
|---|---|---|---|---|---|
| الكشف الحقيقيّ ثانيةً (`apply=true`) | 200 | 0.22s | 20 | 0 | 0 إضافة، و«مقيَّد عندك من قبل» |
| الكشف نفسه بتواريخ مُزاحة إلى 2027 (معاينة) | 200 | 0.26s | 11 | 0 | 1436 جديدة |
| الكشف المُزاح (`apply=true`) | 200 | 0.59s | **2775** | 0 | **1318 أُدرجت**، و118 ردّها قيد مرجع العمليّة. والمتوقَّع أنّ المراجع بقيت كما هي فاصطدمت بالحركات الأصليّة |
| الكشف المُزاح ثانيةً | 200 | 0.28s | 139 | 0 | 0 مجموعات مكرَّرة (استعلام التحقّق: `dup_groups: 0`) |

**ملاحظتان:**
- `bank_imports.new_row_count` سُجّل **1436** والمُدرَج فعلاً 1318 (OPS-111).
- الملفّ الحقيقيّ خرج `bank: "بنك غير محدَّد"`، والاستيرادان المحفوظان في القاعدة يحملان «الأهلي (SNB)» (مشتبَه S-4).

---

## ٥. تجربة استعادة النسخة الاحتياطية

**المصدر:** `/Users/aj/TPH-backups/2026-09-14/full/`. **الهدف:** قاعدة جديدة `tph_restore`، والنصّ في `$SP/ops/restore.mjs`.

1. **المحاولة الأولى بالترتيب الموثَّق سقطت:** `schema-pre-data.sql` ردّ `relation "month_closes" does not exist`. السبب أنّ الدالّة `month_is_closed(m text)` مكتوبة `LANGUAGE sql`، فيتحقّق Postgres من جسمها عند إنشائها، وهي في الملفّ **قبل** الجداول.
2. **المحاولة الثانية** بإضافة `set check_function_bodies = off` قبل الملفّ نجحت كلّها: المخطّط، ثمّ البيانات بـ`json_populate_recordset`، ثمّ `schema-post-data.sql`.
3. **المقارنة ببصمات `manifest.json`** (md5 لكلّ صفّ): **51 من 51 جدولاً تطابق** في العدد والبصمة.
   - المؤثِّرات 7، والقيود 691، والفهارس 164، والدوالّ في `public` 44، و`schema_migrations` 31. كلّها تساوي `tph_ops`.
   - مؤثِّر سجلّ التدقيق يعمل بعد الاستعادة: `DELETE` ردّ «سجل التدقيق غير قابل للتعديل أو الحذف».

**الحكم:** النسخة قابلة للاستعادة. لكنّها نسخة يدويّة واحدة على جهاز أحمد، وأخذها المنسّق، ولا إجراء لها في المستودع، والترتيب الساذج يسقط (OPS-103).

---

## ٦. التكلفة: نداءات الذكاء لكلّ مستند

رفعتُ خمسة مستندات حقيقيّة من نسخة الأرشيف إلى `/api/analyze`، وألحقتُ بكلٍّ منها بايتاً كي لا يُردّ 409.

| المستند | كيف قُرئ | النداءات | حجم الطلب | النتيجة |
|---|---|---|---|---|
| فاتورة PDF نصّيّة (58KB) | نصّ | 3 تصنيف + 3 استخراج | الموجِّه 4.2K و7.7K حرف | 502 (المزيَّف لم يطابق النصّ فردّ 503) |
| فاتورة PDF بلا `raw_text` في القاعدة (366KB) | **نصّ** | 6 | 5.0K و8.6K | 502 |
| صورة JPEG لافا (119KB) | صورة | 6 | **164KB و169KB للطلب** | 502 |
| كشف AVAL ممسوح (403KB) | صورة | 6 | 164KB | 502 |
| إيصال PDF (41KB) | نصّ | 6 | 3.8K و7.4K | 502 |
| الملفّ الأصليّ نفسه (مرفوع سابقاً) | — | **0** | — | 409 في 5ms |
| نموذج مسحوب (رتّبتُ ٤٠٤) | — | **2**: تصنيف واستخراج بلا إعادة | — | 502، ورسالة «النموذج deepseek-flash غير متاح لهذا المفتاح» |

**الخلاصة:**
- **على النجاح:** نداءان للمستند، ويصلان إلى ٤ عند إعادة السؤال الموجَّهة أو الانقطاع (`provider-deepseek.ts:208-296`). والتقدير السابق للكلفة ما زال صحيحاً: ٠٫٠٦٤ دولار لـ١٢٦ فاتورة.
- **على عطبٍ عابر (503):** **٦ محاولات**، لأنّ فشل التصنيف لا يوقف المرحلة الثانية (`:169` يوقفها على `NOT_CONFIGURED` و`NO_BALANCE` وحدهما). هذا لا يُدفَع ثمنه لأنّ ردود ٥٠٣ مجّانيّة، لكنّه يضاعف زمن الانتظار.
- **على ٤٠٤:** نداءان، والرسالة واضحة.
- **الإعادة بلا حاجة:** ⟵ SCN-106.
- **`drive-sync`:** حتى ملفّين يُقرآن في كلّ طلب (`MAX_CONTENT_PER_CALL = 2`)، أي ٤ نداءات على الأقلّ. لم يُجرَّب حيّاً.

---

## ٧. الملاحظات الجديدة

### OPS-101 · تحليل حساب المورّد يعيش 144 ثانية والمسار يُقتَل عند 60

- **الدور:** مهندس التشغيل.
- **الدرجة:** P1، لأنّه يمنع عملاً ويضلّل.
- **الثقة:** مُثبَتة، على المسار وخارجه.
- **الموضع:**
  - `src/app/api/ai-analysis/route.ts:36` يمرّر `deadlineMs: 48_000` ولا يلفّ الطلب بـ`withDeadline`.
  - `src/services/supplier-analysis.service.ts:219-227` يمرّر `timeoutMs: deadline` و`signal: AbortSignal.timeout(deadline)`.
  - `src/lib/ai/deepseek.ts:211-225`: عند كلّ محاولة يُنشأ `AbortController` جديد، ثمّ `call.signal?.addEventListener("abort", onAbort)`. **والإشارة التي انقضت قبلُ لا تُطلق الحدث ثانيةً**، فتعيش المحاولة التالية مهلتها كاملة.
  - الشاشة: زرّا «حلّل» و«حلّل الكلّ» في `/suppliers/[slug]` و`/purchases/insights`.
- **إعادة الإنتاج:**
  - **المدخلات:** طابور ثلاثة ردود متأخّرة ٧٠ ثانية، ثمّ `POST /api/ai-analysis {"supplierId":"ebb5403b-…"}` (AVAL).
  - **المتوقَّع:** وقوف بمهلة معلَنة دون ٦٠ ثانية.
  - **الواقع:** `HTTP=502 TIME=144.20` وجسمه «تعذّر التحليل: تجاوز النداء المهلة (48 ثانية)». وسجلّ المزيَّف يُظهر المحاولة الثانية بعد الأولى بـ٤٨ ثانية بالضبط.
  - **وخارج المسار:** `proof-signal.ts` أعطى `totalMs: 8420, hits: 3` لمهلة ثانيتين.
- **الأثر:**
  - على Vercel تُقتَل الدالّة عند ٦٠، فيردّ المزوّد صفحة نصّيّة. تقرؤها `http-client` «عطب في الخادم» بلا سبب.
  - «حلّل الكلّ» يقف عند أوّل مورّد بطيء، ويبقى أحمد ينتظر دقيقة بلا تفسير.
  - تُدفَع كلفة المحاولة الأولى، لأنّ المزوّد أكمل التفكير، ثمّ تُعاد المحاولة.
  - لا مال يُكتَب خطأً، فالحفظ بعد النجاح وحده.
- **الإصلاح المقترح (ساعتان):**
  1. `src/lib/ai/deepseek.ts:203`، أوّل الحلقة: `if (call.signal?.aborted) return { ok:false, kind:"AI_UNAVAILABLE", reason: lastReason || "أُلغي النداء", … }`. وبعد `:224`: `if (call.signal?.aborted) controller.abort();`. ولا تُعاد المحاولة إن كان سبب الإلغاء إشارة المستدعي.
  2. `src/app/api/ai-analysis/route.ts:36` يلفّ العمل بـ`withDeadline(55_000, …)`، و`supplier-analysis.service.ts:224-226` يُحذف منه `timeoutMs` و`signal` ليقرأ `callDeepseek` الموعد من السياق كما تفعل المسارات الأخرى.
  3. **اختبار** في `src/lib/ai/deepseek.test.ts`: خادم `http` محلّيّ يتأخّر 500ms، و`timeoutMs: 200`، و`signal: AbortSignal.timeout(200)`، ثمّ `expect(totalMs).toBeLessThan(400)` و`expect(hits).toBe(1)`.
  - **مخاطرة الإصلاح:** منخفضة. الإلغاء المبكر قد يُسقط تحليلاً كان سينجح في المحاولة الثانية، لكنّه كان سيُقتَل على المنصّة أصلاً.
- **يخالف قراراً في CLAUDE.md؟** نعم، ثلاثة:
  - «**النموذج يُطلب بمهلةٍ تنقضي قبل عمر المسار** (`AbortSignal.timeout` تحت الستّين)… فلا يُقتل طلبٌ طويل صامتاً».
  - «`withDeadline`… **مسارٌ جديد يستدعي الذكاء يُلفّ بها**».
  - «الوقوف بمهلةٍ معلَنة خيرٌ من قتلٍ صامت».
- **حال المراجعة السابقة:** امتداد لـOPS-002، أُصلحت جزئيّاً.
- **المحامي المضادّ:**
  - هل يحمي Vercel بإلغاء الطلب؟ المنصّة تقتل الدالّة ولا تُرجع JSON، فهو القتل الصامت نفسه.
  - هل `withDeadline` مطبَّق فوقه في طبقة أخرى؟ لا: `grep withDeadline` لا يجده إلّا في المسارات الأربعة.
  - هل الإعادة على المهلة مقصودة؟ التعليق في `deepseek.ts:207-209` يقول «إن ألغى الطلبَ من فوقنا وجب أن ينقطع النداء».
  - **لم تنتقض.**

### OPS-102 · ١٤ نصّاً يكتب أو يحذف في «قاعدة الإنتاج» بلا `guard-write`

- **الدور:** مهندس التشغيل ومهندس الأمن.
- **الدرجة:** P2.
- **الثقة:** مُتتبَّعة. قرأتُ علم كلّ نصّ بـ`grep process.argv`، ولم أشغّل النصوص الكاتبة.
- **الموضع:** `scripts/lib/guard-write.ts:1-9` يقول «القاعدة في `.env` هي الإنتاج… فالكتابة تحتاج إقراراً مكتوباً في الأمر نفسه». لكنّه مستعمَل في **٥ نصوص وحدها**: `seed-demo`، و`seed-suppliers`، و`derive-expenses`، و`build-products`، و`try-archive`. أمّا هذه فتكتب أو تحذف بعلمٍ أقصر:

  | العلم | النصوص |
  |---|---|
  | `apply` بلا شرطتين | `dedupe-bank`، `reclassify-bank`، `rematch-bank`، `learn-counterparties`، `repair-import-scope`، `identity-report` |
  | `--commit` | `backfill-content`، `merge-suppliers`، `migrate-archive`، `recompute-line-pricing`، `repair-bank-rules`، `repair-integrity` |
  | `--apply` | `backfill-missing-invoices`، `remove-duplicate-transaction`، `repair-period-month` |
- **إعادة الإنتاج:**
  - **المدخلات:** `npm run db:dedupe -- apply` من طرفيّة المستودع.
  - **المتوقَّع:** رفض بلا `--i-know-this-is-production`، كما يرفض `db:demo`.
  - **الواقع:** يحذف في القاعدة التي يشير إليها `.env` مباشرةً (`dedupe-bank.ts`: حذفان وتحديثان).
- **الأثر:**
  - كلمة `apply` تُكتب في تجربة عابرة. وهذه النصوص بالذات هي التي حذفت صفوفاً حقيقيّة من قبل: «كان `db:dedupe` يأكل دليله… أربعةَ عشر صفّاً حقيقيّاً».
  - الحذف المتكرّر في `repair-*` و`remove-duplicate-transaction` قرار ماليّ بلا إقرار مكتوب.
- **الإصلاح المقترح (ساعتان):**
  1. في كلّ نصّ من الأربعة عشر، في موضع قراءة العلم: `if (apply) assertWriteAllowed("db:dedupe")`. ويُذكر اسم الأمر في كلّ نصّ.
  2. **اختبار** `src/lib/code-guards.test.ts`: يمرّ على `scripts/*.ts`، وكلّ ملفّ يطابق `/\.(insert|update|delete)\(|delete from|update \w+ set/i` خارج `rollback` يجب أن يحوي `assertWriteAllowed`. والاستثناء الصريح في قائمة: `verify-invariants` (معاملة تُلغى) و`migrate`.
  3. تحديث CLAUDE.md، سطر «النصوص الكاتبة ترفض…»، بالقائمة الكاملة.
  - **مخاطرة الإصلاح:** نصوص CI أو التوثيق التي تمرّر `apply` وحده ستفشل حتى تُحدَّث.
- **يخالف قراراً في CLAUDE.md؟** جزئيّاً. القرار يسمّي خمسة نصوص فقط، لكنّ علّته المكتوبة في `guard-write.ts` («كلّ نصٍّ يكتب فيها… يكتب في بيانات المقهى الحقيقيّة») تشمل الكلّ.

### OPS-103 · لا نسخ احتياطيّ دوريّ ولا إجراء استعادة، والاستعادة بالترتيب الساذج تسقط

- **الدور:** مهندس التشغيل.
- **الدرجة:** P1، لأنّ البيانات الماليّة كلّها معرّضة للفقد.
- **الثقة:** مُثبَتة للجزء الذي يقول إنّ الاستعادة تسقط بلا العلم. ومُتتبَّعة لغياب الإجراء.
- **الموضع:**
  - لا نصّ `pg_dump` أو نسخ في `scripts/`، ولا خطوة في `docs/SETUP.md`. `grep backup` لا يجد إلّا البوّابة و`HANDOVER.md` المؤرّخ.
  - `scripts/production-gate.ts:404-414`: البند `?` لأنّ `ops-attestation.json` غير موجود.
  - `schema-pre-data.sql` في النسخة: `month_is_closed` (`LANGUAGE sql`) مكتوبة قبل `month_closes`.
- **إعادة الإنتاج:**
  - **المدخلات:** قاعدة فارغة ثمّ `schema-pre-data.sql`.
  - **الواقع:** `relation "month_closes" does not exist`، وتتوقّف الاستعادة.
  - **ومع `set check_function_bodies = off`:** نجحت، و51 من 51 جدولاً طابقت البصمة.
  - **والنسخة الوحيدة** هي ما أخذه المنسّق اليوم على الجهاز المحلّيّ، خارج أيّ جدولة.
- **الأثر:**
  - ١٤١ فاتورة (138,803.65 ريال)، و١٤٤٠ حركة، و٢٩٥ سجلّ تدقيق.
  - نصّ إصلاح بعلمٍ خاطئ (OPS-102)، أو قاعدة تُحذف من لوحة Neon، لا عودة منه إلّا بنافذة PITR لم يتحقّق أحد من مدّتها.
  - ومن يحاول الاستعادة وقت الحادثة يسقط عند أوّل ملفّ.
- **الإصلاح المقترح (نصف يوم):**
  1. `scripts/backup.ts` جديد: قراءة داخل معاملة `READ ONLY`، بمنطق `$SP/clone-prod.mjs` نفسه (schema-pre، ثمّ data JSON مضغوط، ثمّ schema-post، ثمّ manifest بالبصمات). ويكتب في أوّل `schema-pre-data.sql`: `set check_function_bodies = off;`. ومرجعه `db:backup` في `package.json`.
  2. `scripts/restore-verify.ts`: يستعيد في قاعدة يسمّيها المستخدم، ويقارن بالـmanifest، ويكتب `backupRestored` في `ops-attestation.json` فقط إن طابق كلّ جدول.
  3. قسم «النسخ والاستعادة» في `docs/SETUP.md`: الأمران، وأين تُحفظ النسخة (خارج Neon وخارج الجهاز)، ونافذة PITR في خطّة Neon الحاليّة.
  4. **اختبار** `src/lib/ops/backup.test.ts` (نقيّ): المولِّد يضع `check_function_bodies = off` في السطر الأوّل، وترتيب الأقسام pre ثمّ data ثمّ post.
  - **مخاطرة الإصلاح:** النسخة تحمل بيانات ماليّة وقد تحمل رموزاً، فيجب أن تُستثنى `accounts.refresh_token` و`sessions` أو تُشفَّر.
- **يخالف قراراً في CLAUDE.md؟** نعم:
  - «**«لم يُفحَص» يمنع الجاهزية**»: البند `?` منذ أُضيف ولا طريق في المستودع لجعله PASS.
  - وفي البوّابة نفسها: «نسخةٌ لم تُجرَّب استعادتُها أملٌ لا نسخة».
- **حال المراجعة السابقة:** OPS-005، جزئيّة.
- **المحامي المضادّ:**
  - هل يكفي PITR الافتراضيّ في Neon؟ مدّته تتبع الخطّة، ولم تُكتب في أيّ وثيقة ولم يُجرَّب منه فرع.
  - هل نسخة المنسّق تكفي؟ هي لقطة اليوم، وستقدم غداً.
  - هل يغني `clone-prod.mjs`؟ هو في مجلّد مؤقّت خارج المستودع.
  - **لم تنتقض.**

### OPS-104 · الاستيراد: ٢٧٧٥ استعلاماً في معاملة واحدة بعد تحكيمٍ يأكل الموعد

- **الدور:** مهندس التشغيل.
- **الدرجة:** P2.
- **الثقة:** العدد مُثبَت، والزمن على Vercel تقدير.
- **الموضع:**
  - `src/app/api/bank-import/route.ts:331`: `adjudicate` حتى 25 نداءً متسلسلاً تحت `withDeadline(55s)`.
  - `:563-570`: `db.update` لكلّ `sync.enrich`.
  - `:620-830`: لكلّ صفّ جديد `insert bankTransactions` ثمّ `insert decisionHistory`، وربّما `adjudications` و`recordBankPayment` و`allocate`.
  - ولا فحص للموعد قبل المعاملة.
- **إعادة الإنتاج:**
  - **المدخلات:** الكشف الحقيقيّ بتواريخ مُزاحة سنة، و`apply=true`.
  - **الواقع:** 2775 استعلاماً (نحو 1.93 للصفّ)، و0.59s محلّياً.
  - **على Vercel:** كلّ رحلة بضعة مللي ثوانٍ إلى Neon، فالمعاملة 14 إلى 28 ثانية (تقدير). وإن استهلك التحكيم 52s وصلت المعاملة بعده إلى ما بعد 60، فتُقتَل الدالّة. تُلغى المعاملة فلا نصف قيد، لكنّ نداءات التحكيم تُدفَع ويُعاد الاستيراد.
  - **وملاحظة:** الاستيراد الشهريّ المعتاد (معروف في أغلبه) 20 استعلاماً في 0.22s، فالخطر في الكشف الكامل أو الربعيّ.
- **الأثر:** استيراد كشف ربعيّ مع التباسات كثيرة يفشل على المنصّة بلا رسالة مفهومة، ويحجز الاتّصال الوحيد (`max:1`) طوال المعاملة.
- **الإصلاح المقترح (يوم):**
  1. `route.ts:620`: قسم الصفوف إلى صفوف لها `plan` وصفوف بلا `plan`. الثانية تُدرج دفعات بـ`tx.insert(bankTransactions).values(batch).onConflictDoNothing().returning({id, identityKey})` بحجم 500، ثمّ `decisionHistory` دفعةً واحدة بمطابقة `identityKey`. والأولى تبقى صفّاً صفّاً لأنّها تنشئ دفعات.
  2. `:563`: تحديث `enrich` بـ`update … from (values …)` في استعلام واحد.
  3. قبل `:618`: `if (currentDeadline() && Date.now() > currentDeadline()! - 15_000) return 503` برسالة «ضاق الوقت بعد التحكيم — أعد الاستيراد، ولن يُعاد تحكيم ما حُكِّم»، وتُحفظ نتائج التحكيم أوّلاً (يلتقي بـSCN-013 القديمة).
  4. **اختبار تكامل** على قاعدة حقيقيّة (يلتقي بـCODE-013): استيراد 1436 صفّاً، والتحقّق أنّ عدد الاستعلامات أقلّ من 200 بعدّاد يُلفّ حول `pool.query`، وأنّ النتيجة مطابقة لما قبل الإصلاح.
  - **مخاطرة الإصلاح:** `onConflictDoNothing` الجماعيّ يُعيد الصفوف المُدرجة وحدها، فيجب أن يُعدّ `rejectedByConstraint` من الفرق لا يُفترَض.
- **يخالف قراراً في CLAUDE.md؟** نعم: «**الكتابة على مئات الصفوف تُجمَع دفعاتٍ**». وARCHITECTURE.md:418 يقرّ بأنّ OPS-009 بقي «لجولةٍ أكبر».
- **حال المراجعة السابقة:** OPS-009، قائمة.

### OPS-105 · `/api/health` يردّ 200 والقارئ معطَّل، ولا أحد يُنبَّه

- **الدور:** مهندس التشغيل.
- **الدرجة:** P2.
- **الثقة:** مُثبَتة.
- **الموضع:**
  - `src/app/api/health/route.ts:141-148`: الجسم `healthy: healthy && providerReachable.ok && driveToken.ok`، لكنّ `status: healthy ? 200 : 503` يستعمل `healthy` الأساسيّ وحده.
  - `:99` يطلب `/models` ولا يتحقّق أنّ `modelFor(...)` الثلاثة في القائمة.
  - `src/instrumentation.ts` لا يكتب إلّا إلى `console.error`، وسجلّ Hobby يُحفظ ساعة.
  - لا `vercel.json` cron، ولا مستطلِع.
- **إعادة الإنتاج:**
  - **المدخلات:** جلسة المالك، و`GET /api/health`، والمزوّد يردّ 503.
  - **المتوقَّع:** HTTP 503.
  - **الواقع:** `HTTP=200` وجسمه `{"healthy":false,…,"providerReachable":{"ok":false,"status":503}}`.
  - **وغير الداخل** يرى `{"healthy":true}`.
  - **وإن سُحب اسم نموذج:** `/models` يردّ 200، فيبقى الفحص أخضر، ويفشل كلّ مستند برسالة «غير متاح لهذا المفتاح» (مُثبَتة بـ٤٠٤).
- **الأثر:** العطب في الثالثة فجراً يُكتشف عند الكاشير. ومستطلِع خارجيّ يُضاف غداً سيقرأ الرمز 200 «سليم».
- **الإصلاح المقترح (نصف يوم):**
  1. `health/route.ts:147`: `status: (healthy && (providerReachable?.ok ?? true) && (driveToken?.ok ?? true)) ? 200 : 503`.
  2. بعد `:103`: قراءة `data[].id` من ردّ `/models` والتحقّق أنّ `modelFor("VISION"|"TEXT"|"REASONING")` كلّها فيه، وإلّا `providerReachable = { ok:false, error:"MODEL_MISSING:"+name }`.
  3. مسار `/api/health/probe` محروس بمفتاح في الترويسة (`HEALTH_PROBE_TOKEN`) يُرجع الفحص العميق بلا جلسة. ويُستطلَع من مستطلِع مجّانيّ كلّ ١٠ دقائق، وينبّه أحمد بفشلين متتاليين.
  4. **اختبار** `src/app/api/health/route.test.ts`: `fetch` مزيَّف يردّ 503 ثمّ قائمة بلا `deepseek-flash`، والتحقّق من `status === 503` في الحالتين.
  - **مخاطرة الإصلاح:** تنبيه كاذب من مزوّد متقطّع، ولذلك شرط الفشلين.
- **يخالف قراراً في CLAUDE.md؟** «الفشل يُسمَع»، وهو يُسمَع في الشاشة لا في التشغيل. وفي التعليق `health/route.ts:92-95`: «كان الفحص يقول «سليم» والرصيدُ صفر».
- **حال المراجعة السابقة:** OPS-004 وOPS-012، جزئيّة.

### OPS-106 · إعداد `tph-invoicing` يشغّل `next dev` بتخطّي الدخول على قاعدة الإنتاج

- **الدور:** مهندس التشغيل ومهندس الأمن.
- **الدرجة:** P1، لأنّ تجربة محلّيّة تكتب في مال المقهى باسم المالك.
- **الثقة:** مُتتبَّعة.
- **الموضع:**
  - `.claude/launch.json`: الإعداد الأوّل `"runtimeArgs": ["-c", "AUTH_BYPASS=true npx next dev -H 127.0.0.1"]`.
  - `next dev` يحمّل `.env`، وفي `.env` `DATABASE_URL` ومفتاح `AUTH_BYPASS` ورمز الدرايف. ومخرَج البناء يقول «Environments: .env.local, .env».
  - `scripts/lib/guard-write.ts:4`: «القاعدة في `.env` هي الإنتاج — لا قاعدةَ تطويرٍ منفصلة».
  - `docs/SETUP.md` §٦: «والتطوير يكتب في قاعدة الإنتاج ما لم يُضبط `DATABASE_URL` في `.env.local`».
  - `src/lib/preview-mode.ts` يمنع التخطّي في `NODE_ENV=production` وحده.
- **إعادة الإنتاج:**
  - **المدخلات:** `preview_start tph-invoicing` (أداة Claude نفسها)، ثمّ الضغط على «أكّد» في `/review`.
  - **المتوقَّع:** قاعدة معزولة.
  - **الواقع:** كتابة في Neon الإنتاجيّ باسم أوّل مستخدم (المالك)، لأنّ `currentUser()` يستعير معرّفه في وضع المعاينة.
  - لم أشغّله، فهو يلمس الإنتاج.
- **الأثر:**
  - كلّ وكيل أو جلسة تطوير تفتح المعاينة تكتب في المال الحقيقيّ.
  - `ops:certify` كتب فيها من قبل، وهو مُثبَت عند غيري.
  - النشر يخرج من الشجرة المحلّيّة (`vercel deploy --prod`) ولا ينتظر CI.
- **الإصلاح المقترح (ساعتان، ثمّ قرار أحمد):**
  1. `.claude/launch.json`: يُحذف إعداد `tph-invoicing`، أو يُستبدل بـ`"DATABASE_URL=$DEV_DATABASE_URL AUTH_BYPASS=true npx next dev"`، مع رفض التشغيل إن كان `DEV_DATABASE_URL` فارغاً.
  2. `src/db/index.ts` قبل `:21`: إن كان `NODE_ENV !== "production"` و`process.env.DATABASE_URL` يحوي معرّف مضيف الإنتاج المكتوب في `PRODUCTION_DB_HOST`، ولم يُضبط `ALLOW_DEV_ON_PRODUCTION=true`، يُرمى خطأ عند الإقلاع.
  3. فرع Neon للتطوير في `.env.local`.
  4. **اختبار** `src/db/index.test.ts`: `NODE_ENV=development` مع مضيف مطابق يرمي، ومع `ALLOW_DEV_ON_PRODUCTION` لا يرمي.
  - **مخاطرة الإصلاح:** يعطّل سير عمل أحمد المحلّيّ حتى يُنشأ الفرع.
- **يخالف قراراً في CLAUDE.md؟** نعم:
  - «**`AUTH_BYPASS`** يفتح كل الأبواب في التطوير»، والتطوير هنا متّصل بالإنتاج.
  - «العزل يُثبَت بمعرّف العنقود»: البند `?`.
- **حال المراجعة السابقة:** OPS-006، قائمة.
- **المحامي المضادّ:**
  - هل `.env.local` يغلب؟ لا يوجد فيه `DATABASE_URL` بحسب `SETUP.md` («ما لم يُضبط»)، ولم أقرأ قيمه.
  - هل `preview-mode.ts` يمنع؟ يمنع في الإنتاج وحده بنصّ CLAUDE.md.
  - هل الإعداد مستعمَل؟ هو الأوّل في الملفّ، واسمه اسم المشروع.
  - **لم تنتقض.**

### OPS-107 · البوّابة PASS على شهادة لم تُربط بكوميت، ولا إقرار واحد

- **الدور:** مهندس التشغيل.
- **الدرجة:** P2.
- **الثقة:** مُثبَتة.
- **الموضع:** `scripts/production-gate.ts:420-431` يقرأ `certify-result.json` بحقول `at` و`total` و`passed` وحدها، والحدّ `STALE_DAYS = 7` (`:57`). ولا `ops-attestation.json`.
- **إعادة الإنتاج:** `ops:gate` قال «PASS · 11 من 11 سيناريو · قبل 6 أيّام». وبين `certify` وHEAD كوميتات «مراجعة سبتمبر» الثمانية وما بعدها، ومنها `match-confirm` والهجرات 027–031.
- **الأثر:** «١١ من ١١» تُقرأ شهادةً على شيفرة لم تُختبَر. ويُضاف إليها أنّ `ops:certify` نفسه يكتب في القاعدة ثمّ يحذف (مُثبَت عند غيري).
- **الإصلاح المقترح (ساعة):**
  1. `scripts/certify-flow.ts` يكتب في `certify-result.json` حقلين: `commit: execSync("git rev-parse HEAD")` و`migrations: (عدد schema_migrations)`.
  2. `production-gate.ts:423`: `: certify.commit !== currentCommit() || certify.migrations !== applied.length ? "UNKNOWN"`، مع تفصيل «على كوميت آخر». والشيء نفسه لـ`truth-audit.json` في `:437`.
  3. **اختبار** في `src/lib/ops/production-gate.test.ts`: `certifyStatus({passed:11,total:11,at:now,commit:"a"}, "b")` يعيد `"UNKNOWN"`. ويُستخرج الحكم إلى دالّة نقيّة في `production-gate.ts` كي يُختبَر.
  - **مخاطرة الإصلاح:** كلّ كوميت يُسقط البند إلى `?`، وهو المقصود.
- **يخالف قراراً في CLAUDE.md؟** نعم: «بوّابةٌ تعدّ غير المفحوص ناجحاً تُنتج ثقةً بلا سند».
- **حال المراجعة السابقة:** OPS-008، جزئيّة.

### OPS-108 · «مجمَّعة إلزاماً» في الوثيقة لا في الشيفرة

- **الدور:** مهندس التشغيل.
- **الدرجة:** P3.
- **الثقة:** مُثبَتة محلّياً.
- **الموضع:**
  - `src/db/index.ts:22-31` يقبل أيّ `DATABASE_URL`.
  - `health/route.ts:85-89` لا يُدخل `pooled` في `healthy`.
  - `scripts/migrate.ts:39` و`bootstrap-schema.ts:12` و`verify-invariants.ts:18` يفتحون اتّصالاتهم بالسلسلة نفسها. وهذا سليم لأنّ الأقفال الاستشاريّة تحتاج جلسة، ويعمل مع pooler Neon في وضع transaction فقط داخل المعاملة.
- **إعادة الإنتاج:** سلسلة غير مجمَّعة ثمّ `/api/health` = `"dbEndpoint":{"pooled":false}` مع HTTP 200.
- **الأثر:** من يلصق سلسلة Neon المباشرة في Vercel يعود إلى «الوقوف الصامت» الموثَّق.
- **الإصلاح المقترح (ساعة):**
  1. `src/db/index.ts` قبل `:21`: `if (isServerless && !new URL(process.env.DATABASE_URL!).hostname.includes("-pooler")) console.error(JSON.stringify({kind:"db-not-pooled"}))`.
  2. `health/route.ts:85`: `&& (info?.pooled || !isServerlessEnv)`.
  3. **اختبار** في `src/lib/ops/db-identity.test.ts` لدالّة `requiresPooler(env, url)`.
- **يخالف قراراً في CLAUDE.md؟** «يجب استعمال نقطة Neon **المجمَّعة**».

### OPS-109 · رسالة الفشل تُسقط السبب، وستّ محاولات على عطبٍ عابر

- **الدور:** مهندس التشغيل ومهندس الجودة.
- **الدرجة:** P3.
- **الثقة:** مُثبَتة.
- **الموضع:**
  - `src/lib/extraction/provider-deepseek.ts:169-175` لا يوقف المرحلة الثانية إلّا على `NOT_CONFIGURED` أو `NO_BALANCE`.
  - `:205` يبدأ `lastReason = ""`، فسبب فشل التصنيف لا يُحمَل.
  - `deepseek.ts:215-217` يقول «لم يبقَ من عمر الطلب…» حين `lastReason` فارغ.
- **إعادة الإنتاج:**
  - مزوّد متأخّر 70s ثمّ `/api/analyze` = 502 برسالة «لم يبقَ من عمر الطلب ما يكفي لنداء القارئ»، والسبب الحقيقيّ «تجاوز النداء المهلة (52 ثانية)» ضاع.
  - مزوّد يردّ 503 = **6 نداءات** للمستند الواحد (مُثبَت على خمسة مستندات).
- **الأثر:** أحمد يُقال له «أعد المحاولة» بلا أن يعرف أنّ المزوّد متعطّل، فيعيد ويضاعف الانتظار.
- **الإصلاح المقترح (ساعة):**
  1. `provider-deepseek.ts:169`: `} else if (classified.kind !== "INVALID_RESPONSE") { return { ok:false, provider:"deepseek", reason: classified.reason }; }`. أي إن كان العطب `AI_UNAVAILABLE` فلا مرحلة ثانية.
  2. `:205`: `let lastReason = classified.ok ? "" : classified.reason;`.
  3. **اختبار** في `src/lib/extraction/provider.test.ts`: `callDeepseek` مزيَّف يعيد `AI_UNAVAILABLE` في التصنيف، والتحقّق من نداء واحد، وأنّ `reason` يحوي نصّ التصنيف.
- **يخالف قراراً في CLAUDE.md؟** «عطبٌ مصنَّف — الرصيد الناضب غير انقطاع الشبكة».

### OPS-110 · CLAUDE.md لا يذكر ٩ أوامر، ولا مخطّط `neon_auth`

- **الدور:** المهندس الرئيسي.
- **الدرجة:** P3.
- **الثقة:** مُثبَتة.
- **الموضع:**
  - الأوامر التسعة المسمّاة في §١ موجودة في `package.json` وغائبة عن CLAUDE.md §الأوامر.
  - مخطّط `neon_auth` بتسعة جداول في القاعدة والنسخة، ولا يرد في الشيفرة ولا في الوثائق.
- **الأثر:** نصوص كاتبة بلا حارس (OPS-102) لا يعرف قارئ CLAUDE.md أنّها موجودة. ومن يعدّ الجداول يجد ٥١ لا ٤١.
- **الإصلاح المقترح (ساعة):**
  1. إضافة الأوامر إلى CLAUDE.md §الأوامر مع «يكتب» أو «يقرأ».
  2. سطر تحت «القاعدة — ٤١ جدولاً»: «ومخطّط `neon_auth` (٩ جداول) من ميزة Neon Auth، لا تستعملها الشيفرة».
  3. **اختبار** `src/lib/code-guards.test.ts`: كلّ مفتاح في `package.json.scripts` يبدأ بـ`db:` أو `ops:` أو `drive:` يرد في CLAUDE.md.

### OPS-111 · `bank_imports.new_row_count` يعدّ ما ردّه القيد جديداً

- **الدور:** المدقّق الماليّ ومهندس التشغيل.
- **الدرجة:** P3.
- **الثقة:** مُثبَتة.
- **الموضع:** `src/app/api/bank-import/route.ts` عند إدراج `bankImports` (`newRowCount: newRows`، بعد `:590`)، وهو يُكتَب قبل المعاملة. أمّا `rejectedByConstraint` فيُعدّ داخلها ولا يُحدَّث به الصفّ.
- **إعادة الإنتاج:** الكشف المُزاح = `new_row_count: 1436` بينما `select count(*) … where bank_import_id='db80c15a…'` = **1318**.
- **الأثر:** «أُضيفت ١٤٣٦» في الأثر المحفوظ عن ملفّ أضاف ١٣١٨. وهذا ما قال القرار إنّه أُصلح في رسالة الشاشة.
- **الإصلاح المقترح (ساعة):**
  1. بعد المعاملة (`:830`): `await db.update(bankImports).set({ newRowCount: newRows - rejectedByConstraint }).where(eq(bankImports.id, importId))`. أو نقل إدراج `bankImports` داخل المعاملة وتحديثه في آخرها.
  2. **اختبار تكامل:** استيراد ملفّ فيه مرجع مكرَّر، والتحقّق أنّ `new_row_count` يساوي عدد الحركات المرتبطة.
- **يخالف قراراً في CLAUDE.md؟** «وكان يُقال «أُضيفت ٣٢٧ حركة» عن ملفٍّ لم يُضِف واحدة» (تعليق `route.ts`)، والمنطق نفسه في الصفّ المحفوظ.

### OPS-112 · `statement-reconcile` ما زال يقبل ٢٥ ميجابايت

- **الدور:** مهندس التشغيل.
- **الدرجة:** P2.
- **الثقة:** مُتتبَّعة.
- **الموضع:** `src/app/api/statement-reconcile/route.ts:35` فيه `MAX_BYTES = 25 * 1024 * 1024`، والمسارات الأخرى ٣ أو ٤ ميجابايت بتعليق «حدُّ المنصّة ٤٫٥».
- **إعادة الإنتاج:**
  - كشف مورّد ممسوح بحجم ٦ ميجابايت يُرفع من `/statements` بمسار `file`.
  - **المتوقَّع:** «الملفّ أكبر من ٤ ميجابايت».
  - **الواقع على Vercel:** ٤١٣ نصّيّ من المنصّة قبل الشيفرة. تقرؤه `http-client` خطأ خادم بلا سبب.
- **الأثر:** الكشوف الممسوحة الطويلة أكبر المستندات حجماً (كشف AVAL وحده 403KB لصفحات قليلة).
- **الإصلاح المقترح (ساعة):**
  1. `:35`: `const MAX_BYTES = 4 * 1024 * 1024`، والفحص الموجود يستعمله بالرسالة نفسها التي في `analyze/route.ts:88`.
  2. **اختبار نصّيّ** في `code-guards.test.ts`: كلّ `MAX_BYTES = N` في `src/app/api/**/route.ts` يكون ٤ ميجابايت أو أقلّ.
- **يخالف قراراً في CLAUDE.md؟** «الفشل يُسمَع».
- **حال المراجعة السابقة:** OPS-010، جزئيّة.

---

### OPS-113 · إعادةُ التسمية كتبت في الأرشيف أسماءً بلا امتداد — والاسمُ المُفسَد يُقرأ صحيحاً فلا يُقترَح إصلاحُه

- **الدور:** مهندس التشغيل، ومدقّق الأرشيف.
- **الدرجة:** P1. الكتابة وقعت في **أرشيف أحمد نفسه**، وهو القيد الأوّل في `CLAUDE.md`؛ ولا خسارة مالٍ فيها فلم تُرفَع إلى P0.
- **الثقة:** مُثبَتة. الدليل من `audit_logs` في نسخة الإنتاج (١٩ سبتمبر)، ومن قياس الدالّة على الأسماء نفسها، ومن تشغيل مسار التسمية على نسخة البيانات الحقيقية.
- **الموضع:**
  - `src/lib/canonical-name.ts:40-44` — `extensionOf` كانت: `fileName.match(/\.([A-Za-z0-9]{1,5})$/)` ثمّ `m ? m[1] : "pdf"`. فوقها تعليقٌ يقول **«الامتداد يبقى كما هو — تغييرُه يكسر فتح الملفّ»**.
  - `src/lib/naming.ts:84-88` — `splitExtension` تعدّ كلّ ما بعد آخر نقطة امتداداً.
  - أسماءُ الأرشيف تنتهي بالمبلغ: `…_SAR996.19`. وكثيرٌ ممّا يصل من المورّدين بلا امتدادٍ أصلاً.
- **إعادة الإنتاج:**
  - **المدخلات:** ملفٌّ اسمه `2026-09-08_CoffeeLabs_Invoice_V405669_SAR638.71` (بلا امتداد)، وقيدُه في القاعدة إجماليّه ٦٣٨٫٠٠.
  - **المتوقَّع:** `2026-09-08_CoffeeLabs_Invoice_V405669_SAR638.00.pdf`.
  - **الواقع:** `extensionOf` تقرأ «71» امتداداً، فيُبنى الأساس المنتهي بالمبلغ ثمّ يُلحَق به: `…_SAR638.00.71`.
  - **والأخطر:** `parseFileName("…_SAR638.00.71")` تُرجع `ok: true` — «71» امتدادٌ صالح عندها، والأساس مستقيم. فتقول `canonicalName` **«على الصيغة»**، ولا يظهر الملفّ في قائمة إعادة التسمية أبداً. عطبٌ يُخفي نفسه.
- **الدليل من سجلّ التدقيق (نسخة الإنتاج، `DRIVE_FILE_RENAMED`، ١٤ سبتمبر ٢٠٢٦ ‏10:32 UTC):** خمسُ تسميات، أربعٌ منها أفسدت الاسم (الصيغة «القديم ← الجديد»):

  | القديم | الذي كُتب في الدرايف |
  |---|---|
  | `…_AVAL_Invoice_INV/2026/00130_SAR996.19` | `…_SAR996.19.19` |
  | `…_CoffeeLabs_Invoice_V405669_SAR638.71` | `…_SAR638.00.71` |
  | `…_Loreva_Invoice_SI-0057_SAR632.5` | `…_SAR632.50.5` |
  | `…_FGanache_Invoice_CIV-008504960_SAR1,030.40F` | `…_SAR1030.40.40F` |

  والخامسة سليمة (`TAX_Invoice-00420.pdf` ← `2026-09-06_BeCof_Invoice_00420_SAR240.00.pdf`) لأنّ أصلها يحمل `.pdf`. ودفعةُ ١٨ سبتمبر (٧ ملفّات) سليمةٌ كلّها للسبب نفسه.
- **الأثر:**
  - أربعةُ ملفّاتٍ حقيقيّة في الأرشيف بأسماءٍ **لا امتداد لها**: نظامُ التشغيل والمتصفّح لا يعرفان بما يُفتَح الملفّ، وهو بعينه ما يمنعه التعليق فوق الدالّة.
  - وهي لا تُقترَح للإصلاح، فالعطب دائمٌ ما لم يُكشَف من خارج المسار — ولم يظهر إلّا بمقابلة الأرشيف بالقيد (`ops:truth`).
  - والتكرار: كلّ ملفٍّ يصل بلا امتداد وينتهي اسمُه بالمبلغ يُفسَد عند أوّل تسمية.
- **الإصلاح (تمّ — `fca7a6f`):**
  1. `naming.ts`: `looksLikeExtension` — حروفٌ من حرفين إلى أربعة (`/^[A-Za-z]{2,4}$/`)، و`splitExtension` تردّ ما عداها فيصير الاسم «بلا امتداد».
  2. `canonical-name.ts`: `extensionOf(fileName, mimeType)` — الامتدادُ المعروف، فإن لم يكن فمن `documents.mime_type` المقيَّد، وإلّا `pdf`. ومرّ العمود في `/api/drive-rename` و`/api/drive-sync`.
  3. **ولم يُقصَر على قائمةٍ معدودة عمداً:** امتدادٌ صحيح خارجها (`xlsx`، `csv`) يصير «بلا امتداد» فيُقترَح له امتدادٌ آخر — وذاك كسرُ الملفّ الذي نتفاداه. والاختبار يحرس الطرفين.
  4. سبعةُ اختباراتٍ جديدة في `naming.test.ts` و`canonical-name.test.ts` بالأسماء الحقيقيّة الأربعة.
  - **التحقّق على البيانات الحقيقية:** مسارُ التسمية على نسخة الإنتاج صار يقترح الصواب للأربعة (`…_SAR996.19.pdf` وأخواتها)، ويمتنع عن اثنين بسببٍ معلَن (عرضُ سعرٍ لا صيغة قياسية له، وفاتورةٌ بلا تاريخ مقيَّد) — امتناعٌ صحيح لا صمت.
  - **مخاطرة الإصلاح:** ملفٌّ امتدادُه خمسةُ أحرف فأكثر (`jpeg` أربعة، فلا يقع) يُعدّ بلا امتداد؛ ولا مثال له في الأرشيف (٤٢٨ ملفّاً).
- **ما لا يُصلَح هنا:** أسماءُ الأربعة في الدرايف. الكتابة في الأرشيف **لا تقع بلا إذن أحمد ملفّاً ملفّاً** — سؤالٌ في §١٠ من التقرير.
- **يخالف قراراً في CLAUDE.md؟** نعم، اثنين: القيد الأوّل «لا يُمسّ أرشيف جوجل درايف… وإعادةُ التسمية مقيَّدةٌ بأربعة»، والتعليقَ المكتوب فوق الدالّة نفسها «الامتداد يبقى كما هو — تغييرُه يكسر فتح الملفّ». **والتعليق الذي يصف نيّةً غير محقَّقة أسوأ من غيابه** — وهو قرارٌ مكتوبٌ في الوثيقة وقع خلافُه في الشيفرة.

---

## ٨. مشتبَه بها (لم تُثبَت)

- **S-1 · اتّصال واحد على Vercel يسلسل الصفحة:** «/» تُطلق ٢٣ استعلاماً. مع `max:1` تتسلسل كلّها، فإن كانت الرحلة إلى Neon ٥–١٠ms صارت الصفحة ١٢٠–٢٣٠ms قاعدةً، ومع Fluid Compute تنتظر الطلبات المتزامنة بعضها حتى ١٠ ثوانٍ. لم أقس المنطقة ولا إعداد Fluid.
- **S-2 · `drive-rename` بلا ميزانيّة:** حتى ٢٥ `files.update` (`drive-rename/route.ts:33,164-167`)، و`googleapis` بلا مهلة صريحة في `src/lib/drive.ts:321-331`. درايف بطيء يُقتل عند ٦٠، فتبقى تسميات نُفّذت بلا سطر تدقيقها إن كان التدقيق بعد الحلقة.
- **S-3 · مسارات بلا `maxDuration`:** `match-confirm` (٦٦٥ سطراً، يشغّل المحرّك)، و`match-undo`، و`payment-run`، و`bank-rule`، وغيرها تأخذ مهلة الخطّة الافتراضيّة. لم أتحقّق من الخطّة.
- **S-4 · كشف البنك لم يُعرَف:** الملفّ الحقيقيّ خرج `bank: "بنك غير محدَّد"`، والاستيرادان المحفوظان «الأهلي (SNB)». قد يكون تراجعاً في `src/lib/bank/parsers/detect.ts`، وهو خارج نطاقي، فيُحال إلى CODE.

---

## ٩. ما لم يُفحَص

- **الكلفة الفعليّة للمسار الناجح:** المزيَّف لم يطابق نصّ الملفّات بعد إلحاق البايت، فردّ 503. عدد النداءات على النجاح مُتتبَّع لا مقيس.
- **`/api/drive-sync` حيّاً على نسخة الأرشيف:** كلّ ملفّات PDF مسجَّلة في القاعدة، والملفّات الجديدة ٩٧ ملفّاً نصّيّاً فقط.
- **Vercel ونيون:** زمن الرحلة من Vercel إلى Neon، والمنطقة، وFluid، والخطّة، ونافذة PITR. كلّها تحتاج لوحتي Vercel وNeon.
- **بيئة المعاينة:** هل تشارك قاعدة الإنتاج؟ `/api/ops/db-identity` قائم ومحروس بـ`audit:view`، ولم يُفتح في البيئتين، وبند البوّابة `?`.
- **نصوص رفض مصنِّف الأدوات تشغيلها:** `migrate`، و`verify-invariants`، و`identity-report`، و`link-counterparties`، و`missing-invoices`.
- **`product.service` (OPS-018).**

---

## ملحق: الأوامر وSQL المستعمَلة

- `sbx tph_ops npm run ops:gate`.
- `select table_schema,count(*) from information_schema.tables where table_schema in ('public','neon_auth') and table_type='BASE TABLE' group by 1`، وعدّ صفوف كلّ جدول في `neon_auth`.
- `ALTER DATABASE tph_ops SET log_min_duration_statement = 0`، ثمّ `-1` في النهاية (تحقّقتُ منه في `pg_db_role_setting`).
- **`$SP/ops/exp-timeouts.sh`:** `POST /__control/deepseek` بـ١٢ ردّاً `delayMs:70000`، ثمّ استدعاء متزامن لـ`/api/analyze` (`aval-mod.pdf`) و`/api/ai-analysis` و`/api/statement-reconcile` (`statementId=f325b5b2…`). والعيّنة من `$SP/ops/sample-activity.mjs`:
  `select state,count(*),max(extract(epoch from now()-xact_start)) from pg_stat_activity where datname='tph_ops' group by state` كلّ ٤ ثوانٍ.
- **`$SP/ops/proof-signal.ts`:** خادم `http` محلّيّ يتأخّر 5s، و`callDeepseek` بـ`timeoutMs:2000` و`AbortSignal.timeout(2000)`.
- **`$SP/ops/pages.sh`:** عدّ `duration: … (statement|execute)` في `$SP/pg.log` لكلّ صفحة بعد طلب إحماء.
- **`EXPLAIN`** على: تخصيصات فاتورة، ونافذة التاريخ في الاستيراد، و`countPendingWork`.
- **الاستيراد:** `POST /api/bank-import` بالكشف الحقيقيّ ثمّ `$SP/ops/E-Statement_shifted_2027.xlsx` (معاينة، ثمّ تطبيق، ثمّ تطبيق ثانٍ)، ثمّ:
  - `select bank_import_id,count(*) from bank_transactions where value_date>='2027-01-01' group by 1`
  - `select count(*) from (select identity_key, coalesce(bank_account_id::text,'~') from bank_transactions where value_date>='2027-01-01' group by 1,2 having count(*)>1) x`
- **`$SP/ops/restore.mjs`:** `create database tph_restore`، ثمّ `set check_function_bodies = off`، ثمّ pre-data، ثمّ `insert into t select * from json_populate_recordset(null::t,$1::json)`، ثمّ post-data، ثمّ لكلّ جدول:
  `select count(*), md5(coalesce(string_agg(md5(x::text),'' order by md5(x::text)),'')) from t x`. ثمّ عدّ `pg_trigger` و`pg_constraint` و`pg_indexes` و`pg_proc`، ومحاولة حذف من `audit_logs`، ثمّ `drop database tph_restore`.
- **التكلفة:** خمسة مستندات من `drive-mirror/files` بإلحاق بايت، و`POST /api/analyze`، و`GET /__control/calls?since=…`. ثمّ الملفّ الأصليّ (409)، ثمّ طابور `[{status:404},{status:404}]`.
- **النصوص:** `sbx tph_ops npx tsx --env-file=.env scripts/{check-isolation,audit-data,measure-system,diagnose-unpaid,find-split-suppliers}.ts`.
- **جرد النصوص:** `grep -c` لـ`guard-write`، و`.insert(`، و`.update(`، و`.delete(`، و`process.argv` في كلّ ملفّ من `scripts/*.ts`.
- **حزمة المتصفّح:** `du -sk` و`ls -S` في `$SP/servers/appB/.next/static/chunks`، و`grep -rl '"use client"' src`.
- **في النهاية:** إيقاف الخادم، ثمّ `node $SP/sandbox/reset-db.mjs tph_ops`.
