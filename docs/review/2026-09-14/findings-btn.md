# جرد الأزرار (BTN): التقرير الكامل · `main` عند `375f0d7` · ٢٠٢٦-٠٩-١٤

**كيف فحصت:**
- قرأتُ الشيفرة، ثمّ زحفتُ على الصفحات الخمس والعشرين بخمس جلسات: مالك، ومحاسب، ومشتريات، وجلسة منتهية، وبلا جلسة.
- طلبتُ ١٢٨ رابطاً داخليّاً فريداً. كلّها ردّت 200، ولا 404 ولا «تعذّر عرض هذه الصفحة».
- أرسلتُ الطلبات الكاتبة بـcurl على خادمٍ إنتاجيّ معزول (`next start` على 3300) فوق `tph_btn`، وعددتُ الصفوف قبل كلّ طلبٍ وبعده.
- أوقفتُ الخادم في النهاية.

**الملفّات:**
- كُتب `docs/review/2026-09-14/coverage-btn.md` فعلاً.
- رفضت الأداةُ كتابة `buttons.md` و`findings-btn.md`، فمحتواهما كاملاً في هذا الردّ.

**ملاحظة بيئة (ليست عطباً):**
- جلستا المحاسب والمشتريات كانتا تُحوَّلان إلى `/login` لأنّ `ALLOWED_EMAILS` في الخادم لا يضمّ بريدَيهما. و`src/auth.ts:96` يحذف الجلسة حينها، وهذا يُثبت أنّ قرار «حذف البريد من القائمة البيضاء يُنهي الجلسة القائمة» مطبَّق.
- أعدتُ تشغيل الخادم بقائمةٍ تضمّ هويّات الاختبار، ثمّ شغّلتُ `reseed-sessions.mjs`. بعدها ردّت الجلستان 200.
- ما رأيتُه قبل ذلك من تحويلٍ لغير المالك أُعيد فحصه بعد الإصلاح.
- أُعيدت القاعدة `tph_btn` مرّةً بعد الدفعة الأولى من الاختبارات.

---

## ٠) الخلاصة

| الدرجة | العدد | المعرّفات |
|---|---|---|
| **P0** | 1 | BTN-108 |
| **P1** | 3 | BTN-101 · BTN-102 · BTN-104 |
| **P2** | 6 | BTN-103 · BTN-106 · BTN-110 · BTN-111 · BTN-113 · BTN-114 |
| **P3** | 6 | BTN-105 · BTN-107 · BTN-109 · BTN-112 · BTN-115 · BTN-116 |

**P0 وP1 (كلّها مُثبَتة بالتشغيل):**
- **BTN-108 (P0):** «ارفضه» على مستند فاتورة ينتظر المراجعة يُسقط حجزها. الفاتورة تبقى، وتدخل «جاهز للتحويل» في ملفّ التحويلات. مدير المشتريات يملك الزرّ.
- **BTN-101 (P1):** `match-confirm` بفرعَي `invoiceIds` و`split` يقيّد مالاً داخلاً سداداً، ويقيّد حوالةَ مورّدٍ على فاتورة مورّدٍ آخر. لا زرّ يستدعي الفرعين.
- **BTN-102 (P1):** «سدِّد على حساب المورّد» يأخذ `supplierId` من الجسم إن كانت الحركة بلا مورّد. تحويلٌ شخصيّ صار رصيداً عند «أوراق الزيتون».
- **BTN-104 (P1):** «أكّد التعريف» يَعِد بأن يعمّ التعريف على «الكشوف السابقة الآن»، والخادم لا ينشره.

---

> الجدول الكامل والواجهات بلا مستدعٍ في `buttons.md`.

## ٤) حال ملاحظات المراجعة السابقة

| القديم | الحال | الدليل |
|---|---|---|
| BTN-001 تعريف الجهة يسقط | **أُصلحت** (م) | `counterparty/route.ts:218,246` تكتب بـ`t`. T9: ‏200 «حُفظت… وطُبّقت على حركة واحدة»، والصفّ صار HUMAN |
| BTN-002 تدقيقٌ لتعلّمٍ لم يقع | **أُصلحت** (ت) | `counterparty.service.ts:218-231` يمرّر `w` (مقبض المعاملة) إلى `recordAudit`. ومورّدٌ غير موجود ← 500 و«لم يُكتب منه شيء» (م) |
| BTN-003 «أعلن سدادها يدوياً» | **أُصلحت** (م) | الزرّ حُذف (`bank-import.tsx:545-567`)، و`mark-paid/route.ts:128` يرفض بلا `invoiceIds` |
| BTN-004 البحث يكشف المال للمشتريات | **أُصلحت** (م) | `search/route.ts:30-31`. المشتريات: «غاناش» مورّدون بلا `amountMinor`، و«5432.60» صفر نتائج |
| BTN-005 المزامنة تسمّي | **أُصلحت** (ت) | `grep renameFile src/app/api/drive-sync` = 0. التسمية في `/api/drive-rename` وحدها |
| BTN-006 «دفعة مقدَّمة» لا تظهر | **أُصلحت** (ت) | `reconcile-queue.tsx:310` و`review-workspace.tsx:69` |
| BTN-007 «سداد مورّد» بلا قائمة | **أُصلحت** (ت) | `review-workspace.tsx:599-613`، والاسم من `canonical.beneficiary` (:624) |
| BTN-008 أزرار لمن لا يملكها + رسالة إنجليزيّة | **أُصلحت** (م) | 403 بعربيّة تسمّي الصلاحية لـmark-paid وpayment-run وmonth-close. والأزرار مخفيّة بـ`canApprove/canEdit`. **ويبقى:** «ارفضه» للمشتريات على فاتورة (BTN-108) |
| BTN-009 حدّ mark-paid عشرة | **أُصلحت** | `lib/rate-limit.ts:88` = 200 |
| BTN-010 فرز الأصناف يتخطّى بنداً | **أُصلحت** (ت) | `product-mapping.tsx:216-222`: الفهرس لا يتقدّم |
| BTN-011 «أعد المطابقة» يكرّر التنبيهات | **أُصلحت** (ت) | `statement-reconcile/route.ts:357` يحذف المفتوح قبل الإدراج |
| BTN-012 «المستحقّ» يُسقط الجزئيّ | **أُصلحت** (م) | `?paid=OPEN` في nav.ts:56 وbank:414 وpage:120. 14 ↔ 14. (رقم الرئيسيّة: UI-001، غيري) |
| BTN-013 «تُحذف الدفعة» | **أُصلحت** (م) | `match-explain.tsx:149`، والأثر REVERSED |
| BTN-014 كلّ تعريفٍ يُنشئ جهة | **أُصلحت** (م) | T9: `created:false` مرّتين متزامنتين |
| BTN-015 موانع الإقفال بلا رابط | **أُصلحت** | `month-close.ts:275` fixHref، و`month-close.tsx:173` «أصلِح ←» |
| BTN-016 «عطّله» بلا تأكيد، والمصروف اليدويّ بلا زرّ | **أُصلحت** (ت) | `recurring-expenses.tsx:89-140` و`manual-expense.tsx`. وبقي الحذف ← BTN-114 |
| BTN-017 لا حارس لتكرار الدفعة على الحركة | **أُصلحت** (م) | متزامنان «سدِّد»: `payments` +1 |
| BTN-018 اللوح يُغلق بعد الفشل · انشغالٌ واحد | **أُصلحت** | `ui-client.tsx:103` · `review-workspace.tsx:103` |
| BTN-019 «افتحها في البنك» أعلى الصفحة | **جزئيّة** | الطابور `/bank?tx=` ✓. وبقي `expense-reclassify.tsx:130` ‏`#tx-` |
| BTN-020 ١٢ مورّداً من ٢٢ | **أُصلحت** | `invoices/page.tsx:182` |
| BTN-021 البحث | **جزئيّة** | الإلغاء ✓ (:59). والرسالة ثابتة «تعذّر البحث. حاول ثانيةً.» (:151) تُهمل `failed` الذي يحمل 429/401/الشبكة |
| BTN-022 «مسح» يرمي القراءات | **أُصلحت** | `uploader.tsx:618` «امسح ما فشل» |
| BTN-023 إعادة التسمية مختارة سلفاً | **أُصلحت** | `drive-rename.tsx:36` و`drive-sync.tsx:75` يبدآن فارغين |
| BTN-024 التصدير بلا تدقيق | **أُصلحت** (م) | `PAYMENT_RUN_EXPORTED` صفر ← واحد. وشهرٌ غير صالح يعرض JSON خاماً، والزرّ لا يُنتج شهراً غير صالح |
| BTN-025 قائمة «يحتاج مراجعة» بلا زرّ | **أُصلحت** (م) | «أكّده» و«ارفضه» يعملان، لكنّ «ارفضه» يُحدث BTN-108 |
| BTN-027 ستّة أسماء للسداد | **قائمة** | انظر §٣ |
| BTN-028 بطاقات معطَّلة بلا سبب | **أُصلحت** | `settings/page.tsx:86-111` `disabledReason` |
| BTN-029 «انسخ المذكّرة» بلا catch | **أُصلحت** | `statement-reconcile.tsx:319` |
| BTN-030 «صنّفها» يستبدل بصمت | **أُصلحت** (م) | الرسالة «واستُبدلت قاعدةٌ كانت تصنّفه حكومي…» |
| BTN-031 «ليست سداداً» بلا تراجع وتدقيقٌ باسمٍ خاطئ | **جزئيّة** (م) | التدقيق `MATCH_REJECTED` ✓، والخادم يقبل الردّ. لكن لا زرّ، والحركة لا تعود إلى طابور ← BTN-106 |
| BTN-032 روابط أعمّ من المقصود | **جزئيّة** | الرئيسيّة ✓. و`[slug]` «كشوفه» و«طابقها» ← /statements بلا مورّد |

---

## ٥) الملاحظات الجديدة

### BTN-108 — «ارفضه» على فاتورةٍ تنتظر المراجعة يُطلقها إلى ملفّ التحويلات
- **الدور:** المدقّق المالي · **الدرجة:** P0 · **الثقة:** مُثبَتة
- **الموضع:**
  - الزرّ في `src/components/reject-document.tsx:25-38`، ويُعرض في `src/app/documents/page.tsx:333-337` بشرط `canDecide = can(role,"document:upload")`، والمشتريات يملكها.
  - الخادم في `src/app/api/document-status/route.ts:79-100`: يحوّل `documents.status` وحده، ولا يمسّ `invoices`.
  - الحجز في `src/app/payments/page.tsx:51` (`bool_or(documents.status = 'NEEDS_REVIEW')`)، ومصدره `lib/payment-run.ts`.
- **إعادة الإنتاج (tph_btn):**
  1. المستند `1f077fba…` (بيكوف 00420، 240.00) حاله `NEEDS_REVIEW` وله فاتورة. قبل الرفض: «ما بقي عليك» 6,851.34.
  2. بجلسة **المشتريات**: `curl -X POST /api/document-status -H 'cookie: authjs.session-token=sbx-purchasing-session' -d '{"documentId":"1f077fba-14fe-451e-9f47-a45fcb70c830"}'` ← `200 «رُفض المستند — وملفّه باقٍ في الدرايف كما هو»`.
  3. الاستعلام: `documents.status = REJECTED`، والفاتورة 00420 قائمة، وتخصيصها 0.
  4. «ما بقي عليك» ما زال 6,851.34، و00420 تظهر في `?paid=OPEN`.
  5. `GET /api/payment-run?month=2026-09` ← السطر `بيكوف,240.00,SAR,1,00420`. وصفحة `/payments?month=2026-09` تضعها في «جاهز للتحويل». أمّا `SI-0057` (ما زال `NEEDS_REVIEW`) فتحت «محجوز حتى تُعالَج».
- **المتوقَّع:** رفض المستند يُخرج فاتورته من المستحقّ ومن ملفّ التحويلات، أو يُمنع الرفض ما دامت له فاتورة. ولا يُعرض للمشتريات على مستندٍ ماليّ لا يرى مبلغه.
- **الأثر:**
  - الرفض، وهو الفعل الذي يقول «هذا المستند خطأ»، هو بالضبط ما **يرفع الحجز** عن قراءةٍ لم يرها إنسان.
  - اليوم ٦ فواتير بحال `NEEDS_REVIEW` قيمتها ≈ 3,780 ريالاً (بيكوف 240 · غاناش 1,030.40 · كوفي لابز 638 · أفال 996.19 · لوريفا 632.50 · …) تصير بضغطةٍ «جاهزة للتحويل».
  - وتبقى المرفوضة محسوبةً في «عليك» وفي الإقفال.
  - وتُكسر حماية SEC-009 التي أُضيفت في `8657678`.
- **الإصلاح:**
  - في معاملة الرفض: إن كان للمستند فاتورة بلا تخصيص تُحذف أو تُوسَم ملغاة بحالٍ صريحة (مع تدقيق)، وإن كان لها تخصيص يُرفض الرفض بـ409 «لها سدادٌ مقيَّد».
  - حجز التحويل يُبنى على «ليس ARCHIVED» لا على «NEEDS_REVIEW».
  - زرّ «ارفضه» يُشرط بـ`amounts:view` حين يكون المستند فاتورة.
  - الجهد نصف يوم مع اختبارٍ يلمس القاعدة. **ومخاطرته:** حذف فاتورة قرارٌ ماليّ، فالأسلم الحال لا الحذف، ومؤثِّر الشهر المقفل (028) يجب أن يُحترم.
- **يخالف قراراً في CLAUDE.md؟** نعم نصّاً: «ما قرأه النموذج في المزامنة «ينتظر المراجعة»، وملفّ التحويلات يحجزه… **لا يدخل مالٌ إلى البنك على قراءةٍ لم يرها إنسان**». وأيضاً «**مدير المشتريات** يرفع ويتابع الناقص فقط — لا أرقام مالية» (`permissions.ts:47`).
- **المحامي المضادّ:** حاولتُ نقضها بأربعة أسئلة، ولم تنتقض:
  - هل تُحذف الفاتورة في خدمةٍ أخرى عند الرفض؟ `document-status/route.ts` لا يستدعي خدمة، والاستعلام بعده يُثبت بقاءها.
  - هل يحجز `buildPaymentRun` المرفوض؟ لا: شرطه `needsReview` وحده، والـCSV حمل 00420.
  - هل هو مقصود لأنّ «الرفض لا يمسّ الأرشيف»؟ التعليق يمنع رفض المؤرشف فقط، ولا يقول إنّ فاتورة المرفوض تبقى مستحقّة.
  - هل التحويل الفعليّ بيد إنسان؟ نعم، لكنّه يحوّل ما يقوله الملفّ «جاهز»، وهذا رقمٌ كاذب يُبنى عليه دفع.

### BTN-101 — `match-confirm` بفرعَي `invoiceIds`/`split` يقيّد مالاً داخلاً سداداً، وحوالةَ مورّدٍ على فاتورة مورّدٍ آخر
- **الدور:** المدقّق المالي · **الدرجة:** P1 · **الثقة:** مُثبَتة
- **الموضع:** `src/app/api/match-confirm/route.ts:277-393` (فرع `invoiceIds`)، و`:273-275` ← `applyManualSplit` `:404-511`. **لا زرّ يستدعيهما** (`grep -rn "invoiceIds\|split:" src/components` لا يجد إلّا mark-paid).
- **إعادة الإنتاج (المالك):**
  1. حركة **واردة** (تسوية شبكة 122.00): `{"transactionId":"384a5b0d-3122-4787-b238-130a8254e6a3","invoiceIds":["3f3b6ee3-0149-4224-bb4b-3be6c22f01f9"]}` ← `200 «طُوبقت مع فاتورة واحدة»`. `payments` 65 ← 66، والحركة `direction=CREDIT, category=SUPPLIER, MATCHED`: صار إيراد الشبكة سداداً لفاتورة بيكوف 00363.
  2. حوالة «أفال — بدر» 8,402.77 (بعد التراجع) مع فاتورة بيكوف 00372: ‏200 «طُوبقت مع فاتورة واحدة وبقي 8,252.77 بلا تخصيص». الاستعلام: `tx_supplier = pay_supplier = بيكوف`، أي **أُعيد كتابة مورّد الحركة**.
  3. `split` بهللة واحدة ← 200 «وبقي 8,402.76 بلا تخصيص».
- **المتوقَّع:** «المال الداخل ليس سداداً» كما في فرع `settleSupplier` (`:258`)، ورفض فاتورةٍ لغير مورّد الحركة.
- **الأثر:** فواتير تُغلق بمالٍ لم يخرج، ورصيدٌ وهميّ عند مورّد تخصمه «دفعة أوّل الشهر» (`payments/page.tsx:59-61`)، فلا يُدفع مستحقّ.
- **الإصلاح:** حذف الفرعين، فهما بلا مستدعٍ منذ `da6fc08`. أو إضافة شرطين: `tx.direction==='DEBIT'`، والمورّد يطابق مورّد الحركة إن وُجد. الجهد ساعة للحذف. المخاطرة: لا مستدعي.
- **يخالف قراراً؟** نعم نصّاً: «**المال الداخل ليس سداداً** — كانت الحركة الواردة تمرّ إلى المطابقة، فيُنشَأ سدادٌ من مالٍ دخل وتُقفَل فاتورةٌ لم تُدفَع»، و«الخادم لا يثق بالمتصفّح».
- **المحامي المضادّ:** حاولتُ نقضها بثلاث طرق، ولم تنتقض:
  - قيد في القاعدة؟ مؤثِّرا 007/026 يحرسان التجاوز لا الاتّجاه ولا المورّد، والإدراج مرّ فعلاً.
  - صلاحية؟ `payment:approve` للمالك وحده، فلا يقع بخطأ نقرة. لكنّه مسارٌ حيّ يخرق قاعدةً مكتوبة.
  - زرّ خفيّ؟ لا.

### BTN-102 — «سدِّد على حساب المورّد» يصدّق `supplierId` من الجسم
- **الدور:** مهندس الأمن · **الدرجة:** P1 · **الثقة:** مُثبَتة
- **الموضع:** `src/app/api/match-confirm/route.ts:250` ‏`g.supplierId ?? body.supplierId ?? ""`، وتعليقه (`:245-249`) يقول «لا يصدّق المتصفّح».
- **إعادة الإنتاج:** حركة «احمد محمد يسلم الجعيدي — تحويل إلى الأهل» 2,875.00 (`1a441330…`، ‏PERSONAL، ‏supplier_id=null):
  - الطلب: `{"transactionId":"1a441330-13de-44fb-adb5-4d0a716b5d5d","settleSupplier":true,"supplierId":"43c50d9d-d9f0-44ba-9ff8-09420dda71a2"}`.
  - الردّ: `200 «قُيّد 2,875.00 على حساب المورّد — لا فاتورة مفتوحة تقابله»`.
  - بعده: `category=SUPPLIER, supplier_id=أوراق الزيتون`، ودفعة غير مخصَّصة.
- **المتوقَّع:** 400 «لا يُسدَّد حسابٌ بلا مورّد واحد يجمع الحركات».
- **الأثر:** تحويلٌ شخصيّ يصير «رصيداً لك عند أوراق الزيتون»، فيُخصم من دفعة أوّل الشهر وتنقص سحوبات المالك.
- **الإصلاح:** حذف `?? body.supplierId` (الشاشتان لا ترسلانه: reconcile-queue:192، review-workspace:193)، ورفض `category <> 'SUPPLIER'`. الجهد ساعة.
- **يخالف قراراً؟** «الخادم لا يثق بالمتصفّح».
- **المحامي المضادّ:** هل تحتاجه شاشة؟ الأزرار لا تظهر إلّا حين يُعرف المورّد (`reconcile-queue.tsx:290`، `review-queue.ts:120-124`)، فالحقل لا يُحتاج. لم تنتقض.

### BTN-104 — «أكّد التعريف» يَعِد بتعميمٍ على «الكشوف السابقة الآن» حذفه الخادم
- **الدور:** أحمد · **الدرجة:** P1 · **الثقة:** مُثبَتة
- **الموضع:**
  - النصوص: `bank/page.tsx:474` · `reconcile-queue.tsx:417-421` · `review-workspace.tsx:633` · `expense-reclassify.tsx:119` («ويسري على أمثاله»).
  - الخادم: `counterparty/route.ts:289-304` («النشر لا يقع في الطلب… `db:reclassify` ينشره»).
- **إعادة الإنتاج:**
  - قبل: حركات `BEN ID:2149830115` = 36 PERSONAL (24 منها MEMORY).
  - `POST /api/counterparty {"transactionId":"6a67b98c…","kind":"OTHER","displayName":"اختبار"}` ← 200 «حُفظت «جهة بلا اسم»، وطُبّقت على حركة واحدة».
  - بعد: الحركة وحدها OTHER/HUMAN، و**الـ24 باقية PERSONAL/MEMORY**. والاسم المكتوب «اختبار» لم يُحفظ لأنّ الجهة قائمة، فالرسالة تقول «جهة بلا اسم».
- **الأثر:** يظنّ أحمد أنّ أخواتها في الكشوف القديمة حُسمت فلا يعود إليها، وتبقى بتصنيفها القديم في كلّ تقرير. وهذا عكس «لا يريد أن يُسأل السؤال نفسه مرّتين»: لن يُسأل، وسيبقى الخطأ.
- **الإصلاح:** نصٌّ صادق: «تُحفَظ أدلّتها فتُعرَف في الكشف القادم، وطُبّقت الآن على ما اخترت»، مع ذكر عدد الأخوات الباقيات. أو نشرٌ خلفيّ بعد الإيداع بعددٍ معلَن. الجهد نصف ساعة للنصّ، ويوم للنشر.
- **يخالف قراراً؟** نعم نصّاً: «**التأكيد يعمّ على القاعدة كلّها لا على الكشف القادم.** بعد الحفظ تُقرأ الذاكرة من جديد ويمرّ المصنِّف نفسه… على ما بقي». الشيفرة ألغت القرار، وCLAUDE.md والواجهة لم يتبعاها.
- **المحامي المضادّ:** حاولتُ نقضها بسؤالين، ولم تنتقض:
  - هل يقع النشر عند الاستيراد القادم؟ للقادم فقط، والنصّ يقول «السابقة الآن».
  - هل هناك مهمّة خلفيّة؟ `grep reclassify src/app` = 0.
  - وتصنيف الـ24 كان صحيحاً بالمصادفة (PERSONAL)، لكنّ الوعد نفسه كاذب مهما كان الباب.

### BTN-103 — «أكّد التراجع» متزامناً: ردّان وقيدا تدقيق لدفعةٍ واحدة
- **الدور:** مهندس الجودة · **الدرجة:** P2 · **الثقة:** مُثبَتة
- **الموضع:** `match-undo/route.ts:55-61` يقرأ خارج المعاملة، و`:104-119` يحدّث بلا شرط `matched_payment_id = paymentId`، والتدقيق خارج المعاملة (`:172`).
- **إعادة الإنتاج:** طلبان متزامنان على `50a13268…` ← كلاهما 200: الأوّل «رُدّت الدفعة و5 تخصيصاً»، والثاني «**ورُدّت الدفعة** و0 تخصيصاً». `audit_logs` +2 و`decision_history` +2. المال سليم.
- **الأثر:** سجلٌّ لا يُعدَّل يحمل ردَّين لواقعة واحدة. الزرّ معطَّل أثناء الانتظار، فالخطر من تبويبين أو إعادة ضغطٍ بعد انقطاع.
- **الإصلاح:** `for update` أو شرطٌ يُعدّ أثره ثمّ «رُدّت من قبل». الجهد ساعة.
- **يخالف قراراً؟** «الحركة تُربط بدفعتها بشرطٍ يُعدّ أثره»: طُبّق على الربط لا على الفكّ.

### BTN-106 — ردّ «ليست سداداً» يقول «عادت تنتظر قراراً» ولا تعود، ولا زرّ له
- **الدور:** أحمد · **الدرجة:** P2 · **الثقة:** مُثبَتة
- **الموضع:** `match-undo/route.ts:66-93` لا يُعيد `category`، و`pending.ts:49-78` يطلب UNKNOWN أو SUPPLIER، و`match-explain.tsx:136` يُظهر التراجع لما له دفعة وحده.
- **إعادة الإنتاج:** حركة مورّد معلّقة (pending=1) ← `notAPayment:"INTERNAL"` (pending=0) ← `POST /api/match-undo` ← 200 «عادت الحركة تنتظر قراراً» ← pending **ما زال 0**، والحركة `INTERNAL/SUGGESTED/REVIEW/UNMATCHED`.
- **الأثر:** رسالة كاذبة، وشارة «تنتظر مراجعتك» حمراء في «آخر ما قرّره المحرّك» بلا فعل.
- **الإصلاح:** إعادة الباب من `decision_history.payload["الباب السابق"]`، وزرّ «ردّ الإعلان» في MatchExplain. الجهد نصف يوم.
- **يخالف قراراً؟** «كل مطابقة قابلة للتراجع»، و«زرٌّ لا يعمل أسوأ من زرٍّ غائب».

### BTN-110 — تنبيهان يفتحان صفحةً لا تعرض ما عدّاه
- **الدور:** أحمد · **الدرجة:** P2 · **الثقة:** مُثبَتة
- **الموضع:** `attention.ts:309` («13 دفعة خرجت ولا فاتورة تفسّرها» ← `/suppliers`)، و`attention.ts:432` («13 مورّداً لم يصل كشفه» ← `/statements`).
- **إعادة الإنتاج:**
  - أعداد /attention من HTML: `unbacked-payments count=13`، و`missing-statements count=13`.
  - `/suppliers` لا يحوي «دفعة» ولا «غير مخصَّص» ولا «بلا فاتورة» (grep = 0).
  - `/statements` لا يحوي «لم يصل» (grep = 0)، وتعرض قائمة كشوفٍ مؤرشفة لا قائمة الغائبين.
- **الأثر:** «يودّي على مكانٍ غلط» حرفيّاً: يضغط أحمد فيجد قائمة مورّدين عامّة، فيعود إلى «اعرض التفاصيل» ليحفظ الأسماء بعينه. والدليل المقصوص عند ستّة والعدد 13، فالسبعة الباقية لا تُبلَغ من أيّ مكان.
- **الإصلاح:** `/suppliers?unbacked=1` تعرض الدفعات غير المخصَّصة مورّداً مورّداً مع «اطلب الفاتورة»، و`/statements?missing=1` تسمّي الغائبين. الجهد نصف يوم.
- **يخالف قراراً؟** `attention.ts:84` «الصفحة التي يُعالَج فيها — البند بلا مكان لا يُعالَج»، وCLAUDE.md «كل تنبيه يحمل `href` (مكان الإصلاح)».

### BTN-111 — «مصروف وصل مرّتين: احذف الزائد بيدك» ولا قائمة ولا زرّ حذف
- **الدور:** أحمد · **الدرجة:** P2 · **الثقة:** مُثبَتة
- **الموضع:** `attention.ts:247-262` ← `/money/expenses`. الصفحة لا تحمل قسم ازدواج (`grep duplicate app/money/expenses/page.tsx` = 0). و`expense-actual` ‏`action:"delete"` (`route.ts:61`) بلا مستدعٍ.
- **إعادة الإنتاج:** /attention «1 مصروفاً يصف حدثاً وصل مرّتين» ← الصفحة لا تحمل من الأزرار إلّا «اشتقّ من كشف 2026-09» و«+ قيّد مصروفاً دُفع نقداً».
- **الأثر:** مصروف الشهر أعلى بقيمة الزائد، ولا يُصلَح إلّا بـSQL.
- **الإصلاح:** قسمٌ يعرض الزوجين بمصدريهما، وزرّ «احذف هذا» يستدعي الفرع القائم مع `recordAudit` (غائب اليوم عن `expense-actual`). الجهد نصف يوم.
- **يخالف قراراً؟** «وازدواجُ الحدث يُكشَف ولا يُحذَف» (آليّاً)، والتنبيه نفسه يطلب الحذف بيد إنسان ولا يعطيها.

### BTN-113 — بطاقة «قواعد تصنيف الحركات» تفتح صفحةً بلا قاعدة
- **الدور:** المصمّم · **الدرجة:** P2 · **الثقة:** مُثبَتة
- **الموضع:** `settings/page.tsx:76` ‏`href:"/bank"`، وعددها من `bank_rules` (`:37`).
- **إعادة الإنتاج:** /settings ← البطاقة ← /bank: كلمة «قاعدة/قواعد» صفر مرّة. لا عرض للقواعد ولا حذف. القاعدة تُكتب فقط من صفّ «صنّفها» داخل معاينة استيراد كشف.
- **الأثر:** قاعدةٌ خاطئة (مثل `customs` ← OTHER في اختباري) لا تُرى ولا تُمحى من الواجهة، وتسري على كلّ كشف.
- **الإصلاح:** قائمة قواعد بحذف وتدقيق، أو نزع الرابط وكتابة «تُضاف عند استيراد كشف». الجهد يوم للقائمة، ودقائق للنزع.

### BTN-114 — «قيّده» مرّتين يقيّد مصروفين، ولا حذف ولا تدقيق
- **الدور:** مهندس الجودة · **الدرجة:** P2 · **الثقة:** مُثبَتة
- **الموضع:** `manual-expense.tsx:74`، و`api/expense-actual/route.ts:66-95` بلا مفتاح تكرار وبلا `recordAudit` (grep = 0).
- **إعادة الإنتاج:** طلبان متزامنان `{"action":"record","label":"صيانة اختبار","amount":"120.5","occurredOn":"2026-09-10","category":"OTHER"}` ← 200 و200، و`expenses` صفّان 12050. و«١٢٠» بأرقامٍ عربيّة تُقبل (سليم).
- **الأثر:** مصروفٌ مضاعف بلا باب إصلاح ولا أثر في سجلّ التدقيق. وهو يُكتب مالاً في «قائمة الدخل».
- **الإصلاح:** `recordAudit` في الفرعين، ومنعٌ ناعم لنفس (البند، المبلغ، التاريخ) خلال دقيقة يُعرض لا يُحذف، وزرّ حذف. الجهد ساعتان.
- **يخالف قراراً؟** «الشهادة/السجلّ لكلّ كتابة ماليّة» في روحه.

### BTN-105 — الخاسر في السباق يردّ 500 بجسمٍ فارغ
- **الدور:** مهندس الجودة · **الدرجة:** P3 · **الثقة:** مُثبَتة
- **الموضع:** `mark-paid/route.ts:45-53`، و`match-confirm/route.ts:66-74` (خطأ 23514 من `assert_allocation_within_bounds`)، و`supplier/route.ts` (قيد فرادة).
- **إعادة الإنتاج:** «حم ← أكّد» متزامنان على بيكوف 00346 ← 200 و500. «سدِّد» متزامنان ← 200 و500. «أنشئه» لمورّدٍ بالاسم نفسه ← 200 و500. القاعدة سليمة في الثلاثة، والشاشة تقول «ردّ الخادم بخطأ (الرمز 500)».
- **الإصلاح:** ترجمة 23514 و23505 إلى 409 «سُجّلت من قبل — حدّث الصفحة». الجهد ساعة.

### BTN-107 — مورّدٌ غير موجود في `/api/counterparty` يردّ 500 «أعد المحاولة (رمز 23503)»
- **الدور:** مهندس الجودة · **الدرجة:** P3 · **الثقة:** مُثبَتة
- **إعادة الإنتاج:** `{"transactionId":"6a67b98c…","kind":"SUPPLIER","supplierId":"nope"}` ← 500. «أعد المحاولة» لا تُصلح شيئاً.
- **الإصلاح:** فحص المورّد قبل المعاملة ← 400. الجهد دقائق.

### BTN-109 — «أكّده» على مستندٍ بلا فاتورة يقول «ويدخل فاتورتُه دفعةَ الشهر»
- **الدور:** المدقّق المالي · **الدرجة:** P3 · **الثقة:** مُثبَتة
- **الموضع:** `document-status/route.ts:75`.
- **إعادة الإنتاج:** المستند `1a508321…` (WesternRoastery، ‏TAX_INVOICE، ‏0 فواتير) ← «أكّده» ← 200 بالرسالة نفسها، والحال ARCHIVED ولا فاتورة.
- **الأثر:** فاتورة 845 ريالاً (من اسم الملفّ) تُؤرشف بلا قيد، والشاشة تقول إنّها دخلت الدفعة.
- **الإصلاح:** رفض التأكيد أو تغيير الرسالة حين لا فاتورة. الجهد ساعة.
- **يخالف قراراً؟** «الفشل يُسمَع — كانت الفاتورة التي يتعذّر قراءة إجماليها تُؤرشَف بصمت وتُعرض «نجحت»».

### BTN-112 — «مستحقّات تأخّرت» يعدّ مورّدين والوجهة تعدّ فواتير
- **الدور:** أحمد · **الدرجة:** P3 · **الثقة:** مُثبَتة
- **الموضع:** `attention.ts:362` ‏`count: f.overdueSuppliers.length`.
- **الواقع:** البطاقة 1 ← `?overdue=1` «المعروض فاتورتان».
- **الإصلاح:** العدّ بالفواتير أو التسمية «مورّد واحد». الجهد دقائق.

### BTN-115 — أفعالٌ غائبة صغيرة
- **الدور:** المصمّم · **الدرجة:** P3 · **الثقة:** مُتتبَّعة
- **ما يغيب:**
  - «سُدّد مرّتين» (`bank/page.tsx:429-467`) يقول «يُطالَب به» بلا رسالة مطالبة ولا وسم «طُولِب».
  - فرع `product` ‏`unlink` بلا زرّ.
  - زرّ الباب في `reconcile-queue.tsx:354` غير معطَّل أثناء الحفظ.
- **الإصلاح:** رسالة واتساب كالتي في /payments، و«فُكّ الربط»، و`disabled={busy}`. الجهد ساعتان.

### BTN-116 — ثلاثة أزرار رفع في الرئيسيّة
- **الدور:** أحمد · **الدرجة:** P3 · **الثقة:** مُتتبَّعة
- **الموضع:** «+ رفع» (القشرة) · «+ ارفع فاتورة» (`page.tsx:112`) · «أضف مستنداً» (`:246`)، وثلاثتها إلى `/upload`.
- **الإصلاح:** حذف `:246-247` («الأرشيف» موجود في التنقّل). الجهد دقائق.

---

## ٦) مشتبَه بها (لم تُثبَت)
- **التدقيق خارج المعاملة** في `settleAccounts` (`match-confirm/route.ts:633`) و`match-confirm-bulk/route.ts:310`: فشل التدقيق بعد الإيداع يترك دفعة بلا قيد تدقيق. لم أُحدث فشلاً.
- **`bank-import.tsx:560` يستعمل `<a>`** لا `Link`: تحميلٌ كامل. أثرٌ على السرعة لا على الصحّة.
- **`/purchases/invoices` على الحاسوب**: الصفّ غير قابل للنقر، ورابطه في الجوّال وحده.
- **مجموعات «حلّ المعلّقات»**: لم تظهر في اللقطة (الطابور صفر)، فلم أفحص «أكّد — وطبّقها على N» على مجموعة حقيقيّة، ولا ردّ الخادم لمعرّفاتٍ لا يجمعها نمط (`counterparty/route.ts:139-145`).

## ٧) ما لم يُفحَص
- **بالتشغيل:**
  - رفع مستند وقراءته وأرشفته (`analyze`/`archive`)، وعند غيري SEC-102 وSEC-106.
  - استيراد كشف البنك وتطبيقه (`bank-import` بملفّ).
  - المزامنة وإعادة التسمية (`drive-sync`/`drive-rename`)، وعند غيري SEC-104.
  - مطابقة الكشف (`statement-reconcile`، يستدعي النموذج).
  - تحليل الذكاء (`ai-analysis`).
  - ربط الأصناف (`product`).
  - المصروف المتكرّر (`expense`، مُتتبَّع فقط).
  - «اشتقّ من كشف البنك».
  - «خروج» و«الدخول بحساب جوجل».
- **بالقراءة سطراً سطراً:** `uploader.tsx` و`drive-sync.tsx` و`drive-rename.tsx` و`product-mapping.tsx` و`ai-analysis.tsx` جُردت أزرارها بـgrep لا بقراءةٍ كاملة.
- **بمتصفّح:** التركيز، ولوحة المفاتيح، ومقاسات اللمس، والنقر المزدوج الفعليّ (قيس بطلبين متزامنين)، وتحديث الأرقام بعد `router.refresh` بصريّاً (قيس بـHTML بعد الفعل).
- **التغطية:** الصفحات ٢٥/٢٥ وروابطها ١٢٨/١٢٨ · المكوّنات ٣٢/٣٢ جُردت (٢٥ قراءة كاملة) · الواجهات ٢٥/٢٥ جُردت، و١٤ منها أُرسل طلبها. التفصيل في `docs/review/2026-09-14/coverage-btn.md`.

**حال البيئة:** `tph_btn` تحمل آثار اختباراتي (رفض 00420، وتأكيد WesternRoastery، ورصيدا أغسطس، ومصروفات «اختبار»، ومورّد «مورّد اختبار الأزرار»، وقاعدة `customs`=OTHER). تُعاد بـ`reset-db.mjs tph_btn` قبل أيّ استعمالٍ آخر. والخادم 3300 أُوقف.
