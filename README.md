# نظام إدارة فواتير مؤسسة ذا بوبليك هاوس

تطبيق ويب يدير مالية المقهى وتشغيله: من لحظة وصول المستند حتى قيده وسداده
ومطابقته بالبنك وبكشوف مورّديه — مرتبطاً مباشرة بأرشيف جوجل درايف القائم،
دون تغيير بنيته ولا إعادة تسمية ملفاته.

**الرقم الضريبي:** ٣١٠٠٠٧٩٧١٦٠٠٠٠٣ · **السجل التجاري:** ٧٠٥٢٧٦٦٩٤١

---

## الرابط المباشر

**https://tph-invoicing.vercel.app**

يعمل من الآيفون والآيباد واللابتوب بلا تحميل شيء. ومن سفاري في الآيفون اضغط
زر المشاركة ثم «إضافة إلى الشاشة الرئيسية» ليصير أيقونة تفتح بملء الشاشة كالتطبيقات.

## ما هو هذا النظام

نظام إدارة الدورة المالية والتشغيلية للمقهى. يبدأ من المستند حين يصل — بفاتورة
أو كشف أو إيصال — وينتهي عند سؤال واحد يفتحه صاحب العمل كل صباح:

> **ماذا أحتاج أن أعرف أو أفعل اليوم؟**

لا: «ما السجلات الموجودة في قاعدة البيانات؟»

### المساحات السبع

| المساحة | السؤال الذي تجيب عنه |
|---|---|
| `/` **الرئيسية** | حال المقهى: المشتريات · المستحقّ · ما يحتاج انتباهك · صحّة البيانات |
| `/attention` **يحتاج انتباهك** | كل الاستثناءات في مكان واحد، لكلٍّ منها خطوة ومكانٌ يُعالَج فيه |
| `/purchases` **المشتريات** | الفواتير · المورّدون · الأصناف · المستحقّات · ذكاء الشراء · كشوف المورّدين |
| `/money` **المال** | البنك · المستحقّات · المصروفات · الضريبة · التدفّق النقدي · الإقفال |
| `/performance` **الأداء** | الأصناف وأسعارها — ما تدعمه البيانات وحده |
| `/documents` **المستندات** | صندوق وارد ثمّ أرشيف: ما ينتظرك أوّلاً، ثمّ ما مضى |
| `/settings` **الإعدادات** | المورّدون · قواعد التصنيف · المصروفات المتكرّرة · سجل التدقيق |

والرفع فعلٌ لا مساحة: زرّ `+ رفع` ظاهر في كل صفحة، وصفحته `/upload`.

### ما هو منجَز

| | |
|---|---|
| **الدخول والصلاحيات** | جوجل · قائمة بيضاء · ثلاثة أدوار مفروضة على الخادم |
| **قراءة المستند** | نموذج رؤية يقرأ الملف نفسه — بلا OCR منفصل |
| **الأرشفة** | تسمية وتصنيف ورفع للدرايف بلا استبدال ملف قائم |
| **الفحص الضريبي** | الأركان الأربعة · حدّ الرسملة · حساب الضريبة |
| **محرّك تسوية البنك** | معياريّ ← تصنيف ← تعريف مستفيد ← مرشّحون ← تسوية شاملة ← قرار |
| **تسوية غير جشعة** | تفريعٌ وتحديد يبحث عن أعلى **مجموع** لا أعلى درجة، ويُعلن إن رجع للتقريب |
| **قرارٌ بثلاث درجات** | تلقائيّ · اقتراح · مراجعة — ولا تلقائيّ إلّا بالدرجة **والفارق** عن التالي |
| **تصنيف بطبقات** | البنية ← ما أكّده إنسان ← الكلمات بشروطها ← المجهول يُعلَن |
| **حركات الشبكة** | تسوية البطاقات ورسومها وضريبتها — كانت كلّها «ضجيجاً داخلياً» |
| **ذاكرة المستفيدين** | تأكيدٌ واحد يعمّ: تُحفَظ الهويّة والحساب والآيبان لا نصّ الوصف |
| **حَكَمٌ عند العجز** | يُستدعى نموذجٌ للملتبس وحده، على مرشّحين مولَّدين، وحكمه **اقتراح** |
| **«لماذا؟» والتراجع** | كل مطابقة تحمل أدلّتها بنصّها، وتُفكّ بأثرٍ في سجل التدقيق |
| **طابور المعلّقات** | سؤالٌ واحد عن حركةٍ واحدة، ولكلٍّ زرّ — والتأكيد يُغذّي الذاكرة |
| **قراءة PDF** | نصّه ومواضعه حسابياً — والمصوَّر **يُعلَن** ولا يُخمَّن |
| **كشف البنك** | ستّة توقيعات، والثقة تنمو بعدد العلامات لا بأوّلها |
| **فجوات التغطية** | «بين كشفيك أسبوعٌ لم يُستورَد» — والغائب لا يُرى فيجب أن يُحسَب |
| **الرسوم والردود والإشعارات** | رسمُ التحويل داخل الدفعة · ما خرج ثمّ عاد · الإشعار يخفض المستحقّ |
| **مطابقة كشوف المورّدين** | تكشف الفاتورة التي حُمّلت عليك ولم تصلك |
| **دفعة أوّل الشهر** | تحجز ما ليس ضريبياً كاملاً · ملف تحويلات · رسالة واتساب |
| **إقفال الشهر** | قائمة تحقّق تُقرأ · قفلٌ يمنع الإضافة · إعادة فتح مسجَّلة |
| **الأصناف المعيارية** | ربط أصناف المورّدين بصنف واحد — يُقترح ويؤكّده إنسان |
| **المصروف الفعلي** | مقابل المتوقَّع — وسداد المورّد ليس مصروفاً، وإلّا حُسب مرّتين |
| **التدفّق النقدي وقائمة الدخل** | ممّا هو معلوم وحده |
| **بيان مصدر الرقم** | «من أين جاء؟» تحت كل رقم: ما دخل وما استُبعد ولماذا |
| **الفروع والحسابات** | أساسٌ لا يفترض فرعاً واحداً ولا حساباً واحداً |
| **ثوابت مالية في القاعدة** | لا تخصيص فوق الدفعة ولا فوق الفاتورة — بمؤثِّرات لا بشيفرة |
| **صحّة البيانات** | نسبة تغطية كل شيء، وما ليس موصولاً يُقال عنه ذلك |
| **سجل التدقيق** | غير قابل للتعديل ولا الحذف — بمشغّلات في القاعدة |

### ما هو غير موصول — عمداً

**المبيعات · تكلفة المبيعات · هامش الربح · المخزون · الوصفات · ربحية القائمة.**

كلّها تحتاج مصدر مبيعات لم يُوصَل. وتُعرض في الواجهة **«غير موصولة»** ولا تُملأ
بصفر ولا برقم مقدَّر — لأنّ صفر المبيعات يوحي بأنّ المقهى لم يبع شيئاً، وذلك
أسوأ من الفراغ.

والبنية جاهزة: جداول المبيعات وطرق الدفع والمرتجعات ودفعات التسوية موجودة،
وواجهة الموصل في [`src/lib/sales/connector.ts`](src/lib/sales/connector.ts)
**ترمي** `NotConnectedError` ولا تُرجع أصفاراً. بلا واجهة برمجية لأيّ مزوّد.

وقد صار البنك يعرف تسويات الشبكة ورسومها — وهي نصف السؤال الذي يُنتظَر من
الربط: «بعتُ تسعة آلاف ووصلني ثمانية آلاف وسبعمئة وستّون، أين الفرق؟»

---

## ما جرى ولماذا

سجلّ المراجعات والأعطال التي وُجدت في بيانات حقيقية — وما استُخلص منها:
**[`docs/CHANGELOG.md`](docs/CHANGELOG.md)**

والفحص الشامل للمنتج والمعمارية: **[`docs/AUDIT.md`](docs/AUDIT.md)**

## المبادئ التي تحكم الكود

كلٌّ منها كُتب بعد عطل حقيقي، لا شعاراً:

1. **المال أعداد صحيحة بالهللات** — لا فاصلة عائمة في أي مسار.
2. **المجهول ليس صفراً** — الحالة الضريبية `VALID · INVALID · UNKNOWN · NOT_APPLICABLE`،
   والصافي والضريبة يقبلان الفراغ. الفرق عملي: علاج «غير صالحة» مطالبة المورّد،
   وعلاج «مجهولة» قراءة المستند.
3. **الخادم هو صاحب القرار** — مخرجات النموذج ومدخلات المتصفّح كلاهما غير موثوق.
4. **الاستيراد منيع من التكرار** — بصمة للملف، وهوية لكل حركة، وقيد فريد في
   القاعدة. الفحص في الكود يفلت من طلبين متزامنين، والقيد لا يفلت.
5. **الدرايف لا يُمسّ** — لا دالة حذف ولا نقل في `lib/drive.ts`.
6. **الحساب في دوال خالصة** — فيُختبر كلّه بلا قاعدة بيانات.
7. **الصمت ممنوع** — الفشل يُعلَن، والفراغ خير من التخمين.

**المعمارية الفعلية:** [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — مصدر الحقيقة الواحد.
**دليل التشغيل:** [docs/SETUP.md](docs/SETUP.md)

> [docs/PLAN.md](docs/PLAN.md) وثيقة تاريخية تحمل الخطّة الأولى، وفيها قرارات
> نُقضت — أبرزها أنّها تذكر Prisma والمشروع على Drizzle.

---

## مبدأ ثابت: الأرشيف لا يُمسّ

النظام **لا يحذف ولا ينقل ولا يعيد تسمية** أي ملف موجود في الدرايف.
أدوات المرحلة صفر كلها تطلب صلاحية **قراءة فقط**، فالكتابة مستحيلة تقنياً لا اتفاقاً.
ولا يبدأ الرفع إلا في المرحلة الأولى، وبتأكيد بشري في كل مرة.

---

## التشغيل السريع

```bash
npm install
cp .env.example .env     # ثم املأ القيم — راجع «إعداد جوجل» أدناه
npm run db:push          # مرّةً واحدة فقط: ينشئ الجداول الأولى
npm run db:seed          # يؤسّس سجل المورّدين
npm run db:migrate       # يطبّق الهجرات — وهو الطريق الوحيد بعد التأسيس
npm test                 # يشغّل اختبارات منطق الأعمال
npm run dev              # يشغّل التطبيق على http://localhost:3000

npm run db:audit         # تدقيق اكتمال البيانات وسلامتها
npm run db:repair        # إصلاح أعطال السلامة (بمعاينة أوّلاً)
```

### الأوامر

**التطوير والفحص**

| الأمر | ما يفعله |
|---|---|
| `npm run dev` | تشغيل التطبيق للتطوير |
| `npm test` | ١٬٢٥٨ اختباراً في ٨٥ ملفاً — منطق الأعمال كلّه |
| `npm run test:watch` | الاختبارات في وضع المراقبة |
| `npm run typecheck` | فحص الأنواع |
| `npm run lint` | فحص الأسلوب |
| `npm run build` | البناء كما يبنيه Vercel |

**القاعدة**

| الأمر | ما يفعله |
|---|---|
| `npm run db:migrate` | يطبّق الهجرات — **الطريق الوحيد لتغيير المخطّط** |
| `npm run db:verify` | يثبت أنّ القيود المالية ترفض فعلاً — والقيد بلا اختبار ادّعاء |
| `npm run db:push` | ينشئ الجداول الأولى — **مرّةً واحدة عند التأسيس لا غير** |
| `npm run db:studio` | متصفّح بيانات رسومي |
| `npm run db:seed` | تأسيس سجل المورّدين (قابل للتكرار بلا ضرر) |

**تدقيق البيانات وإصلاحها**

| الأمر | ما يفعله |
|---|---|
| `npm run db:audit` | تدقيق اكتمال البيانات وسلامتها |
| `npm run db:repair` | إصلاح أعطال السلامة (بمعاينة أوّلاً) |
| `npm run db:dedupe` | إزالة الحركات البنكية المكرّرة |
| `npm run db:merge` | دمج مورّد مكرَّر — يُعطَّل ولا يُحذف |
| `npm run db:split-check` | كشف المورّد المسجَّل مرّتين، بدليلٍ ماليّ لا بتشابه اسم |
| `npm run db:reprice` | إعادة حساب أسعار البنود |
| `npm run db:products` | بناء أصناف المورّدين من بنود الفواتير |
| `npm run db:expenses` | اشتقاق المصروف الفعليّ من دفتر البنك |
| `npm run db:unpaid` | تشخيص المستحقّ: أيّ فاتورةٍ لها حركةٌ بمبلغها ولم تُطابَق |

**البنك والمطابقة**

| الأمر | ما يفعله |
|---|---|
| `npm run db:reclassify` | إعادة تصنيف الحركات |
| `npm run db:rematch` | إعادة المطابقة — تكتب القرار وأثره كما يكتبهما الاستيراد |
| `npm run db:learn` | تعلّم الجهات من المطابقات المؤكَّدة |
| `npm run db:link` | ربط الجهات المتعلَّمة بالمورّدين |
| `npm run db:identity` | تقرير الهويّة: بمَ عُرفت كلّ حركة |
| `npm run db:measure` | أنتحسّن أم نسوء — ويُقاس على ما حكم فيه إنسان وحده |

**الدرايف**

| الأمر | ما يفعله |
|---|---|
| `npm run drive:auth` | الحصول على مفتاح الدرايف — **قراءة فقط** |
| `npm run drive:auth -- --write` | مفتاح بصلاحية الكتابة (لإعادة التسمية وحدها) |
| `npm run drive:inventory` | جرد الأرشيف وإخراج تقرير — قراءة فقط |
| `npm run drive:backfill` | قراءة محتوى الملفّات (مرّة واحدة) |
| `npm run drive:diagnose` | تشخيص الوصول إلى الأرشيف |

**بوّابة الإنتاج — لا يُقال «جاهز» بلا دليل**

| الأمر | ما يفعله |
|---|---|
| `npm run ops:gate` | خمسة عشر بنداً: `PASS` أو `FAIL` أو **`?` لم يُفحَص** — والمجهول يمنع كما يمنع الفشل |
| `npm run ops:certify` | سبعةُ سيناريوهات ماليّة على المخطّط الحقيقيّ، كلٌّ يُلغى أثرُه |
| `npm run ops:truth` | الأصل في الدرايف مقابل القيد |
| `npm run ops:isolation` | بصمةُ القاعدة — ومعرّف العنقود هو الحاكم لا اسمُ المضيف |

**النشر**

الدفع إلى `main` في GitHub يبني وينشر على Vercel تلقائياً:

```bash
git push origin main
```

---

## إعداد جوجل خطوة بخطوة

تحتاج هذا مرة واحدة، ويستغرق نحو عشر دقائق.

### ١. أنشئ مشروعاً في Google Cloud

1. افتح [console.cloud.google.com](https://console.cloud.google.com).
2. من الشريط العلوي اضغط قائمة المشاريع ← **New Project**.
3. سمِّه `TPH Invoicing` ثم **Create**.
4. تأكّد أنه المشروع المختار في الشريط العلوي قبل أي خطوة تالية.

### ٢. فعّل واجهة الدرايف

1. من القائمة الجانبية: **APIs & Services ← Library**.
2. ابحث عن `Google Drive API` ثم افتحها واضغط **Enable**.

### ٣. اضبط شاشة الموافقة

1. **APIs & Services ← OAuth consent screen**.
2. اختر **External** ثم **Create**.
3. املأ: اسم التطبيق `فواتير ذا بوبليك هاوس` · بريد الدعم (بريدك) · بريد المطوّر (بريدك).
4. في صفحة **Scopes** اضغط **Add or Remove Scopes** وأضف:
   - `https://www.googleapis.com/auth/drive` — للقراءة والكتابة
   - `openid` · `email` · `profile` — للهوية
5. في صفحة **Test users** أضف بريدك وبريد المحاسب ومدير المشتريات.

> **مهم:** ما دام التطبيق في حالة **Testing** ينتهي مفعول الجلسة كل سبعة أيام
> ويُطلب تسجيل دخول جديد. لتفادي ذلك انشر التطبيق: **Publish App** من نفس
> الصفحة. ستظهر حينها شاشة «لم توثّق جوجل هذا التطبيق» — تتجاوزها مرة واحدة
> لكل مستخدم بالضغط على **Advanced ← Continue**. السقف مئة مستخدم، وهو أكثر
> من كافٍ لثلاثة.

### ٤. أنشئ بيانات الاعتماد

1. **APIs & Services ← Credentials ← Create Credentials ← OAuth client ID**.
2. النوع: **Web application**.
3. في **Authorized redirect URIs** أضف العنوانين معاً:
   - `http://localhost:3000/api/auth/callback/google` — لتسجيل الدخول
   - `http://127.0.0.1:53682/callback` — لأداة `drive:auth`
4. اضغط **Create** وانسخ **Client ID** و **Client Secret** إلى ملف `.env`:

```
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."
```

### ٥. احصل على مفتاح الدرايف

```bash
npm run drive:auth
```

يفتح الأمر رابطاً في المتصفح، تسجّل الدخول بالحساب المالك للأرشيف، ثم يطبع
سطراً تضيفه إلى `.env`. الصلاحية الافتراضية **قراءة فقط**.

### ٦. جرّب الجرد

```bash
npm run drive:inventory
```

يمشي على الأرشيف ويخرج تقريراً بعدد الملفات وتصنيفها وما احتاج منها تدخلاً
بشرياً، ويحفظ نسخة كاملة في مجلد `reports/`. لا يعدّل شيئاً.

---

## بنية الأرشيف في الدرايف

```
My Drive / ACCOUNTS / 2026 / 2026-08 / Olive Leaves / <الملفات>
                                     / _إيصالات السداد
                                     / _نقدي - Cash receipts
                                     / _مرافق وحكومي - Utilities & Gov
                                     / _أخرى - Other suppliers
```

معرّفات المجلدات تُقرأ من متغيرات البيئة ولا تُكتب داخل الكود.

### قواعد التسمية

```
الفاتورة : YYYY-MM-DD_<الاسم المختصر>_Invoice_<رقم الفاتورة>_SAR<المبلغ>.pdf
الكشف    : YYYY-MM-DD_<الاسم المختصر>_Statement_SAR<المبلغ>.pdf
الإيصال  : YYYY-MM-DD_Receipt_<الاسم المختصر>[-<اسم المستفيد>]_SAR<المبلغ>.pdf
النقدي   : YYYY-MM-DD_Cash_<وصف>_SAR<المبلغ>.jpg
```

- المبلغ بمنزلتين عشريتين دائماً: `SAR410.00` لا `SAR410`.
- الصورة تحتفظ بامتدادها الأصلي ولا تُحوَّل.
- عند تكرار الاسم يُضاف ` (2)` — ولا يُستبدل ملف موجود أبداً.

**انتبه:** اسم المجلد يخالف الاسم المختصر غالباً. مجلد `Olive Leaves` ملفاته
باسم `OliveLeaves`، ومجلد `Golden Cup Factory` ملفاته `GoldenCup`. ولهذا
يحمل كل مورد الاسمين منفصلين في قاعدة البيانات.

### قواعد الترتيب

1. **الشهر = تاريخ الفاتورة، لا تاريخ السداد.** فاتورة ٢٩ أغسطس تذهب إلى
   `2026-08` ولو سُدّدت في سبتمبر.
2. **إيصال السداد يتبع شهر فواتيره لا شهر تحويله.** إيصال حُوّل ٢ سبتمبر عن
   فواتير أغسطس يذهب إلى `2026-08`.
3. **الفاتورة غير المسددة التي وصلت بعد كشف موردها تُرحَّل للشهر التالي**، مع
   حفظ شهرها الأصلي في الحقل `carriedForwardFrom` ليبقى الأثر مرئياً.

### قواعد الصحة الضريبية

الفاتورة صالحة لخصم ضريبة المدخلات فقط إذا حملت الأربعة معاً:
رقم فاتورة · الرقم الضريبي للبائع · الرقم الضريبي للمشتري مطابقاً لرقم المنشأة ·
تفصيل الضريبة.

ويضيف النظام فحصاً حسابياً يكشف أخطاء الاستخراج قبل القيد: المجموع يجب أن يساوي
الصافي زائد الضريبة، والضريبة تقارب ١٥٪ من الصافي.

وأي مبلغ يتجاوز ٣٬٠٠٠ ريال يُرفع كأصل ثابت محتمل يُرسمل ولا يُصرف. والأساس هو
الصافي حين تكون الضريبة قابلة للخصم، والمجموع حين لا تكون — لأن الضريبة غير
القابلة للخصم جزء من تكلفة الأصل.

---

## بنية الكود

```
src/
  app/                     المساحات السبع وصفحاتها · و`api/` ثلاثٌ وعشرون واجهة
  components/              مكوّنات العرض · `ui.tsx` الجدول يصير بطاقات على الجوّال
  config/drive.ts          معرّفات المجلدات وثوابت المنشأة — من البيئة لا من الكود
  db/schema.ts             نموذج البيانات كاملاً (٣٩ جدولاً)
  db/index.ts              عميل قاعدة البيانات

  services/                طبقة الخدمات: المنطق هنا، والواجهات رقيقة
    guard.ts               `guard(route, capability)` — مدخل كل واجهة
    invoice · payment · document · supplier · expense · product · search
    reconcile.service.ts   الجسر: صفوف الكشف ← قرارٌ لكل حركة مع أدلّته
    adjudicator.service.ts استدعاء الحَكَم — وحكمه اقتراحٌ لا مطابقة

  lib/money.ts             المبالغ بالهللات — لا فاصلة عائمة في المال إطلاقاً
  lib/confirm.ts           الخادم يعيد اشتقاق حال الضريبة — لا يُصدَّق المتصفّح
  lib/allocation.ts        يمنع تخصيص أكثر من قيمة الدفعة
  lib/line-pricing.ts      قلب صحّة الأسعار
  lib/payment-state.ts     سبعُ حالاتٍ للدفعة — والمردودة لا تُحسَب مدفوعة
  lib/statement-match.ts   مطابقة كشوف المورّدين
  lib/month-close.ts       الإقفال: `BLOCK` يمنع، `WARN` لا يمنع
  lib/attention.ts         كل تنبيه يحمل مكان إصلاحه ودليل ظهوره
  lib/naming.ts            بناء وتفكيك أسماء الملفات
  lib/canonical-name.ts    الاسم القياسيّ من المقيَّد — ولا يُمَسّ اسمٌ يُقرأ
  lib/filing.ts            قواعد الترتيب — أي شهر يذهب إليه كل مستند
  lib/validation.ts        فحص الصحة الضريبية والمحاسبية
  lib/drive.ts             طبقة الوصول للدرايف — **لا دالة حذف فيها**

  lib/bank/                نواة البنك: من الملفّ الخام إلى قرارٍ بأدلّته
    parsers/               محوِّل لكل بنك · نصّ PDF · كشف البنك من الملفّ
    canonical · classification · entities · pattern · identity
    candidates · optimizer · decision · adjudicate
    balance-equation.ts    افتتاحي + وارد − صادر = ختامي — وهذه التسوية
    sync.ts                «أهي عندنا؟» قبل «ما معناها؟»
    lifecycle · review-queue · coverage · metrics · fees · reversal

  lib/extraction/          مخطّطٌ لكل نوع مستند · نسخ الموجِّه · قراءة الكشف
  lib/ops/                 بوّابة الإنتاج — الحكم يُشتقّ من فحوصٍ تجري
  lib/sales/connector.ts   واجهة المبيعات — معرَّفة بلا تطبيق، **ترمي ولا تُصفّر**

scripts/                   ستّةٌ وثلاثون نصّاً — الجدول أعلاه يصفها
drizzle/sql/               خمسٌ وعشرون هجرة بـSQL صريح، يشغّلها `db:migrate`
```

### قرارات هندسية تستحق الشرح

**المبالغ أعداد صحيحة بالهللات.** لأن `0.1 + 0.2` في جافاسكربت لا تساوي `0.3`،
وخطأ «فاتورة بـ٣٬٤٠٠ قُيّدت ١٬٧٠٠» لا يجوز أن يتكرر من جهة النظام.

**سجل التدقيق غير قابل للتعديل ولا الحذف على مستوى قاعدة البيانات نفسها.**
ثلاثة مشغّلات ترفض `UPDATE` و `DELETE` و `TRUNCATE`. الاتفاق البرمجي وحده لا
يكفي لسجل يُفترض أن يصمد أمام المراجعة.

**مخرجات نموذج الاستخراج تُحفظ خاماً قبل أي تصحيح يدوي** في حقل
`extractionJson`، وثقة كل حقل على حدة في `fieldConfidence`. فما عُدّل يدوياً
يبقى قابلاً للمقارنة بما استُخرج آلياً.

---

## الواجهة والخط

الخط **ثمانية سانس** للنصّ و**ثمانية سيرف ديسبلاي** للعناوين، محمَّلان ذاتياً من
مجلد `src/fonts` بصيغة woff2 — فلا يعتمد التطبيق على خادم خطوط خارجي ولا يتأخّر
عرض النص العربي.

النظام اللوني أبيض وأسود خالصان بأعلى تباين، والرمادي للنص الثانوي والحدود فقط.
واللون محجوز للحالات وحدها — أخضر وكهرماني وأحمر — حتى يبقى التنبيه ملحوظاً
لأنه الملوّن الوحيد في الشاشة. والوضع الليلي يتبع إعداد الجهاز تلقائياً.

الواجهة مبنية للجوال أولاً، ومعها ملف `manifest.webmanifest` يجعلها تُضاف إلى
الشاشة الرئيسية وتفتح بملء الشاشة بلا شريط متصفح.

## النشر

التطبيق منشور على Vercel، ومتغيّرات البيئة مضبوطة على المشروع هناك لا في الكود.
لنشر نسخة جديدة بعد أي تعديل:

```bash
vercel deploy --prod
```

> **تنبيه على الخطة:** الحساب الآن على خطة Vercel المجانية، وشروطها للاستخدام
> الشخصي غير التجاري. قبل أن يصير النظام أداة العمل اليومية تحتاج الترقية إلى
> خطة Pro بعشرين دولاراً شهرياً، أو النقل إلى Firebase App Hosting كما في الخطة
> الأصلية — والنقل سهل لأن التطبيق لا يعتمد على شيء خاص بـVercel.

## الأمان

- لا مفتاح ولا سرّ داخل الكود. كل شيء في `.env`، وهو مستثنى من git.
- `.env.example` يحمل أسماء المتغيرات فقط بلا قيم حقيقية.
- أدوات المرحلة صفر بصلاحية قراءة فقط من الدرايف.
- الصلاحيات تُفرض على الخادم لا بإخفاء عناصر الواجهة: مدير المشتريات لا يصل
  إلى الأرقام المالية ولو استدعى الواجهة البرمجية مباشرة.
