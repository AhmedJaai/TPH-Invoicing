@AGENTS.md

# TPH-Invoicing — الذاكرة الدائمة

نظام مالي وتشغيلي لمقهى **مؤسسة ذا بوبليك هاوس** (النسيم، جدة).
الرقم الضريبي `310007971600003` · السجل `7052766941`.

الشرح الطويل في **`docs/ARCHITECTURE.md`** — وهو مصدر الحقيقة للمعمارية.
هذا الملف يحمل ما تحتاجه *قبل* أن تكتب سطراً: القرارات وأسبابها، والمصائد.
`docs/HANDOVER.md` و`docs/PLAN.md` **وثيقتان مؤرّختان** — تصفان ما كان، لا ما هو قائم.

## قيود لا تُخترق

1. **لا يُمسّ أرشيف جوجل درايف.** لا حذف ولا نقل ولا إعادة تسمية بلا طلب صريح من أحمد.
2. **العربية** في كل ما يُكتب له: فصحى سهلة.
3. **المال عدد صحيح بالهللات** (`HALALAS_PER_RIYAL = 100` في `src/lib/money.ts`). لا عدد عشري في أي حساب مالي، أبداً.
4. **المجهول ليس صفراً.** ما لم يُقرأ يُعرض «غير معروف»، لا `0`. صفرٌ كاذب يقول إنّ المقهى لم يشترِ شيئاً.
5. **لا بيانات مبيعات مخترَعة.** لا Foodics بعد؛ `notConnected` في `src/lib/sales/connector.ts` **يرمي** `NotConnectedError` ولا يُرجع أصفاراً.

## البنية التقنية

Next.js **16.3.4** (App Router) · React 19.2.8 · TypeScript 5 صارم · Tailwind v4
Drizzle ORM 0.45.2 + `pg` 8.23 على **Neon Postgres**
Auth.js v5 (`next-auth@5.0.0-beta.32`) + `@auth/drizzle-adapter` — جوجل، جلسات في القاعدة
Vitest 4.1.11 · zod 4.5.4 · googleapis 178 · `@anthropic-ai/sdk` 0.123

**القاعدة:** يجب استعمال نقطة Neon **المجمَّعة** (`...-pooler.c-2.us-east-2.aws.neon.tech`).
غير المجمَّعة تنفد اتصالاتها تحت Vercel.

**النشر:** Vercel `tph4/tph-invoicing` · GitHub `AhmedJaai/TPH-Invoicing` · الفرع `main`.

## القاعدة — ٣٩ جدولاً

`users` `accounts` `sessions` `verification_tokens` · `documents` `invoices` `invoice_lines` `issues`
`suppliers` `supplier_aliases` `supplier_products` · `payments` `payment_allocations`
`bank_imports` `bank_transactions` `bank_rules` · `statements` `statement_lines` · `month_closes`
`products` `recurring_expenses` · `sales` `sale_lines` `sales_sources` `pos_products` (فارغة عمداً)
`audit_logs` `rate_limits` `expenses`
`counterparties` `counterparty_evidence` · `branches` `bank_accounts` `reconciliation_periods`
`sale_payments` `refunds` `refund_lines` `settlement_batches`
`adjudications` `decision_history`

التعريف في `src/db/schema.ts`.

### الهجرات: SQL صريح، لا `drizzle-kit push`

الملفات في `drizzle/sql/` ويشغّلها `npm run db:migrate` (`scripts/migrate.ts`).

**السبب:** `drizzle-kit push` يطلب طرفيّة تفاعلية — فيتعذّر في CI — ولا يترك أثراً يُراجَع.

| الملف | ماذا يفعل |
|---|---|
| `001_audit_log_immutable.sql` | مؤثِّرات تمنع تعديل سجلّ التدقيق أو حذفه أو تفريغه |
| `002_tax_status_and_idempotency.sql` | حالات الضريبة، ومفاتيح منع التكرار الثلاثة |
| `003_transaction_type.sql` | `bank_transactions.transaction_type` |
| `004_rate_limits.sql` | جدول حدّ الطلبات |
| `005_products_and_sales_domain.sql` | الأصناف والمصروفات المتكرّرة ومجال المبيعات |
| `006_expenses.sql` | المصروف الفعلي، وقيدٌ يمنع تكراره عن حركة أو فاتورة |
| `007_financial_invariants.sql` | قيود المال في القاعدة: التخصيص والفاتورة والمصروف |
| `008_bank_kinds.sql` | أبواب حركة البنك: تسوية الشبكة ورسومها وضريبتها |
| `009_match_evidence.sql` | أدلّة المطابقة وقرارها ودرجتها — كي يُعرَض «لماذا؟» ويُتراجَع |
| `010_counterparties.sql` | ذاكرة المستفيدين: هويّة الجهة وأدلّتها |
| `011_branches_and_accounts.sql` | الفروع والحسابات وفترات التسوية |
| `012_sales_domain.sql` | طرق دفع البيعة والمرتجعات ودفعات التسوية |
| `013_decision_provenance.sql` | أثر القرار: من صنّف، وأيّ نموذجٍ حكَم، وكيف تطوّر |
| `014_identity_scoping.sql` | فرادة الحركة مقيَّدة بحسابها، وفرادة الدليل بحسب نوعه |
| `015_payment_lifecycle.sql` | سبعُ حالاتٍ للدفعة، ورسمُ التحويل، والردّ لا يحذف |
| `016_lifecycle_and_expense_events.sql` | طبقات الحركة، وبصمةُ حدث المصروف |
| `017_sales_identity_and_units.sql` | فرعُ البيعة وهويّتها، ومحتوى عبوة الصنف |
| `018_statement_balances_nullable.sql` | رصيدا الكشف يقبلان المجهول — والصفر كان يقول «لا يطالبنا بشيء» |
| `019_pattern_identity.sql` | النمط دليلٌ على الجهة — ظنّيٌّ كالاسم، وبه تُعرَف الحركة التي لا اسم لها ولا حساب |
| `020_natural_transaction_key.sql` | المنع على المفتاح الطبيعيّ لا على بصمةٍ تُحسَب — و`occurrence` جزءٌ منه |
| `021_amount_classification.sql` | `AMOUNT` مصدرَ تصنيف — المقدار دلّ حين صمت الوصف |
| `022_bank_vat_category.sql` | `BANK_VAT` — ضريبةُ الرسم بابٌ غير باب الرسم |
| `023_operation_reference.sql` | مرجع العمليّة فريد — من تطابق مرجعُه تطابقت عمليّته |
| `024_identity_key.sql` | الهويّة تُخزَّن وتُقيَّد: `REF` بالمرجع أو `FACT` بالوقائع |
| `025_identity_supersedes_natural.sql` | قيدٌ واحد للهويّة — لا قيدان بتوحيدين مختلفين |

## المكتبات التي يجب معرفتها

| الملف | مسؤوليّته |
|---|---|
| `src/lib/confirm.ts` | `reviewConfirmed()` — **الخادم** يعيد اشتقاق حال الضريبة. لا يُصدَّق المتصفّح في رقم مالي. |
| `src/lib/line-pricing.ts` | `resolveLinePricing()` / `reconcileInvoiceLines()` — قلب صحّة الأسعار |
| `src/lib/allocation.ts` | `planAllocations()` يمنع تخصيص أكثر من الدفعة · `settleSupplierAccount()` سياسة الأقدم أوّلاً |
| `src/lib/bank/identity.ts` | المفتاح الطبيعيّ للحركة وبصمتها — و`unseenRows` تُدخِل الفرق لا الملف |
| `src/lib/bank/pattern.ts` | هويّات الحركة مرتَّبةً، والنمط آخرها — وبها يُجمَع المتشابه |
| `src/lib/bank/sync.ts` | **أهي عندنا؟** مرجعُ العمليّة ثمّ الوقائع — قبل أن يُسأل ما معناها |
| `src/lib/bank/rules.ts` | تصنيف الحركات. **GOVERNMENT قبل ZAKAT** وإلّا قُرئت «زاتكا» صدقةً |
| `src/lib/bank/canonical.ts` | نموذج الحركة المعياريّ — المرجع يُنسب إلى نوعه بدليل |
| `src/lib/bank/classification.ts` | تصنيف بطبقات: البنية ← المتعلَّم ← الكلمات ← المجهول يُعلَن |
| `src/lib/bank/entities.ts` | تعريف المستفيد بأدلّة قاطعة وظنّية — والظنّيّ وحده لا يكفي |
| `src/lib/bank/candidates.ts` | توليد المرشّحين وتسجيلهم · مجموع الجزئيات بلا سقف ثلاث |
| `src/lib/bank/optimizer.ts` | تفريعٌ وتحديد: أعلى **مجموع** لا أعلى درجة. وميزانيّة عقد، فإن نفدت رجع للجشع وأعلن `exact: false` |
| `src/lib/bank/coverage.ts` | فجوات التغطية — الغائب لا يُرى فيجب أن يُحسَب |
| `src/lib/bank/balance-equation.ts` | **افتتاحي + وارد − صادر = ختامي.** وهذه التسوية؛ وعدُّ المطابقات ليس تسوية |
| `src/lib/bank/lifecycle.ts` | طبقات الحركة: خام ← مُستنتَجة ← مقترَحة ← مُقَرَّة ← مُقيَّدة · **`detectAnomalies` لا يستدعيها شيء — الأعطاب تُحسَب ولا تُعرَض** |
| `src/lib/bank/review-queue.ts` | ثلاثة أعمالٍ لا عملٌ واحد: يُقَرّ · يُراجَع · يُحسَم |
| `src/lib/bank/evidence-uniqueness.ts` | القاطع يُحتكَر ويُعلَن تضاربه، والظنّيّ يُشترَك — والاسم ليس هويّة |
| `src/lib/bank/supplier-profile.ts` | عادةُ سداد المورّد — ترجّح ولا تحسم، ولا تُبنى على أقلّ من خمس سابقات |
| `src/lib/bank/vision-statement.ts` | الكشف المصوَّر: كلّ مبلغٍ يُفحَص، والمعادلةُ تردّ الكشف كلّه إن اختلّت |
| `src/lib/bank/metrics.ts` | أنتحسّن أم نسوء — ويُحسَب على ما فعله الإنسان وحده |
| `src/lib/bank/parsers/adapters.ts` | محوِّل لكل بنك، و`verified` يُعرَض للمستخدم لا يُدفَن في تعليق |
| `src/lib/payment-state.ts` | سبعُ حالاتٍ للدفعة — والمردودة لا تُحسَب مدفوعة |
| `src/lib/unit-conversion.ts` | لا جسر بين وزنٍ وحجم، والمجهول لا يُحوَّل · **لا تصل إليها شاشة** |
| `src/lib/bank/fees.ts` | الرسم البنكيّ داخل الدفعة — والنقص سدادٌ جزئيّ لا رسم |
| `src/lib/bank/reversal.ts` | ما خرج ثمّ عاد — لا إيراد ولا تحويل داخليّ · **لا تصل إليها شاشة** |
| `src/lib/credit-notes.ts` | الإشعار الدائن يخفض المستحقّ ولا يُعدّ سداداً · **لا تصل إليها شاشة** |
| `src/services/adjudicator.service.ts` | استدعاء الحَكَم — وحكمه اقتراحٌ لا مطابقة |
| `src/lib/bank/adjudicate.ts` | متى يُستدعى الذكاء: عند العجز وحده، وعلى مرشّحين مولَّدين لا بيانات خام |
| `src/lib/bank/parsers/detect.ts` | كشف البنك من الملفّ — و`\b` لا تفصل عند `_` وأسماء الملفّات مليئة بها |
| `src/lib/bank/parsers/pdf-text.ts` | نصّ PDF ومواضعه — ولا يُرمى ملفٌّ كامل إلى نموذج |
| `src/lib/extraction/schemas-by-kind.ts` | مخطّط لكل نوع مستند — لا ثلاثون حقلاً لفاتورةٍ فيها ستّة |
| `src/lib/extraction/benchmark.ts` | مقياس النماذج — **الخطأ الواثق** يسبق الدقّة · **لا تصل إليها شاشة** |
| `src/lib/extraction/versions.ts` | نسخ الموجِّه والمخطّط، والنماذج مثبَّتة لا عائمة |
| `src/services/counterparty.service.ts` | تأكيدٌ واحد يعمّ على أمثاله |
| `src/lib/bank/decision.ts` | تلقائيّ/اقتراح/مراجعة بقاعدة الهامش |
| `src/services/reconcile.service.ts` | الجسر: صفوف الكشف ← قرارٌ لكل حركة مع أدلّته |
| `src/lib/bank/match.ts` | ربط الحركة بمورّد — حدود الكلمات، لا احتواء نصّي |
| `src/lib/statement-match.ts` | مطابقة كشوف المورّدين |
| `src/lib/month-close.ts` | الإقفال: `BLOCK` يمنع، `WARN` لا يمنع |
| `src/lib/attention.ts` | كل تنبيه يحمل `href` (مكان الإصلاح) و`evidence` (لِمَ ظهر) |
| `src/lib/data-health.ts` | التغطية لا تُختلق؛ فيها حال `NOT_CONNECTED` |
| `src/lib/expenses.ts` | المصروف الفعلي. **سداد المورّد ليس مصروفاً** — محسوبٌ في المشتريات. وازدواجُ الحدث يُكشَف ولا يُحذَف |
| `src/lib/provenance.ts` | «من أين جاء هذا الرقم» — التغطية بالعدد لا بالمبلغ |
| `src/lib/arabic.ts` | تمييز العدد — القاعدة تدور عند المئة فلا تُكتب بالحدس |
| `src/components/ui.tsx` | البطاقة والجدول والحالات. **الجدول يصير بطاقات على الجوّال** |
| `src/lib/preview-mode.ts` | وضع المعاينة — لا يعمل في الإنتاج مهما فُعِّل المتغيّر |
| `src/lib/nav.ts` | المساحات وأقسامها. الموضع يُشتقّ من المسار لا من خاصيّة تُكتب باليد فتُنسى |
| `src/services/guard.ts` | `guard(route, capability)` + `respondTo(e)` → 401/403/429. **مدخل كل واجهة** |

## القرارات وأسبابها

- **الخادم لا يثق بالمتصفّح.** كان `/api/archive` يحفظ `isTaxValid` كما أرسله المتصفّح. صار `reviewConfirmed()` يشتقّه من جديد.
- **الفشل يُسمَع.** كانت الفاتورة التي يتعذّر قراءة إجماليها تُؤرشَف بصمت وتُعرض «نجحت». صارت تُرفض بـ409.
- **الاستيراد منيع من التكرار** بثلاث طبقات: بصمة الملف، ومعرّف الحركة الخارجي، وبصمة المستند (`documents_sha_uniq` جزئي `WHERE status <> 'REJECTED'`). السبب: كشف بنكي استُورد ثلاث مرّات — ١٤٢٨ حركة صارت ٤٢٨٤.
- **بصمة الحركة مفهرسة بالتكرار** (`row_number()`) كي تنجو الأسطر المتكرّرة فعلاً داخل الملف نفسه.
- **الصنف يُفتَح بمفتاح `${supplierId}::${اسم}`** لا بالاسم وحده. السبب: «العنب» من لافا كمبوتشا، ومن محمصة الغربية كيلو بنّ — فأنتج دمجهما «توفيراً» وهمياً بـ٢١٢٢ ريالاً.
- **فترة كشف المورّد تُشتقّ من تواريخ أسطره** لا من اسم الملف. السبب: كشف Olive Leaves تراكمي (مايو–أغسطس) فأنتج ٣٦ فاتورة «مفقودة» كذباً.
- **نافذة مطابقة الفاتورة ±٧ أيام** — سطر بتاريخ ٢٣ يخصّ فاتورة بتاريخ ٢٦.
- **المورّد المكرَّر يُدمَج ويُعطَّل، ولا يُحذف** (`npm run db:merge`).
- **الذكاء الاصطناعي لا يخترع رقم فاتورة.** اختلق النموذج `TPH-20260521` بثقة ١٫٠٠. صار محظوراً في الموجِّه، ولا يُؤخذ رقم من النموذج لمستندات الأرشيف.
- **المصروف الفعلي لا يشمل سداد المورّد** — وإلّا حُسب مرّتين. وما يقول وصفه «شراء بضاعة» يُستبعَد ولو صنّفته القاعدة راتباً؛ ولا يُصحَّح التصنيف آلياً، بل يُعرَض التناقض.
- **عرض الصفحة يتبع ما فيها** — `form` للنماذج و`page` للعادي و`wide` للوحات والجداول. كان الكلّ `max-w-5xl`.
- **الثوابت المالية تُفرَض في القاعدة لا في الكود وحده** — `npm run db:verify` يثبت أنّها ترفض فعلاً. والقيد بلا اختبار ادّعاء.
- **يُتسامح بريالٍ في فرق (صافي+ضريبة) عن الإجمالي** — المورّد يُسقط كسور الريال والمطبوع هو الملزِم. القيمة في `TOTAL_ROUNDING_TOLERANCE_MINOR`، وهي نفسها في القاعدة.
- **الكتابة لا تُحرَس بصلاحية قراءة** — `bank:edit` و`expense:edit` منفصلتان عن `bank:view` و`amounts:view`.
- **محتوى المستند بيانات لا تعليمات** — الموجِّه يعلنها صراحةً في أوّله.
- **المرجع في تسجيل المطابقة مؤيِّد لا نافٍ** — أوصاف الأهلي تحمل مراجعه هو لا أرقام فواتير المورّدين، فعدم تطابقها كان يسقف كل مطابقة عند ٠٫٨٣ فتستحيل التلقائية بحكم المعايرة لا بحكم الشكّ.
- **لا مطابقة تلقائية إلّا بشرطين**: الدرجة فوق الحدّ **والفارق** عن المرشّح التالي فوق حدّه. أثبِت المطابقة، لا تجدها.
- **عمود المستفيد يُقرأ من الكشف** — كان الاستيراد يكتب فيه اسم المورّد المطابَق، فإن لم تُطابَق الحركة بقي فارغاً: دائرةٌ مغلقة على نفسها.
- **الأدلّة تُحفَظ بنصّها** مع القرار والدرجة — رقمُ الثقة الثابت لا يقول شيئاً بعد شهر.
- **كل مطابقة قابلة للتراجع** — `‎/api/match-undo` يفكّ التخصيصات ويحذف الدفعة إن لم تعد تفسّر حركة، ويكتب ذلك في سجلّ التدقيق.
- **مصدر قرارٍ واحد.** خرج `matchBankTransactions` من مسار الاستيراد نهائياً. كان يعمل بجانب المحرّك الجديد: هذا يكتب المال وذاك يكتب أدلّةً فوقه — فتُحفَظ حركةٌ بدرجة ٩١ ونتيجةِ «فاتورة بعينها» وحالتُها `UNMATCHED`. والاحتياط `engine ?? old` يعني مصدرَي حقيقة، وهو ما يُنتج التناقض لا يُصلحه.
- **الكتابة من `planned` وحده** — والاقتراح لا يُكتَب، ينتظر تأكيداً. والمعاينة تعرض ما سيُكتَب فعلاً.
- **شهر الدفعة هو الأحدث بين فواتيرها** لا شهر أوّلها، والشهور كلّها تُحفَظ.
- **الكشوف تستعمل نواة البنك نفسها** — `reconcile()` واحدة، ولا مطابقان مختلفان لعملٍ واحد. وحالات السطر صارت ثمانياً: مكرَّر، وتنازع مرجع، وإشعار دائن، واختلاف تاريخ…
- **حكم الحَكَم اقتراحٌ لا مطابقة.** النموذج يرجّح بين مرشّحين حُسبوا، ولا يقرّر مالاً. ولا يخترع مرشّحاً: يختار من قائمةٍ بمعرّفات مختصرة، ويُتحقَّق من حكمه في الخادم.
- **الحلّ التقريبيّ لا يُطابَق تلقائياً** — حين تنفد ميزانيّة المحسِّن يصير التلقائيّ اقتراحاً، فقولُ «تمّت المطابقة» عن حلٍّ لم يُثبَت أنّه الأفضل ادّعاء.
- **`beneficiary_raw` في الصفوف القديمة ملوَّث** — كان يحمل اسم المورّد المطابَق لا المستفيد. فلا يُبنى عليه اسمُ جهة؛ يُؤخذ من الوصف قبل `BEN ID`.
- **المورّد المسجَّل مرّتين يمنع المطابقة** — دفعةٌ لـ«سرد كو» وفاتورةٌ لـ«سرد للتجارة». يكشفه `npm run db:split-check` بدليلٍ ماليّ لا بتشابه اسم.
- **`as` تُسكت المترجم ولا تُصلح اختلافاً.** ذاكرة المستفيدين كانت تُسنِد `TxCategory` إلى `TxKind` بـ`as`، فخرجت كل جهةٍ متعلَّمة من مطابقة الفواتير **صامتةً** — والتعلّم يُنقص المطابقات بدل أن يزيدها. الترجمة صريحة الآن في `apply.ts`، ولها اختبارٌ يمرّ على كل باب وكل نوع.
- **كل ما يُحتمَل أنّه سداد يحمل حالةَ قرار** — وإلّا قالت الخلاصة «٨٥ تحتاج مراجعة» ولم يجدها الطابور.
- **قواعد البنك تعمّ ولا تخصّ عمليةً واحدة** — كانت ٣٢ قاعدة تحمل كلٌّ مرجع حوالة بعينها.
- **فرادة الحركة مقيَّدة بحسابها** — بصمةٌ فريدة في النظام كلّه تجعل حوالتين متطابقتين من حسابين تبتلع أولاهما الأخرى. والنطاق في القيد لا في البصمة، و`coalesce(bank_account_id,'~')` مقصود: `NULL <> NULL` يفتح باب التكرار بدل أن يسدّه.
- **تمثيلٌ واحد للهوية يُعدّ به ويُبصَم به** — كان العدّ بالوصف الخام والبصمة بالموحَّد، فحركتان يفرّقهما فراغٌ مزدوج تُبتلَع إحداهما.
- **الاسم ليس هويّة** — القاطع (حساب · آيبان · هوية · تاجر) يُحتكَر ويُعلَن تضاربه، والظنّيّ (اسم · مرجع) يُشترَك؛ وإن اشتُرك سقط من الذاكرة لأنّه لم يعد يدلّ على واحدة.
- **التسوية معادلةٌ لا عدُّ مطابقات** — «طوبقت ٣٠١ حركة» قد تكون مطابقةً تامّة على كشفٍ ناقص. والفجوة والفرق مانعا إقفال.
- **المال الداخل ليس سداداً** — كانت الحركة الواردة تمرّ إلى المطابقة، فيُنشَأ سدادٌ من مالٍ دخل وتُقفَل فاتورةٌ لم تُدفَع. كشفته مصفوفة التسوية.
- **الزمن ذو اتّجاه** — تصل الفاتورة ثمّ تُدفَع. والفاتورة بعد الدفعة نافذتُها سبعة أيام بنصف درجة: ممكنةٌ لا مرجَّحة.
- **بركة البحث تُرتَّب بالصلة لا بالحجم** — والقصّ ليس خطأً؛ الخطأ أن يقصّ بمعيارٍ لا صلة له بالسؤال. وما جاوز الدفعة يخرج: حذفُ المستحيل لا تقريب.
- **الرسم يخرج من القسمة قبلها** — وإلّا نُسب إلى المورّد مالٌ ذهب إلى البنك، وظهرت الدفعة «فائضة» فرُفعت إلى المراجعة بلا سبب.
- **الردّ لا يحذف** — الدفعة المردودة تبقى `REVERSED` بسببها ومن ردّها. والحذف يجعل الفاتورة تعود مستحقّةً بلا سببٍ ظاهر فتُدفَع مرّتين.
- **الإقرار الجماعيّ يُعيد الحساب** — الخادم لا يأخذ من المتصفّح إلّا معرّفات الحركات، ويحسبها مجتمعةً فلا تُحجَز فاتورةٌ لاثنتين.
- **ما قُرئ بصرياً لا يُحسَم تلقائياً** — كالحلّ التقريبيّ: حسابٌ صحيح على مدخلٍ غير مثبت.
- **حدود القراءة تُعرَض دائماً لا عند الخطأ** — محوِّلُ بنكٍ لم يُجرَّب حدٌّ معلوم سلفاً لا خطأٌ وقع.
- **المقياس لا يقيس نفسه بنفسه** — الدقّة تُحسَب على ما حكم فيه إنسان وحده، والاستدعاء يُعلَن أنّه لا يُقاس.
- **قيدٌ يحذف بيانات مالية يُفحَص أثرُه على الإنتاج قبل كتابته.** قيدُ ازدواج المصروف في `016` كُتب ثمّ **سُحب**: فُحص فوجِد أنّه سيحذف ثلاثة مصروفات حقيقية — فاتورتَي كهرباء وفاتورةَ اتصالات في يومٍ واحد بمبالغ متطابقة، لا يفرّقها إلّا مرجعُ السداد الواقع بعد الحرف الستّين في وصف الأهليّ. والأعمق: حدثان لهما **أثران مختلفان** (حركتان بنكيّتان ببصمتين) حدثان، مهما تطابق ما عداهما. فصار الازدواج كشفاً يُعرَض لا قيداً يحذف.
- **البصمة تُلبَّد ولا تُقتطَع** — القصّ لتقصير المفتاح يُسقط ما يميّز.
- **«لم يُفحَص» يمنع الجاهزية كما يمنعها «فشل»** — بوّابةٌ تعدّ غير المفحوص ناجحاً تُنتج ثقةً بلا سند، وهي أخطر من ألّا تكون هناك بوّابة.
- **العزل يُثبَت بمعرّف العنقود لا باسم المضيف** — لنقطة Neon الواحدة أسماءٌ مجمَّعة وغيرُ مجمَّعة، فمضيفان مختلفان قد يشيران إلى قاعدةٍ واحدة. و`/api/ops/db-identity` يُفتَح في البيئتين ويُقارَن.
- **الشهادة تُلغي أثرها** — كلّ سيناريو ماليّ يجري في معاملةٍ تُلغى، فيُختبَر المسار الحقيقيّ على المخطّط الحقيقيّ بلا أن يُمَسّ ريال. ولو كُتب ثمّ حُذف لبقي أثرٌ في سجلّ التدقيق، وهو جدولٌ لا يُحذَف منه شيء.
- **الاختبارات النقيّة لا تُثبت أنّ النظام يعمل.** ١٢٠١ اختباراً تمرّ في أقلّ من ثانية — لأنّ ولا واحدٍ منها يلمس قاعدةً أو صفحة. وقد مرّت كلّها بينما أحد عشر كشفاً بلا سطر، و`classification_source` فارغ في ١٤٢٢ حركة، و`decision_history` فارغ تماماً. **الأخضر يقول إنّ القطع سليمة، لا إنّها موصولة.**
- **الشيفرة تسبق البيانات فتفترق عنها.** آخر استيراد بيانات حقيقية كان ٤ سبتمبر ٢٠٢٦، وبعده ٤٢ كوميتاً من أصل ٧٢. فكلّ ما بُني بعده لم يمرّ على ريالٍ واحد: لذلك كانت جداول `decision_history` و`adjudications` و`bank_accounts` و`reconciliation_periods` و`statement_lines` فارغة كلّها. **من يبني طبقةً جديدة يُعيد تشغيلها على البيانات القائمة في الكوميت نفسه، وإلّا فهي دعوى.**
- **نصوص الإصلاح تحفظ ما يحفظه مسار الاستيراد — كاملاً.** كان `db:rematch` يكتب الأدلّة والقرار ويُسقط `classification_source` و`reason` و`version` و`lifecycle`، ولا يكتب `decision_history`. فبقيت هويّة التصنيف فارغةً في ١٤٢٢ حركة رغم أنّ الاستيراد يكتبها — لأنّ الذي صنّف فعلاً هو النصّ لا الاستيراد. **نصفُ كتابةٍ أسوأ من لا كتابة: تُظهر الحقل موجوداً وفارغاً فيُقرأ «لم يُصنَّف».**
- **الكشف يُقيَّد ولو لم يُقرأ رصيده** — الفاتورة بلا مبلغٍ لا معنى لها، أمّا الكشف فهويّته مورّدُه وفترتُه. وكان يُشترَط له مبلغٌ في الاسم، فسقطت ثلاثة كشوف مؤرشفة من الجدول تماماً وظهر «أوراق الزيتون» في «لم يصل كشفه» وقد أرسله مرّتين.
- **أسطر الكشف تُحفَظ عند الأرشفة لا عند المطابقة** — كان النموذج يستخرجها ثمّ تُرمى، ويُحفَظ الرصيد الختاميّ وحده. فبقيت `statement_lines` فارغةً في أحد عشر كشفاً، والمطابقة مستحيلةٌ لأنّ ما يُطابَق غير موجود — ثمّ تقول البوّابة «لم يُطابَق منها واحد» كأنّ التقصير من صاحب المقهى.
- **رصيد الكشف المجهول ليس صفراً** — كان العمودان `NOT NULL DEFAULT 0`، فكشفٌ لم يُقرأ رصيدُه يُحفَظ «لا يطالبنا بشيء»، ثمّ تُحسَب المعادلة على ذلك الصفر فيُقال «حساب المورّد لا يستقيم» — ويُتَّهم المورّد بخطأٍ مصدره أنّنا لم نقرأ. صارا يقبلان `NULL` في `018`.
- **الشهر الجاري يُقارَن بمثله** — كانت الصفحة تجمع سبتمبر حتّى اليوم وتقارنه بأغسطس كاملاً، فتقول في السادس من كلّ شهر «▼ ٩٨٪ أنفقتَ أقلّ». والنقص يومٌ لا سلوك؛ والإنذار الكاذب الدوريّ يُفقد الثقة بما عداه. فيُقصّ السابق عند اليوم نفسه ويُعلَن الأساس: «عن أوّل ٦ أيّامٍ من أغسطس».
- **عدد الهجرات يُقرأ من المجلّد لا يُكتَب رقماً** — كان `EXPECTED_MIGRATIONS = 17` في البوّابة، فأوّل هجرةٍ تُضاف تقول «١٨ من ١٧ — فشل»: تُعاقِب على التقدّم ثمّ يُسكِتها من يزيد الرقم بيده، فتصير رقماً يُجامَل لا فحصاً يجري. والمقارنة بالأسماء تكشف الاتّجاهين — ومنهما هجرةٌ طُبّقت وملفّها غائب، وهي الأخطر.
- **الهويّة تُمسَك من النمط حين لا اسم ولا حساب.** كانت الذاكرة تُمسك الحركة من اسم المستفيد أو رقم حسابه، وحركاتُ أحمد المجهولة الخمس والثمانون **كلّها** بلا اسمٍ وبلا حساب: سدادٌ حكوميّ عبر «سداد»، ورسومُ شبكة، وأوصافٌ فارغة. فالمفتاح يخرج `null` لكلّها، و«يؤكّد مرّةً فيعمّ» لم يكن يعمّ على شيءٍ ممّا يحتاجه فعلاً. فصار الوصفُ بعد إسقاط أرقامه دليلاً — ظنّيّاً كالاسم: إن اشترك فيه اثنان سقط.
- **الاسم يُقرأ بالصيغة التي يُكتب بها.** كان يُحفَظ موحَّداً (`normalizeText`) ويُقرأ خاماً في `merchantKey`، فاسمٌ فيه همزةٌ أو تاء مربوطة لا يلتقي بنفسه — وهو أكثر الأسماء العربية. والتوحيد الآن في `identitiesOf` وحدها، يكتب بها المؤكِّد ويقرأ بها المصنِّف.
- **السؤال يُطرَح على المجموعة لا على الحركة.** كشفٌ فيه ستّون حركة وخمسَ عشرة منها لمورّدٍ واحد كان يعني خمسة عشر سؤالاً عن شيءٍ واحد — فيُترَك الطابور. وقياساً على معلَّقات أحمد: ١٥٣ حركة صارت ٤٧ سؤالاً. والخادم يُعيد اشتقاق المجموعة ولا يصدّق المتصفّح في أنّها متشابهة.
- **التأكيد يعمّ على القاعدة كلّها لا على الكشف القادم.** بعد الحفظ تُقرأ الذاكرة من جديد ويمرّ **المصنِّف نفسه** الذي يمرّ عليه الاستيراد على ما بقي. ولا يُمَسّ ما صنّفه إنسان.
- **`db:reclassify` كان يمحو التعلّم.** يستدعي `classify` بلا ذاكرة، فيُلغي في كل تشغيل كلَّ ما أكّده صاحب العمل — والمسارُ الذي يُفترَض أن ينشر التعلّم كان يُبطله. ويكتب `category` وحده تاركاً `classification_source` يقول شيئاً غير ما وقع.
- **تغييرُ دالّة الهوية يُبطل بأثرٍ رجعيّ كلَّ ما حُفظ بها.** خرج رقم الحساب من `transactionIdentity`، فصار للحركة الواحدة بصمتان — واحدة قبل التغيير وأخرى بعده — فلم يرَ الاستيراد الثاني الأوّل، ودخل كشفٌ كامل مرّة ثانية: ١٤١٣ حركة، ٩١٢٬٧١١ ريالاً. **والمنع صار على المفتاح الطبيعيّ نفسه** (حساب · تاريخ · مبلغ · اتجاه · وصف موحَّد · ترتيب) لا على بصمةٍ تُشتقّ منه: المفتاح الطبيعيّ ليس محسوباً فلا «نسخة» له تتغيّر. ويُثبته `db:verify` بمحاولة إدخالٍ تُرَدّ.
- **العدد الحقّ لحركةٍ هو أكثر ما قاله ملفٌّ واحد** — لا واحد، ولا مجموع الملفّين. فالكشف يذكر الحركة بعدد ما وقعت؛ فإن قال كشفان «مرّتين» فهي مرّتان لا أربع، وإن قال أحدهما «مرّة» فذاك نقصُ تغطيةٍ لا نفيُ وقوع. وبه يُحذف ما تكرّر بالاستيراد ويبقى ما تكرّر في الواقع — و**في كشف أحمد ٢٣ مجموعةً متكرّرة حقيقيّة**، لولا هذا التمييز لابتُلعت.
- **والحذف يُسبَق بدمج** — النسختان ليستا متطابقتين: الباقي (الأقدم) بلا «نوع العملية» في ١٤٣٢ حركة، والمحذوف يحمله. فحذفٌ بلا دمج يشتري إزالة التكرار بثمنِ حقلٍ لا يُستعاد إلّا بإعادة استيراد الكشف.
- **`occurrence` جزءٌ من المفتاح فلا تبقى فيه ثغرة** — صفٌّ باقٍ ترتيبُه ١ وقد حُذف صاحبُ الصفر يعني أنّ استيراداً قادماً يكتب الصفر فلا يصطدم بشيء، فتعود الحركة مرّتين. فالترقيم يُعاد بعد كل حذف، على مرحلتين لأنّ الفهرس فريد.
- **المقدار دليلٌ حين يصمت الوصف** — الصادر الأقلّ من عشرين ريالاً وليس ضريبةً رسمُ بنك. وموضعها بعد الكلمات مقصود: زكاةُ خمسة ريالات تبقى زكاة. ولها مصدرها `AMOUNT`، ولم تُدسّ تحت `KEYWORD` لأنّها ليست كلمة: من يقرأ «كيف عُرفت هذه؟» بعد شهر يجب أن يقرأ الحقّ.
- **ضريبةُ الرسم بابٌ غير باب الرسم.** وصفُ رسم القناة الرقمية ووصفُ ضريبته واحد — «CITY:Digital Channel» — ولا يفرّقهما إلّا «نوع العملية». فمن فحص الرسم أوّلاً نسب إلى البنك ما ذهب إلى الدولة (٣٥ حركة). والنظام يفصل أصلاً `POS_FEE` عن `POS_VAT`، فصار `BANK_VAT` هو الفصل نفسه في رسوم البنك. **والضريبة تُفحص قبل الرسم دائماً.**
- **صيغة الضريبة مضيَّقة عمداً**: «Tax» وحدها تقع في وصف زاتكا (`Zakat, Tax and Customs`) وهو سدادٌ حكوميّ بأربعة عشر ألف ريال — فلو أُخذت لصار سدادُ الدولة «ضريبةَ رسمٍ بنكيّ».
- **«نوع العملية» يصنّف حين يصمت الوصف.** `81140155-260626-POS 0` لا كلمة فيه، ونوعُه يقول «ضريبة عملية نقاط بيع فوري»؛ و`PoSMonthlyFeeSep…` لا يشبه صيغة الدفعات. وكان النوع يُقرأ للمطابقة ثمّ يُطرح من التصنيف — فبقيت حركاتٌ «غير مصنَّفة» وما يصنّفها مكتوبٌ في عمودٍ مجاور. **ويدخل النوعُ التعرّفَ ولا يدخل النمط**: هناك خاصّيةُ تصديرٍ تفرّق نمط الحركة الواحدة بين ملفّين، وهنا دليلٌ صريح على بابها.
- **القاعدة المقيَّدة بأوّل النصّ وآخره هشّة.** كان `^CITY:Digital Channel$` يُطابَق على `searchText` وهو يجمع الوصف والنوع والمستفيد — فالرسم الذي يُعرَف حين يغيب نوعه خرج مجهولاً حين حضر (بعد أن استعاده الدمج).
- **استعادةُ حقلٍ تكشف خطأً كان مستوراً.** لمّا أعاد الدمجُ «نوع العملية» إلى ١٤٠٩ حركات، خرجت ٢٥ حركة من «رسوم بنكية» إلى «غير مصنَّفة» — لا لأنّ شيئاً فسد، بل لأنّ تصنيفها كان صحيحاً بالمصادفة: قاعدةٌ هشّة طابقت نصّاً ناقصاً. **والانحدارُ الظاهر بعد إصلاحٍ يُفحَص، فقد يكون كشفاً لا عطباً.**
- **الطابور يقرأ الطبقة لا القرار القديم.** كان شرطُ «ما يحتاج قرارك» هو `match_disposition in ('SUGGEST','REVIEW')` — وهو عمودٌ عن مطابقة **فاتورة** لا عن معرفة الجهة. فمن عرّف حركةً وحدّث الصفحة وجدها تُسأل عنه ثانيةً: التعريف كُتب فعلاً، والعمود الآخر بقي كما هو، والطابور يقرأه. ثلاثٌ وأربعون حركة أكّدها أحمد بيده كانت تعود إليه كلّ مرّة، وأربعٌ وسبعون رسماً صُنّف تلقائياً كان يُسأل عنه. **والشرط صار الطبقةَ والمصدر**: ما قرّره إنسان لا يُسأل عنه، وما ليس سداد مورّد لا قرار فيه أصلاً. (٥٢ → ٥).
- **والقرار القديم يُطوى مع الجواب** — من قال «هذه رسمٌ بنكيّ» قال ضمناً إنّها ليست سداد فاتورة، فتُطوى `match_disposition`. ويطويها `db:reclassify` عن كلّ بابٍ ليس سداد مورّد.
- **مرجع العمليّة هويّةٌ فوق المفتاح الطبيعيّ.** المفتاح يمنع دخول الكشف مرّتين، ولا يمنع أن **يذكر الكشفُ الواحد الحوالةَ مرّتين** — يحميهما `occurrence` بوصفهما حقيقيّتين، وهو صحيحٌ في الرسوم (١٦ مجموعة `CITY:Digital Channel` متكرّرة في اليوم حقيقيّة) خطأٌ في الحوالات. والحوالة تحمل رقم عمليّة لا يتكرّر. **والقياس قبل القيد**: ١٢٤ حركة لها مرجع، والمكرَّر منها خمسة، كلّها بنفس المبلغ، ولا مرجعَ واحدٌ يخدم مبلغين — فالقيد الفريد لا يردّ حركةً حقيقيّة. وحركات الشبكة تُستثنى: «REFERENCE : 81140155 MC26 0811» رقمُ طرفيّةٍ لا عمليّة.
- **الموقوف يبقى بلا مرجع، ولا يُدَّعى له.** نسختان لعمليّةٍ واحدة كلتاهما مرتبطة بدفعة لا يقبلهما القيد، وكتابةُ المرجع لإحداهما ادّعاءٌ بأنّ الأخرى عمليّةٌ ثانية. فيبقى العمود فارغاً حتى يحسم إنسان — والفراغ يقول «لم يُحسَم».
- **نصٌّ يُشغَّل مرّتين فيعطي نتيجتين ليس نصَّ إصلاح.** `db:dedupe` كان يأكل دليلَه: بعد أوّل تنظيف تتوزّع النجاةُ على الملفّين، فيقرأ الشوطُ الثاني «أكثر ما قاله ملفٌّ واحد = ١» ويحذف ما أبقاه بالأمس — أربعةَ عشر صفّاً حقيقيّاً. فصارت الحقيقة أكبرَ الاثنين: ما يقوله ملفّ، وما قُبل من قبل (`max(occurrence)+1`).
- **اسم المستفيد يُقرأ من نصّ البنك، لا من عمودٍ يكتبه نظامُنا.** بُني التجميعُ على `beneficiary_raw` — وهو ملوَّث في **٣٧ صفّاً من ٤٦**: يحمل اسم المورّد الذي طابقه الاستيراد القديم لا المستفيد. فجُمعت أربع حوالات لأربع جهات — «أطوار العقارية» و«top taste» و«مؤسسة عمار» و«PURE BEVERAGE» — تحت اسمٍ واحد، **وقرارٌ واحد عليها كان سيُخطئ في أربع**. والقاعدة كانت مكتوبةً في هذا الملف وخُولفت. فصار `beneficiaryFrom` يقرأ الاسم من الوصف: بعد رمز الحوالة وقبل `BENBK`/`BEN ID`/اسمِ بنك المستفيد. ونصُّ البنك يسبق العمود ولا يُلغيه — والتلوّث يُعالَج في مصدره (`db:reclassify` يعيد كتابة العمود ويُفرّغ ما لم يقله البنك) لا بالالتفاف حوله في كل قراءة.
- **ولا يدخل العمودُ النمطَ أيضاً** — الاسم المُقحَم يتصدّر الكلمات الاثنتي عشرة فيطغى على ما يفرّق الحوالات.
- **ولا يُشتقّ اسمٌ من كلّ وصف** — «CITY:Digital Channel» رسمٌ لا مستفيد له، واشتقاقُ اسمٍ منه يصنع جهةً وهميّة تُجمَع تحتها حركات. فيُشترَط دليلٌ على وجود مستفيد: `BEN ID` أو `BENBK` أو «حوالات تحت الطلب» أو «رقم السداد».
- **التعميم يضاعف الخطأ كما يضاعف الصواب** — وهو أخطر ما في «عرّفه مرّة فيعمّ». فكلّ ما يُجمَع به يجب أن يكون خبراً من البنك لا استنتاجاً من عندنا.
- **عميلٌ في جدول الموردين.** «سبعة جرة — عميل» (واسمه الحقيقيّ «سابع جار») مسجَّل مورّداً، وأسماؤه البديلة فيها `Sales - Sabea Jar (فاتورة صادرة)` — أي أنّه يشتري من المقهى لا يبيع له. ولا جدولَ للعملاء بعد. فُصلت عنه أربع حركات نُسبت إليه خطأً، وبقي هو معلَّقاً لقرار أحمد.
- **هويّةٌ قُيِّدت بمعرفةٍ تتحسّن مع الوقت ليست هويّة — وهذا الدرس وقع ثلاث مرّات.** أوّلاً في دالّة البصمة (خرج منها رقم الحساب فبطل ما حُفظ بها)، ثمّ في نطاق الفرادة نفسه: فرادةُ الحركة مقيَّدة بحسابها (`014`) وهو صحيح — لكنّ الحساب **يُقرأ من الملفّ**. فالكشوف الأولى استُوردت ولا يُقرأ منها الرقم فحُفظت بحسابٍ فارغ، ثمّ صار القارئ يقرأه، فبُحث عن السابق **في نطاق الحساب الجديد** ولم يوجد شيء — لأنّ القديم كلّه في نطاق «المجهول». فدخل الكشف كلّه ثانيةً: ١٤٣٦ حركة، وصار الصادر ٧٣٦ ألفاً بدل ٣٦٨.
- **والفارغ ليس «حساباً آخر»، هو «لم نكن نعرف».** فالبحث عن السابق لا يُقيَّد بالحساب، ثمّ **يُتبنّى ما طابق**: يُكتَب له الحساب فينتقل من نطاق المجهول إلى نطاقه، ويحرسه القيدُ في القاعدة بعدها. ولا يُتبنّى إلّا ما طابق مفتاحاً في الكشف — تلك وحدها التي ثبت أنّها منه.
- **والإصلاح يُقابل واحدةً بواحدة لا يُبقي واحداً** — `db:repair-scope` كان سيأكل تسعةَ عشر صفّاً حقيقيّاً لو أبقى من كل مجموعةٍ صفّاً: المجموعة قد تحمل تكراراً حقيقيّاً (رسمُ القناة الرقمية مرّتين في اليوم). والدمج قبل الحذف كذلك: النسخة الجديدة قد تحمل عملَ إنسانٍ وقع عليها بعد رفعها — دفعةً رُبطت بها، أو تصنيفاً أكّده — فيُنقَل إلى الباقي قبل أن تُحذف.
- **الاستيراد صار مزامنة: «أهي عندنا؟» قبل «ما معناها؟».** كان المسار يحسب لثلاثمئة حركة مرشّحيها ودرجاتِها ويستدعي الحَكَم، ثمّ يكتشف أنّ مئتين وتسعين منها مقيَّدة. فصار الترتيب: اقرأ ← عرّف الحساب ← اعرف الهويّات ← افصل المعروف ← **سوِّ الجديد وحده**.
- **هويّة الحركة طبقتان، والوصف ليس أُولاهما.** المرجعُ الذي يعطيه البنك للعمليّة هو هويّتها؛ ووصفُها يتغيّر بين تصديرٍ وآخر والعمليّة واحدة. فمن جعل الوصف هويّةً أدخلها مرّتين. والوقائع (تاريخ · مبلغ · اتجاه · وصف موحَّد) طبقةٌ ثانية لما لا مرجع له.
- **والمراجع تُقابَل مجموعةً لا نصّاً واحداً** — كشفٌ يكتب «مرجع…» ورمزَ الحوالة معاً، وآخر يكتب المرجع وحده. فلو رُبطا في نصٍّ واحد لاختلف النصّان لحوالةٍ واحدة. ومرجعٌ واحد مشترك يكفي.
- **«رقم السداد» ليس مرجع عمليّة** — هو رقم المشترك عند الجهة (عدّاد الكهرباء، رقم الهاتف، الرقم الضريبيّ)، ويتكرّر في فاتورة كلّ شهر. فلو عُدّ هويّةً لابتُلعت فاتورةُ الشهر الثاني بوصفها «مقيَّدة»، ومالٌ خرج فعلاً لا أثر له. **كشفه الفحص**: ثلاث حركات سدادٍ حكوميّ تغيّر تصنيفُها بمجرّد قلب ترتيب الصفوف — والترتيب لا يغيّر حقيقة.
- **المفتاح لا يحمل الحساب، والنطاق يقع في اختيار ما يُقابَل** — لو دخل الحسابُ المفتاحَ لتغيّر المفتاح بتغيّر ما نعرفه عن الحساب. والوارد المجهولُ حسابُه يُقابَل بالكلّ: لا نعرف أنّه من حسابٍ آخر، والادّعاء بذلك يُضاعف المال.
- **والترتيب آخرُ شيء لا أوّلُه** — `occurrence` فاصلٌ بين متطابقتين لا مرجع لهما، لا هويّة. وبه تنجو الحركتان المتطابقتان الحقيقيّتان (١٨ مجموعة في كشف أحمد)، ولا يُعتمَد عليه في تمييز حركةٍ عن أخرى.
- **الالتباس يُعرَض ولا يُحسَم** — واردٌ بلا مرجع تطابق وقائعُه حركةً لها مرجع: أهي هي صُدِّرت بلا رقمها، أم عمليّةٌ ثانية تشبهها؟ لا تُدسّ في المقيَّد، ولا تُضاف، ولا يُحذَف طرف. تُعرَض في المعاينة ليقرّر إنسان.
- **ومرجعان مختلفان ليسا التباساً** — البنك يعطي كلّ عمليّةٍ رقمها، فاختلافُ الرقمين اختلافُ عمليّتين.
- **`db:identity` يعرض ولا يحذف** — مكرَّرٌ قاطع، ومحتمل، وتكرارٌ مشروع، وفريدٌ مؤكَّد. وتنظيفُ المال قرارُ إنسان: يُعرَض بدليله ويُنتظَر فيه.
- **المورّد محور المستحقّات، لا الفاتورة.** كان النظام يسأل عن كل حوالة «أيّ فاتورة تفسّرها؟» فإن لم يجد مبلغاً مطابقاً وقف — ولا يعطي فعلاً. وبعض مورّدي أحمد لا يعطون فاتورةً أصلاً: كشفُ حسابٍ أو ورقةٌ باليد. فالحوالة **سدادٌ لحساب المورّد**، والفواتير تفصيلٌ داخله: تُقيَّد الدفعة، وتُوزَّع على المفتوح، وما بقي يبقى «غير مخصَّص» — وهي حالٌ صحيحة لا نقص.
- **وسياسة التخصيص معلَنة: الأقدم أوّلاً.** لا يكسرها إلّا رقمُ فاتورةٍ في الحوالة أو توزيعٌ يكتبه صاحب العمل بيده — ولا يخترعها المحسِّن ليرفع درجة. ولا تُسدَّد فاتورةٌ لم تكن قد وُجدت: النافذة سبعةُ أيّامٍ بعد الحوالة (الفاتورة تُكتب بتاريخ التسليم وتصل بعده)، وما جاوزها سدادٌ مقدَّم يقرّره إنسان.
- **والخادم يُعيد الحساب لحظةَ السداد** — لا يأخذ من المتصفّح إلّا معرّف الحركة. لأنّ ما رُجّح لحظةَ الاستيراد قد تكون فاتورتُه سُدّدت بعده من دفعةٍ أخرى.
- **المجموعة تُسدَّد في معاملةٍ واحدة لا في خمسةَ عشر طلباً.** كانت الشاشة ترسل حركةً حركة، فإن نجح ثمانية وفشل التاسع بقيت المجموعة نصفَ مقيَّدة ولا أحد يعرف أين وقفت. والمستحقّ يتناقص بين حركةٍ وأختها **داخل المعاملة نفسها**، فلا تُخصَّص فاتورةٌ واحدة لدفعتين.
- **وسباقُ دفعتين على فاتورةٍ واحدة محسومٌ في القاعدة لا في الترتيب** — مؤثِّرُ `007` يرفض تجاوز إجمالي الفاتورة. ويثبته `db:verify` بمحاولة إدخالٍ تُرَدّ: لولاه لصارت الفاتورة مسدَّدة مرّتين ولخرج ستّة آلاف عن ثلاثة.
- **السياسة تُقال للمستخدم قبل الضغط** — «لا رقمَ فاتورةٍ في الحوالة، فتُوزَّع بالأقدم أوّلاً»، ومعها ما سيُخصَّص وما سيبقى على الحساب. والسياسة المخفيّة تُنتج ثقةً بلا فهم.
- **والذاكرة تُختبَر على المستقبل لا على الكشف نفسه** — «أعرّفه مرّة» تعني أن يأتي كشفُ الشهر القادم فيُعرَف بلا سؤال، لا أن يختفي التنبيه من هذه الشاشة.
- **لا يُحاسَب القديم بما لم يكن موجوداً** — `decision_history` وُلد في `013`، فحركةٌ قُيّدت قبله لا أثر لها فيه: تاريخٌ لا عطب. والفحص يقتصر على ما بعد أوّل سجلٍّ فيه.

## المصائد المعروفة

- **حصّة Gemini المجانية = ٢٠ طلباً/اليوم لكل نموذج** (لا في الدقيقة). لذلك تدوير سبعة نماذج.
- **الطبقة المجانية تتدرّب على الفواتير ويراجعها بشر.** التبديل سطر: `EXTRACTION_PROVIDER=claude`.
- **`AUTH_BYPASS`** يفتح كل الأبواب في التطوير، ولا يعمل في الإنتاج مهما فُعِّل — الحارس مزدوج في `src/lib/preview-mode.ts`.
- **مساعد `now()` يسمّي كل عمود `created_at`** مهما كان اسم الحقل في TypeScript. فـ`documents.uploadedAt` عمودها `created_at` — ومن يكتب SQL خاماً يقع فيها.
- **`transaction_type`** كان ناقصاً في ألفٍ وأربعمئة حركة — استعاده الدمجُ في `db:dedupe` من نسختها المحذوفة، وصار كاملاً في ١٤٤٥ حركة. ومن يحذف مكرَّراً بلا دمج يفقده ثانيةً.
- **مفاتيح تحتاج تدويراً بيد أحمد:** `GEMINI_API_KEY` و`GOOGLE_CLIENT_SECRET` (ظهرا في محادثة).
- **بيئة المعاينة في Vercel** — لم يُتحقّق أتشارك قاعدة الإنتاج أم لا. إن كانت تشاركها فكل نشر تجريبي يكتب في البيانات الحقيقية.

## الأوامر

`npm test` (١٢٣٣ اختباراً · ٨٣ ملفاً) · `npm run typecheck` · `npm run lint`
`npm run db:migrate` · `db:verify` · `db:dedupe` · `db:rematch` · `db:reclassify` · `db:learn` · `db:link` · `db:split-check`
`npm run db:expenses` · `db:audit` · `db:measure` · `db:repair` · `db:products` · `db:merge` · `db:reprice` · `db:repair-rules` · `db:repair-scope` · `db:identity`
`npm run drive:auth` · `drive:inventory` · `drive:backfill` · `drive:diagnose`

### بوّابة الإنتاج — لا يُقال «جاهز» بلا دليل

| الأمر | ماذا يفعل |
|---|---|
| `npm run ops:gate` | خمسة عشر بنداً: `PASS` أو `FAIL` أو **`?` لم يُفحَص** — والمجهول يمنع كما يمنع الفشل |
| `npm run ops:certify` | سبعةُ سيناريوهات ماليّة على المخطّط الحقيقيّ، **كلٌّ يُلغى أثرُه** فلا يُكتَب شيء |
| `npm run ops:truth` | الأصل في الدرايف مقابل القيد: مطابق · يحتاج تصحيحاً · مكرَّر · مفقود · لا يُقطَع فيه |
| `npm run ops:isolation` | بصمةُ القاعدة — و`systemIdentifier` هو الحاكم، لا اسمُ المضيف |

وما لا يُفحَص آلياً — تدويرُ مفتاح، وأخذُ نسخة — يُقَرّ في `ops-attestation.json`
(انظر المثال). ولا يُكتَب `PASS` بالنيّة.

## مبنيٌّ ولا يصل إليه أحد — لا عمداً

هذه ليست «لم تُبنَ»؛ هي بُنيت واختُبرت ووُثّقت، ثمّ لم تُوصَل بشاشة.
والفرق مهمّ: ما لم يُبنَ يُعرَف أنّه ناقص، وما بُني ولم يُوصَل **يُظنّ
عاملاً** — في الوثيقة وفي الذهن — فيُبنى عليه.

| الوحدة | ماذا يضيع بغيابها |
|---|---|
| `src/lib/credit-notes.ts` | الإشعار الدائن لا يخفض المستحقّ — لا إشعارات في البيانات اليوم، فالأثر كامنٌ لا واقع |
| `src/lib/bank/reversal.ts` | الردّ لا يُكشَف آلياً — حالةٌ واحدة محتملة في البيانات |
| `src/lib/unit-conversion.ts` | مقارنة الأسعار لا تعبر الوحدات — والأصناف ١٠٩ ولم يُربَط منها إلّا واحد |
| `src/lib/extraction/benchmark.ts` | لا يُقاس أيّ نموذجٍ أدقّ |
| `detectAnomalies` في `lifecycle.ts` | «أُقرَّت ولم تُقيَّد» لا تظهر — وفي البيانات حركتان كذلك بـ٩٬٤٦٢٫٧٧ ريالاً |
| `branches` · `bank_accounts` · `reconciliation_periods` | مخطَّطٌ بلا شيفرة: `branchId` لا يرد في استعلامٍ واحد، و`bank_account_id` فارغ في ١٤٢٢ حركة — فتقييد الفرادة بالحساب في `014` لا أثر له |

**قبل أن تُضيف وحدةً جديدة: أوصِل واحدةً من هذه.**

## ما ليس مبنيّاً — عمداً

Foodics ونقاط البيع · تكلفة المبيعات والهامش · المخزون والوصفات · التنبّؤ · «المدير المالي» الذكي · تصدير محاسبي · الإشعارات.
جداول المبيعات موجودة وفارغة، وواجهة `SalesConnector` معرَّفة بلا تطبيق. **لا تملأها ببيانات وهمية.**
