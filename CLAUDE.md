@AGENTS.md

# TPH-Invoicing — الذاكرة الدائمة

نظام مالي وتشغيلي لمقهى **مؤسسة ذا بوبليك هاوس** (النسيم، جدة).
الرقم الضريبي `310007971600003` · السجل `7052766941`.

الشرح الطويل في **`docs/ARCHITECTURE.md`** — وهو مصدر الحقيقة للمعمارية.
هذا الملف يحمل ما تحتاجه *قبل* أن تكتب سطراً: القرارات وأسبابها، والمصائد.
`docs/HANDOVER.md` و`docs/PLAN.md` **وثيقتان مؤرّختان** — تصفان ما كان، لا ما هو قائم.

## قيود لا تُخترق

1. **لا يُمسّ أرشيف جوجل درايف.** لا حذف ولا نقل بحال. و**إعادة التسمية** أذِن بها أحمد صراحةً في ٧ سبتمبر ٢٠٢٦، مقيَّدةً بأربعة: التسميةُ وحدها · ما له سجلٌّ عندنا وحده · ولا يُقترَح اسمٌ إلّا لملفٍّ **لا يُقرأ اسمُه** · ولا شيء بلا اختيار الإنسان ملفّاً ملفّاً وأثرٍ في سجلّ التدقيق. الدالّة الوحيدة `renameFile` في `src/lib/drive.ts`.
2. **العربية** في كل ما يُكتب له: فصحى سهلة.
3. **المال عدد صحيح بالهللات** (`HALALAS_PER_RIYAL = 100` في `src/lib/money.ts`). لا عدد عشري في أي حساب مالي، أبداً.
4. **المجهول ليس صفراً.** ما لم يُقرأ يُعرض «غير معروف»، لا `0`. صفرٌ كاذب يقول إنّ المقهى لم يشترِ شيئاً.
5. **لا بيانات مبيعات مخترَعة.** لا Foodics بعد؛ `notConnected` في `src/lib/sales/connector.ts` **يرمي** `NotConnectedError` ولا يُرجع أصفاراً.

## البنية التقنية

Next.js **16.3.4** (App Router) · React 19.2.8 · TypeScript 5 صارم · Tailwind v4
Drizzle ORM 0.45.2 + `pg` 8.23 على **Neon Postgres**
Auth.js v5 (`next-auth@5.0.0-beta.32`) + `@auth/drizzle-adapter` — جوجل، جلسات في القاعدة
Vitest 4.1.11 · zod 4.5.4 · googleapis 178 (الدرايف وحده) · `@anthropic-ai/sdk` 0.123

**الذكاء: DeepSeek وحده** — بلا حزمة، واجهةٌ متوافقة مع OpenAI عبر `fetch`.
نموذجان لثلاث مهامّ: `deepseek-flash` (V4.1، متعدّد الوسائط منذ ١٠ سبتمبر ٢٠٢٦) رؤيةً ونصّاً ·
`deepseek-v4-pro` استدلالاً وتحليلاً. والإعداد في
`src/lib/ai/models.ts` وحده.

**القاعدة:** يجب استعمال نقطة Neon **المجمَّعة** (`...-pooler.c-2.us-east-2.aws.neon.tech`).
غير المجمَّعة تنفد اتصالاتها تحت Vercel.

**النشر:** Vercel `tph4/tph-invoicing` · GitHub `AhmedJaai/TPH-Invoicing` · الفرع `main`.

## القاعدة — ٥٢ جدولاً

`users` `accounts` `sessions` `verification_tokens` · `documents` `invoices` `invoice_lines` `issues`
`suppliers` `supplier_aliases` `supplier_products` · `payments` `payment_allocations`
`bank_imports` `bank_transactions` `bank_rules` · `statements` `statement_lines` · `month_closes`
`products` `recurring_expenses` · `sales` `sale_lines` `sales_sources` `pos_products`
`audit_logs` `rate_limits` `expenses`
`counterparties` `counterparty_evidence` · `branches` `bank_accounts` `reconciliation_periods`
`sale_payments` `refunds` `refund_lines` `settlement_batches`
`adjudications` `decision_history` · `ai_findings` · `extraction_cache` · `alert_resolutions`
`recipes` `recipe_versions` `recipe_ingredients` · `sales_imports` `sales_import_rows`
`inventory_counts` `inventory_count_lines` `inventory_count_snapshots` `inventory_movements` `waste_records`

التعريف في `src/db/schema.ts`.

### الهجرات: SQL صريح، لا `drizzle-kit push`

الملفات في `drizzle/sql/` ويشغّلها `npm run db:migrate` (`scripts/migrate.ts`).

**السبب:** `drizzle-kit push` يطلب طرفيّة تفاعلية — فيتعذّر في CI — ولا يترك أثراً يُراجَع.

**وتُطبَّق مع النشر**: `vercel-build` هو `migrate && next build`، بـ`&&` لا
بـ`;` — فالهجرةُ الساقطة توقف النشر، ونشرٌ لم يقع خيرٌ من نشرٍ يقرأ عموداً
غير موجود. ويحرسه `deploy-guards.test.ts`.

> **وكان الادّعاء قائماً والفعل غائباً.** قالت هذه الوثيقة «تُطبَّق مع كلّ
> نشر» ولم يكن ثمّة `vercel-build` ولا `postbuild` ولا خطوةٌ في CI، فبقيت
> قاعدةُ الإنتاج عند ٣٤ والمستودعُ عند ٣٥. ولم يظهر إلّا عند نشرٍ يقرأ
> عمودين جديدين — **ونشرُه قبل الهجرة كان يكسر كلّ استعلامِ مورّد**.
> والوثيقةُ التي تصف ما لا يقع أسوأ من ألّا تُكتب.

**ولذلك: الهجرةُ تسبق الكودَ الذي يحتاجها بنشرةٍ كاملة.** لأنّها تجري في
**البناء**، أي قبل أن يُرفَع الكودُ الجديد — فبين الاثنين نافذةٌ يعمل فيها
**الكودُ القديم على المخطّط الجديد**. فالإضافةُ آمنة (عمودٌ بقيمةٍ
افتراضيّة، جدولٌ جديد)، و**الحذفُ وإعادةُ التسمية ليسا كذلك**: يُقسَّمان
على نشرتين — تُضاف الأولى ويُقرأ الاثنان، ثمّ يُحذف القديم.

| الملف | ماذا يفعل |
|---|---|
| `001_audit_log_immutable.sql` | مؤثِّرات تمنع تعديل سجلّ التدقيق أو حذفه أو تفريغه |
| `002_tax_status_and_idempotency.sql` | حالات الضريبة، ومفاتيح منع التكرار الثلاثة |
| `003_transaction_type.sql` | `bank_transactions.transaction_type` |
| `004_rate_limits.sql` | جدول حدّ الطلبات |
| `005_products_and_sales_domain.sql` | الأصناف والمصروفات المتكرّرة ومجال المبيعات |
| `006_expenses.sql` | المصروف الفعلي، وقيدٌ يمنع تكراره عن حركة أو فاتورة |
| `007_financial_invariants.sql` | قيود المال في القاعدة: التخصيص والفاتورة والمصروف |
| `008_bank_kinds.sql` | أبواب حركة البنك: تسوية الشبكة ورسومها وضريبتها |
| `009_match_evidence.sql` | أدلّة المطابقة وقرارها ودرجتها — كي يُعرَض «لماذا؟» ويُتراجَع |
| `010_counterparties.sql` | ذاكرة المستفيدين: هويّة الجهة وأدلّتها |
| `011_branches_and_accounts.sql` | الفروع والحسابات وفترات التسوية |
| `012_sales_domain.sql` | طرق دفع البيعة والمرتجعات ودفعات التسوية |
| `013_decision_provenance.sql` | أثر القرار: من صنّف، وأيّ نموذجٍ حكَم، وكيف تطوّر |
| `014_identity_scoping.sql` | فرادة الحركة مقيَّدة بحسابها، وفرادة الدليل بحسب نوعه |
| `015_payment_lifecycle.sql` | سبعُ حالاتٍ للدفعة، ورسمُ التحويل، والردّ لا يحذف |
| `016_lifecycle_and_expense_events.sql` | طبقات الحركة، وبصمةُ حدث المصروف |
| `017_sales_identity_and_units.sql` | فرعُ البيعة وهويّتها، ومحتوى عبوة الصنف |
| `018_statement_balances_nullable.sql` | رصيدا الكشف يقبلان المجهول — والصفر كان يقول «لا يطالبنا بشيء» |
| `019_pattern_identity.sql` | النمط دليلٌ على الجهة — ظنّيٌّ كالاسم، وبه تُعرَف الحركة التي لا اسم لها ولا حساب |
| `020_natural_transaction_key.sql` | المنع على المفتاح الطبيعيّ لا على بصمةٍ تُحسَب — و`occurrence` جزءٌ منه |
| `021_amount_classification.sql` | `AMOUNT` مصدرَ تصنيف — المقدار دلّ حين صمت الوصف |
| `022_bank_vat_category.sql` | `BANK_VAT` — ضريبةُ الرسم بابٌ غير باب الرسم |
| `023_operation_reference.sql` | مرجع العمليّة فريد — من تطابق مرجعُه تطابقت عمليّته |
| `024_identity_key.sql` | الهويّة تُخزَّن وتُقيَّد: `REF` بالمرجع أو `FACT` بالوقائع |
| `025_identity_supersedes_natural.sql` | قيدٌ واحد للهويّة — لا قيدان بتوحيدين مختلفين |
| `026_allocation_bounds_serialize.sql` | مؤثِّر التخصيص يقفل الفاتورة والدفعة قبل الجمع — فلا يُخدَع بالتزاحم |
| `027_owner_account_and_ai_findings.sql` | `OWNER_ACCOUNT` طريقةَ سداد، وجدول `ai_findings` لاقتراحات تحليل الذكاء وقرار الإنسان فيها |
| `028_month_lock.sql` | الشهر المقفل لا يُكتب فيه: مؤثِّرات على الفواتير والدفعات والتخصيصات — وتحديث الحال مقبول |
| `029_ops_indexes.sql` | فهارس ناقصة: `payment_allocations.invoice_id` و`matched_payment_id` و`bank_import_id` و`user_id` |
| `030_extraction_cache.sql` | `extraction_cache` — ما قرأه النموذج بيد الخادم ببصمة الملفّ، لا من المتصفّح |
| `031_extraction_text_source.sql` | مصدرُ القراءة (نصّ · صورة مضمَّنة · صورة) يُحفَظ مع ما قُرئ ويُنقل إلى `documents.text_source` |
| `032_payment_month_stable.sql` | `payment_month` تُوسَم `STABLE` لا `IMMUTABLE` — `to_char(timestamptz)` يتبع منطقة الجلسة |
| `033_document_drive_md5.sql` | `documents.drive_md5` — بصمةُ الدرايف لما قيّدته المزامنةُ بالاسم، يقابلها `/api/analyze` قبل القراءة |
| `034_alert_resolutions.sql` | قرارُ الإنسان في تنبيه «سُدّد مرّتين»: طالبتُ · استُردّ · ليس ازدواجاً — بمفتاحٍ يُشتقّ من الحركات |
| `035_supplier_document_policy.sql` | سياسةُ مستندات المورّد: `contract_required` و`paper_invoices` — يكتبهما الإنسان ولا يُشتقّان، وقيدٌ يمنع «ورقيّة» مع «لا يصدر فواتير» |
| `036_inventory_reconciliation.sql` | الجرد: الوصفةُ بنسخها المؤرَّخة، واستيرادُ المبيعات بصفوفه الخام، وجلسةُ الجرد وأسطرُها ولقطتُها، والحركاتُ اليدويّة وسجلُّ الهدر — ومؤثِّران: لا نسختا وصفةٍ ساريتان معاً، ولا كتابةَ في جردٍ مقفَل |

والمشغّل لا يعيد هجرةً مطبَّقة تغيّر ملفّها إلّا بـ`--reapply <الاسم>`، وبقفلٍ استشاريّ ضدّ تشغيلين.

## المكتبات التي يجب معرفتها

| الملف | مسؤوليّته |
|---|---|
| `src/lib/confirm.ts` | `reviewConfirmed()` — **الخادم** يعيد اشتقاق حال الضريبة. لا يُصدَّق المتصفّح في رقم مالي. |
| `src/lib/line-pricing.ts` | `resolveLinePricing()` / `reconcileInvoiceLines()` — قلب صحّة الأسعار |
| `src/lib/allocation.ts` | `planAllocations()` يمنع تخصيص أكثر من الدفعة · `settleSupplierAccount()` سياسة الأقدم أوّلاً |
| `src/lib/bank/identity.ts` | بصمةُ الملفّ (اختصارٌ لا هويّة) ومراجعُ العمليّة — لا خوارزميّةَ هويّةٍ هنا |
| `src/lib/bank/pattern.ts` | هويّات الحركة مرتَّبةً، والنمط آخرها — وبها يُجمَع المتشابه |
| `src/lib/bank/sync.ts` | **أهي عندنا؟** مرجعُ العمليّة ثمّ الوقائع — قبل أن يُسأل ما معناها |
| `src/lib/bank/rules.ts` | تصنيف الحركات. **GOVERNMENT قبل ZAKAT** وإلّا قُرئت «زاتكا» صدقةً |
| `src/lib/bank/canonical.ts` | نموذج الحركة المعياريّ — المرجع يُنسب إلى نوعه بدليل |
| `src/lib/bank/classification.ts` | تصنيف بطبقات: البنية ← المتعلَّم ← الكلمات ← المجهول يُعلَن |
| `src/lib/bank/entities.ts` | تعريف المستفيد بأدلّة قاطعة وظنّية — والظنّيّ وحده لا يكفي |
| `src/lib/bank/candidates.ts` | توليد المرشّحين وتسجيلهم · مجموع الجزئيات بلا سقف ثلاث |
| `src/lib/bank/optimizer.ts` | تفريعٌ وتحديد: أعلى **مجموع** لا أعلى درجة. وميزانيّة عقد، فإن نفدت رجع للجشع وأعلن `exact: false` |
| `src/lib/bank/coverage.ts` | فجوات التغطية — الغائب لا يُرى فيجب أن يُحسَب |
| `src/lib/bank/balance-equation.ts` | **افتتاحي + وارد − صادر = ختامي.** وهذه التسوية؛ وعدُّ المطابقات ليس تسوية |
| `src/lib/bank/lifecycle.ts` | طبقات الحركة: خام ← مُستنتَجة ← مقترَحة ← مُقَرَّة ← مُقيَّدة · و`detectAnomalies` موصولةٌ ببندٍ في «يحتاج انتباهك» |
| `src/lib/bank/review-queue.ts` | ثلاثة أعمالٍ لا عملٌ واحد: يُقَرّ · يُراجَع · يُحسَم |
| `src/lib/bank/evidence-uniqueness.ts` | القاطع يُحتكَر ويُعلَن تضاربه، والظنّيّ يُشترَك — والاسم ليس هويّة |
| `src/lib/bank/supplier-profile.ts` | عادةُ سداد المورّد — ترجّح ولا تحسم، ولا تُبنى على أقلّ من خمس سابقات |
| `src/lib/bank/vision-statement.ts` | الكشف المصوَّر: كلّ مبلغٍ يُفحَص، والمعادلةُ تردّ الكشف كلّه إن اختلّت |
| `src/lib/bank/metrics.ts` | أنتحسّن أم نسوء — ويُحسَب على ما فعله الإنسان وحده |
| `src/lib/bank/parsers/adapters.ts` | محوِّل لكل بنك، و`verified` يُعرَض للمستخدم لا يُدفَن في تعليق |
| `src/lib/payment-state.ts` | سبعُ حالاتٍ للدفعة — والمردودة لا تُحسَب مدفوعة |
| `src/services/payment.service.ts` | `createPayment` يسأل التوأم ويرمي ما لم يُقَرّ · `recordBankPayment` يتبنّى المقيَّد بلا حركة · `claimBankTransaction` يربط بشرط ألّا تكون مربوطة ويرمي |
| `src/lib/unit-conversion.ts` | لا جسر بين وزنٍ وحجم، والمجهول لا يُحوَّل · و`StoredUnit` جسرُ عمود القاعدة (`G`/`L`) إلى `BaseUnit` (`GRAM`/`LITER`) — في موضعٍ واحد |
| `src/lib/bank/fees.ts` | الرسم البنكيّ داخل الدفعة — والنقص سدادٌ جزئيّ لا رسم |
| `src/lib/bank/reversal.ts` | ما خرج ثمّ عاد — لا إيراد ولا تحويل داخليّ · **لا تصل إليها شاشة** |
| `src/lib/credit-notes.ts` | الإشعار الدائن يخفض المستحقّ ولا يُعدّ سداداً · **موصولةٌ نصفَ وصل** — لا يُمرَّر لها إشعار |
| `src/services/adjudicator.service.ts` | استدعاء الحَكَم — وحكمه اقتراحٌ لا مطابقة |
| `src/lib/bank/adjudicate.ts` | متى يُستدعى الذكاء: عند العجز وحده، وعلى مرشّحين مولَّدين لا بيانات خام |
| `src/lib/bank/parsers/detect.ts` | كشف البنك من الملفّ — و`\b` لا تفصل عند `_` وأسماء الملفّات مليئة بها |
| `src/lib/bank/parsers/pdf-text.ts` | نصّ PDF ومواضعه — ولا يُرمى ملفٌّ كامل إلى نموذج |
| `src/lib/ai/models.ts` | النموذج يتبع المهمّة — وموضعٌ واحد لأسمائها، فلا سلسلةٌ حرفيّة تُكتب مرّةً ولا يُسأل عنها |
| `src/lib/ai/deadline.ts` | `withDeadline` — المسار يعلن عمره، وكلّ محاولة نداءٍ تأخذ ما بقي منه. **مسارٌ جديد يستدعي الذكاء يُلفّ بها** |
| `src/lib/ai/deepseek.ts` | النداء: مهلةٌ معلَنة، وإعادةٌ على العابر وحده، و**عطبٌ مصنَّف** — الرصيد الناضب غير انقطاع الشبكة |
| `src/lib/ai/document-input.ts` | كيف يبلغ المستندُ النموذج: نصّاً يُقرأ حسابياً، أو صورةً تُنتزَع، أو **لا يُقرأ فيُعلَن** |
| `src/lib/ai/pdf-images.ts` | ينتزع JPEG المضمَّن — الماسح وضعه في الملفّ كما هو، فلا يُرسَم ولا تُضاف حزمة |
| `src/lib/extraction/provider-deepseek.ts` | قراءة المستند على مرحلتين، والمخطّط في الموجِّه لأنّ المزوّد لا يفرضه |
| `src/lib/extraction/validate-extraction.ts` | أقرأ النموذجُ صحيحاً؟ — سؤالٌ غير «أهذه الفاتورة سليمة؟»، وجوابُه يُعيد السؤال موجَّهاً |
| `src/lib/extraction/schemas-by-kind.ts` | مخطّط لكل نوع مستند — لا ثلاثون حقلاً لفاتورةٍ فيها ستّة |
| `src/lib/extraction/statement-extras.ts` | أسطر الكشف ورصيداه من مخرَج النموذج الخام — والرصيد المجهول `null` لا صفر |
| `src/lib/extraction/benchmark.ts` | مقياس النماذج — **الخطأ الواثق** يسبق الدقّة · **لا تصل إليها شاشة** |
| `src/lib/extraction/versions.ts` | نسخ الموجِّه والمخطّط، والنماذج مثبَّتة لا عائمة |
| `src/services/counterparty.service.ts` | تأكيدٌ واحد يعمّ على أمثاله |
| `src/lib/bank/decision.ts` | تلقائيّ/اقتراح/مراجعة بقاعدة الهامش |
| `src/services/reconcile.service.ts` | الجسر: صفوف الكشف ← قرارٌ لكل حركة مع أدلّته |
| `src/lib/bank/match.ts` | ربط الحركة بمورّد — حدود الكلمات، لا احتواء نصّي |
| `src/lib/statement-match.ts` | مطابقة كشوف المورّدين |
| `src/lib/month-close.ts` | الإقفال: `BLOCK` يمنع، `WARN` لا يمنع |
| `src/lib/attention.ts` | كل تنبيه يحمل `href` (مكان الإصلاح) و`evidence` (لِمَ ظهر) |
| `src/lib/data-health.ts` | التغطية لا تُختلق؛ فيها حال `NOT_CONNECTED` |
| `src/lib/expenses.ts` | المصروف الفعلي. **سداد المورّد ليس مصروفاً** — محسوبٌ في المشتريات. وازدواجُ الحدث يُكشَف ولا يُحذَف |
| `src/lib/provenance.ts` | «من أين جاء هذا الرقم» — التغطية بالعدد لا بالمبلغ |
| `src/lib/arabic.ts` | تمييز العدد — القاعدة تدور عند المئة فلا تُكتب بالحدس. **ثلاثة عشر جدولاً، وكلُّ عددٍ في الواجهة يمرّ بها** |
| `src/components/ui.tsx` | البطاقة والجدول والحالات. **الجدول يصير بطاقات على الجوّال**، وعمودُ المال `numeric` تلتقي فواصله |
| `src/components/scroll-x.tsx` | يقيس الفيض ويرسم الحافّة على الجهة التي وراءها شيء — فالعلامة إن ظهرت صدقت |
| `src/lib/bank/pending.ts` | سؤالان لا سؤال: **من الجهة؟** و**أيّ فاتورة؟** — والثاني لا يُطوى بجواب الأوّل |
| `src/lib/bank/double-paid.ts` | مالٌ خرج مرّتين في اليوم لجهةٍ واحدة — يُطالَب به ولا يُصلَح في قيدنا |
| `src/components/review-workspace.tsx` | ثلاثة أبواب، ولكلّ بندٍ فعلُه: يُؤكَّد · ليس سداداً · تُعرَّف جهته |
| `src/components/ui-client.tsx` | `ConfirmAction` — الفعل الخطير يُقرّ به لا يُسأل عنه «هل أنت متأكّد؟» |
| `src/lib/preview-mode.ts` | وضع المعاينة — لا يعمل في الإنتاج مهما فُعِّل المتغيّر |
| `src/lib/canonical-name.ts` | الاسم القياسيّ من المقيَّد — ولا يُمَسّ اسمٌ يُقرأ |
| `src/lib/nav.ts` | **خمسُ مساحات** وألسنتُها، والمساحة تجمع ما يُسأل معاً لا ما يُخزَّن معاً: «دفعة الشهر» عند المورّدين لأنّها جوابُ «لمن أدين». الموضع يُشتقّ من المسار لا من خاصيّة تُكتب باليد فتُنسى، والإعدادات في `ACCOUNT_LINKS` خارج التنقّل |
| `src/lib/work.ts` | **عددٌ واحد للعمل الباقي** — هو عددُ بنود `/attention` نفسِها، بـ`cache()` فلا تُستعلَم القاعدة مرّتين في الطلب |
| `src/lib/invoice-findings.ts` | **لماذا ناقصةُ ركن** — يُشتقّ ولا يُخزَّن، ولكلّ سببٍ فعلُه ومن يُطالَب به |
| `src/lib/drive-readonly.ts` | الكتابة على الدرايف في الإنتاج وحده — فالقاعدة تتفرّع والأرشيف لا يتفرّع |
| `src/components/view-controls.tsx` | الوضع الداكن وإخفاء المبالغ — على `<html>` بنصٍّ يسبق الرسم، ولا حالةَ في الترميز |
| `src/components/task-list.tsx` | **صفُّ مهمّة** في الرئيسية — سطران وفعلٌ واحد (~٨٠ بكسلاً)، لا بطاقةُ تنبيه (٢٤٩) |
| `src/components/attention-workspaces.tsx` | **العملُ يقع في الطابور** — طلبُ الفواتير، واعتمادُ المستندات، وسياسةُ المورّد، وطلبُ الكشوف: كلٌّ لوحٌ داخل بنده |
| `src/lib/supplier-policy-rules.ts` | **«من يحتاج عقد توريد» قاعدةٌ واحدة بصيغتين** — تُقرأ في TypeScript وتُحقَن في SQL، ولا تُنسَخ في شاشة |
| `src/components/double-paid-section.tsx` | «سُدّد مرّتين» في موضعٍ واحد — كان معروضاً كاملاً في شاشتين |
| `src/components/review-section.tsx` | ورشةُ قرار حركات البنك — لوحٌ داخل بندها لا صفحةٌ تُفتَح فارغة |
| `src/lib/supplier-balances.ts` | **«كم أدين؟» بالمورّد لا بالفاتورة**: المفتوح ناقص رصيدنا عنده، ولا يُخصم رصيدُ مورّدٍ من دين آخر · المصدر `supplier-balance.service.ts` لكلّ شاشة تقول «عليك» |
| `src/services/supplier-credit.service.ts` | `applySupplierCredit` يخصم ما دُفع من فواتير وصلت بعده (٧ أيّام آلياً) · `markPaidByOwner` يقيّد السداد من حساب المالك **وينقل حوالات المقهى عن تلك الفاتورة** · ومعاينته قبله |
| `src/lib/ai/supplier-analysis.ts` | تحليل حساب المورّد: الإشارات تُحسب قبل النموذج، والمراجع قصيرة من قوائم حُسبت، **والمبلغ يحسبه الخادم** · الأنواع قائمة مغلقة |
| `src/services/supplier-analysis.service.ts` | الوقائع ← النداء (بمهلةٍ تحت عمر المسار) ← `ai_findings` · والإقرار يمرّ بالخدمات نفسها، والرفض يقرؤه التحليل القادم |
| `src/lib/http-client.ts` | **قراءة الردّ في المتصفّح**: نصّاً قبل JSON، و«تعذّر الاتصال» حين لا يصل الطلب وحده. ويمنع `code-guards.test.ts` ‏`await res.json()` في الشاشات |
| `src/lib/code-guards.test.ts` | حرّاسٌ نصّيّة: لا `db.` داخل معاملة · لا `renameFile` خارج `/api/drive-rename` · لا `res.json()` خامّ |
| `src/services/month-guard.ts` | `assertMonthsOpen(tx, months)` — في `createPayment` و`allocate` و`createInvoice`، ومؤثِّرات ٠٢٨ خلفه |
| `src/lib/bank/statement-balances.ts` | رصيدا كلّ شهرٍ من عمود الرصيد، إن استقامت السلسلة — وإلّا مجهول |
| `src/lib/riyadh-time.ts` | «اليوم» و«الشهر الجاري» بتوقيت الرياض — لا `toISOString().slice(0, 7)` |
| `src/lib/token-crypto.ts` | رمز الدرايف مشفَّراً (`sealToken`/`openToken`) — يُفعَّل بـ`TOKEN_ENCRYPTION_KEY`، وبلا مفتاحٍ لا يتغيّر شيء. **كلّ `setCredentials` يمرّ بـ`openToken`** |
| `src/services/guard.ts` | `guard(route, capability)` + `respondTo(e)` → 401/403/429. **مدخل كل واجهة** |
| `src/lib/inventory/units.ts` | **الكمّيّة عددٌ صحيح بالمِلّي** — كما أنّ المال هللات. ولا جسر بين وزنٍ وحجم، ولا بين حبّةٍ وعبوة |
| `src/lib/inventory/recipe.ts` | النسخةُ السارية **في تاريخ البيعة** — لا الأحدث. وبها وحدها يبقى تقريرُ ما مضى صادقاً |
| `src/lib/inventory/consumption.ts` | الاستهلاك المتوقَّع — والملغى يُستبعَد، والمرتجَع يُنقص، والمجانيّ يُستهلَك ويُعرَض على حدة |
| `src/lib/inventory/purchases.ts` | كمّيّةُ الشراء من مواصفة العبوة — **ولا تُخمَّن كمّيّةٌ من ريال** |
| `src/lib/inventory/equation.ts` | افتتاحيّ + مشتريات − استهلاك − هدر = المتوقَّع · و`null` تنتشر ولا تُبتَلع |
| `src/lib/inventory/coverage.ts` | ما دخل الحساب وما خرج ولماذا — والحكم `READY`/`PARTIAL`/`BLOCKED` |
| `src/lib/inventory/engine.ts` | المحرّك النقيّ، ونسختُه تُحفَظ مع كلّ جردٍ مقفَل |
| `src/lib/sales/columns.ts` | ترويسةُ ملفّ فودكس تُفهَم بالاسم لا بالموضع — وما لم يُفهَم يُعلَن |
| `src/lib/sales/foodics-excel.ts` | محوِّلُ فودكس بصيغتيه، والهويّةُ من البيانات لا من الملفّ |
| `src/services/sales-import.service.ts` | ثلاثُ طبقاتٍ تمنع التكرار، **ولا صفَّ يُرمى صامتاً** |
| `src/services/inventory.service.ts` | جمعُ الوقائع ودورةُ الجرد — والمقفَلُ يُقرأ ولا يُعاد حسابُه |
| `src/services/recipe.service.ts` | النسخةُ تُغلَق سابقتُها ثمّ تُفتَح — ومؤثِّرُ `036` يمنع التداخل في القاعدة |

## القرارات وأسبابها

- **الخادم لا يثق بالمتصفّح.** كان `/api/archive` يحفظ `isTaxValid` كما أرسله المتصفّح. صار `reviewConfirmed()` يشتقّه من جديد.
- **الفشل يُسمَع.** كانت الفاتورة التي يتعذّر قراءة إجماليها تُؤرشَف بصمت وتُعرض «نجحت». صارت تُرفض بـ409.
- **الاستيراد منيع من التكرار** بثلاث طبقات: بصمة الملف، ومعرّف الحركة الخارجي، وبصمة المستند (`documents_sha_uniq` جزئي `WHERE status <> 'REJECTED'`). السبب: كشف بنكي استُورد ثلاث مرّات — ١٤٢٨ حركة صارت ٤٢٨٤.
- **بصمة الحركة مفهرسة بالتكرار** (`row_number()`) كي تنجو الأسطر المتكرّرة فعلاً داخل الملف نفسه.
- **الصنف يُفتَح بمفتاح `${supplierId}::${اسم}`** لا بالاسم وحده. السبب: «العنب» من لافا كمبوتشا، ومن محمصة الغربية كيلو بنّ — فأنتج دمجهما «توفيراً» وهمياً بـ٢١٢٢ ريالاً.
- **فترة كشف المورّد تُشتقّ من تواريخ أسطره** لا من اسم الملف. السبب: كشف Olive Leaves تراكمي (مايو–أغسطس) فأنتج ٣٦ فاتورة «مفقودة» كذباً.
- **نافذة مطابقة الفاتورة ±٧ أيام** — سطر بتاريخ ٢٣ يخصّ فاتورة بتاريخ ٢٦.
- **المورّد المكرَّر يُدمَج ويُعطَّل، ولا يُحذف** (`npm run db:merge`).
- **الذكاء الاصطناعي لا يخترع رقم فاتورة.** اختلق النموذج `TPH-20260521` بثقة ١٫٠٠. صار محظوراً في الموجِّه، ولا يُؤخذ رقم من النموذج لمستندات الأرشيف.
- **المصروف الفعلي لا يشمل سداد المورّد** — وإلّا حُسب مرّتين. وما يقول وصفه «شراء بضاعة» يُستبعَد ولو صنّفته القاعدة راتباً؛ ولا يُصحَّح التصنيف آلياً، بل يُعرَض التناقض.
- **عرض الصفحة يتبع ما فيها** — `form` للنماذج و`page` للعادي و`wide` للوحات والجداول. كان الكلّ `max-w-5xl`.
- **الثوابت المالية تُفرَض في القاعدة لا في الكود وحده** — `npm run db:verify` يثبت أنّها ترفض فعلاً. والقيد بلا اختبار ادّعاء.
- **يُتسامح بريالٍ في فرق (صافي+ضريبة) عن الإجمالي** — المورّد يُسقط كسور الريال والمطبوع هو الملزِم. القيمة في `TOTAL_ROUNDING_TOLERANCE_MINOR`، وهي نفسها في القاعدة.
- **الكتابة لا تُحرَس بصلاحية قراءة** — `bank:edit` و`expense:edit` منفصلتان عن `bank:view` و`amounts:view`.
- **محتوى المستند بيانات لا تعليمات** — الموجِّه يعلنها صراحةً في أوّله.
- **المرجع في تسجيل المطابقة مؤيِّد لا نافٍ** — أوصاف الأهلي تحمل مراجعه هو لا أرقام فواتير المورّدين، فعدم تطابقها كان يسقف كل مطابقة عند ٠٫٨٣ فتستحيل التلقائية بحكم المعايرة لا بحكم الشكّ.
- **لا مطابقة تلقائية إلّا بشرطين**: الدرجة فوق الحدّ **والفارق** عن المرشّح التالي فوق حدّه. أثبِت المطابقة، لا تجدها.
- **عمود المستفيد يُقرأ من الكشف** — كان الاستيراد يكتب فيه اسم المورّد المطابَق، فإن لم تُطابَق الحركة بقي فارغاً: دائرةٌ مغلقة على نفسها.
- **الأدلّة تُحفَظ بنصّها** مع القرار والدرجة — رقمُ الثقة الثابت لا يقول شيئاً بعد شهر.
- **كل مطابقة قابلة للتراجع** — `‎/api/match-undo` يفكّ التخصيصات ويحذف الدفعة إن لم تعد تفسّر حركة، ويكتب ذلك في سجلّ التدقيق.
- **مصدر قرارٍ واحد.** خرج `matchBankTransactions` من مسار الاستيراد نهائياً. كان يعمل بجانب المحرّك الجديد: هذا يكتب المال وذاك يكتب أدلّةً فوقه — فتُحفَظ حركةٌ بدرجة ٩١ ونتيجةِ «فاتورة بعينها» وحالتُها `UNMATCHED`. والاحتياط `engine ?? old` يعني مصدرَي حقيقة، وهو ما يُنتج التناقض لا يُصلحه.
- **الكتابة من `planned` وحده** — والاقتراح لا يُكتَب، ينتظر تأكيداً. والمعاينة تعرض ما سيُكتَب فعلاً.
- **شهر الدفعة هو الأحدث بين فواتيرها** لا شهر أوّلها، والشهور كلّها تُحفَظ.
- **الكشوف تستعمل نواة البنك نفسها** — `reconcile()` واحدة، ولا مطابقان مختلفان لعملٍ واحد. وحالات السطر صارت ثمانياً: مكرَّر، وتنازع مرجع، وإشعار دائن، واختلاف تاريخ…
- **حكم الحَكَم اقتراحٌ لا مطابقة.** النموذج يرجّح بين مرشّحين حُسبوا، ولا يقرّر مالاً. ولا يخترع مرشّحاً: يختار من قائمةٍ بمعرّفات مختصرة، ويُتحقَّق من حكمه في الخادم.
- **الحلّ التقريبيّ لا يُطابَق تلقائياً** — حين تنفد ميزانيّة المحسِّن يصير التلقائيّ اقتراحاً، فقولُ «تمّت المطابقة» عن حلٍّ لم يُثبَت أنّه الأفضل ادّعاء.
- **`beneficiary_raw` في الصفوف القديمة ملوَّث** — كان يحمل اسم المورّد المطابَق لا المستفيد. فلا يُبنى عليه اسمُ جهة؛ يُؤخذ من الوصف قبل `BEN ID`.
- **المورّد المسجَّل مرّتين يمنع المطابقة** — دفعةٌ لـ«سرد كو» وفاتورةٌ لـ«سرد للتجارة». يكشفه `npm run db:split-check` بدليلٍ ماليّ لا بتشابه اسم.
- **`as` تُسكت المترجم ولا تُصلح اختلافاً.** ذاكرة المستفيدين كانت تُسنِد `TxCategory` إلى `TxKind` بـ`as`، فخرجت كل جهةٍ متعلَّمة من مطابقة الفواتير **صامتةً** — والتعلّم يُنقص المطابقات بدل أن يزيدها. الترجمة صريحة الآن في `apply.ts`، ولها اختبارٌ يمرّ على كل باب وكل نوع.
- **كل ما يُحتمَل أنّه سداد يحمل حالةَ قرار** — وإلّا قالت الخلاصة «٨٥ تحتاج مراجعة» ولم يجدها الطابور.
- **قواعد البنك تعمّ ولا تخصّ عمليةً واحدة** — كانت ٣٢ قاعدة تحمل كلٌّ مرجع حوالة بعينها.
- **فرادة الحركة مقيَّدة بحسابها** — بصمةٌ فريدة في النظام كلّه تجعل حوالتين متطابقتين من حسابين تبتلع أولاهما الأخرى. والنطاق في القيد لا في البصمة، و`coalesce(bank_account_id,'~')` مقصود: `NULL <> NULL` يفتح باب التكرار بدل أن يسدّه.
- **تمثيلٌ واحد للهوية يُعدّ به ويُبصَم به** — كان العدّ بالوصف الخام والبصمة بالموحَّد، فحركتان يفرّقهما فراغٌ مزدوج تُبتلَع إحداهما.
- **الاسم ليس هويّة** — القاطع (حساب · آيبان · هوية · تاجر) يُحتكَر ويُعلَن تضاربه، والظنّيّ (اسم · مرجع) يُشترَك؛ وإن اشتُرك سقط من الذاكرة لأنّه لم يعد يدلّ على واحدة.
- **التسوية معادلةٌ لا عدُّ مطابقات** — «طوبقت ٣٠١ حركة» قد تكون مطابقةً تامّة على كشفٍ ناقص. والفجوة والفرق مانعا إقفال.
- **المال الداخل ليس سداداً** — كانت الحركة الواردة تمرّ إلى المطابقة، فيُنشَأ سدادٌ من مالٍ دخل وتُقفَل فاتورةٌ لم تُدفَع. كشفته مصفوفة التسوية.
- **الزمن ذو اتّجاه** — تصل الفاتورة ثمّ تُدفَع. والفاتورة بعد الدفعة نافذتُها سبعة أيام بنصف درجة: ممكنةٌ لا مرجَّحة.
- **بركة البحث تُرتَّب بالصلة لا بالحجم** — والقصّ ليس خطأً؛ الخطأ أن يقصّ بمعيارٍ لا صلة له بالسؤال. وما جاوز الدفعة يخرج: حذفُ المستحيل لا تقريب.
- **الرسم يخرج من القسمة قبلها** — وإلّا نُسب إلى المورّد مالٌ ذهب إلى البنك، وظهرت الدفعة «فائضة» فرُفعت إلى المراجعة بلا سبب.
- **الردّ لا يحذف** — الدفعة المردودة تبقى `REVERSED` بسببها ومن ردّها. والحذف يجعل الفاتورة تعود مستحقّةً بلا سببٍ ظاهر فتُدفَع مرّتين.
- **الإقرار الجماعيّ يُعيد الحساب** — الخادم لا يأخذ من المتصفّح إلّا معرّفات الحركات، ويحسبها مجتمعةً فلا تُحجَز فاتورةٌ لاثنتين.
- **ما قُرئ بصرياً لا يُحسَم تلقائياً** — كالحلّ التقريبيّ: حسابٌ صحيح على مدخلٍ غير مثبت.
- **حدود القراءة تُعرَض دائماً لا عند الخطأ** — محوِّلُ بنكٍ لم يُجرَّب حدٌّ معلوم سلفاً لا خطأٌ وقع.
- **المقياس لا يقيس نفسه بنفسه** — الدقّة تُحسَب على ما حكم فيه إنسان وحده، والاستدعاء يُعلَن أنّه لا يُقاس.
- **قيدٌ يحذف بيانات مالية يُفحَص أثرُه على الإنتاج قبل كتابته.** قيدُ ازدواج المصروف في `016` كُتب ثمّ **سُحب**: فُحص فوجِد أنّه سيحذف ثلاثة مصروفات حقيقية — فاتورتَي كهرباء وفاتورةَ اتصالات في يومٍ واحد بمبالغ متطابقة، لا يفرّقها إلّا مرجعُ السداد الواقع بعد الحرف الستّين في وصف الأهليّ. والأعمق: حدثان لهما **أثران مختلفان** (حركتان بنكيّتان ببصمتين) حدثان، مهما تطابق ما عداهما. فصار الازدواج كشفاً يُعرَض لا قيداً يحذف.
- **البصمة تُلبَّد ولا تُقتطَع** — القصّ لتقصير المفتاح يُسقط ما يميّز.
- **«لم يُفحَص» يمنع الجاهزية كما يمنعها «فشل»** — بوّابةٌ تعدّ غير المفحوص ناجحاً تُنتج ثقةً بلا سند، وهي أخطر من ألّا تكون هناك بوّابة.
- **العزل يُثبَت بمعرّف العنقود لا باسم المضيف** — لنقطة Neon الواحدة أسماءٌ مجمَّعة وغيرُ مجمَّعة، فمضيفان مختلفان قد يشيران إلى قاعدةٍ واحدة. و`/api/ops/db-identity` يُفتَح في البيئتين ويُقارَن.
- **الشهادة تُلغي أثرها** — كلّ سيناريو ماليّ يجري في معاملةٍ تُلغى، فيُختبَر المسار الحقيقيّ على المخطّط الحقيقيّ بلا أن يُمَسّ ريال. ولو كُتب ثمّ حُذف لبقي أثرٌ في سجلّ التدقيق، وهو جدولٌ لا يُحذَف منه شيء.
- **الاختبارات النقيّة لا تُثبت أنّ النظام يعمل.** ١٢٠١ اختباراً تمرّ في أقلّ من ثانية — لأنّ ولا واحدٍ منها يلمس قاعدةً أو صفحة. وقد مرّت كلّها بينما أحد عشر كشفاً بلا سطر، و`classification_source` فارغ في ١٤٢٢ حركة، و`decision_history` فارغ تماماً. **الأخضر يقول إنّ القطع سليمة، لا إنّها موصولة.**
- **الشيفرة تسبق البيانات فتفترق عنها.** آخر استيراد بيانات حقيقية كان ٤ سبتمبر ٢٠٢٦، وبعده ٤٢ كوميتاً من أصل ٧٢. فكلّ ما بُني بعده لم يمرّ على ريالٍ واحد: لذلك كانت جداول `decision_history` و`adjudications` و`bank_accounts` و`reconciliation_periods` و`statement_lines` فارغة كلّها. **من يبني طبقةً جديدة يُعيد تشغيلها على البيانات القائمة في الكوميت نفسه، وإلّا فهي دعوى.**
- **نصوص الإصلاح تحفظ ما يحفظه مسار الاستيراد — كاملاً.** كان `db:rematch` يكتب الأدلّة والقرار ويُسقط `classification_source` و`reason` و`version` و`lifecycle`، ولا يكتب `decision_history`. فبقيت هويّة التصنيف فارغةً في ١٤٢٢ حركة رغم أنّ الاستيراد يكتبها — لأنّ الذي صنّف فعلاً هو النصّ لا الاستيراد. **نصفُ كتابةٍ أسوأ من لا كتابة: تُظهر الحقل موجوداً وفارغاً فيُقرأ «لم يُصنَّف».**
- **الكشف يُقيَّد ولو لم يُقرأ رصيده** — الفاتورة بلا مبلغٍ لا معنى لها، أمّا الكشف فهويّته مورّدُه وفترتُه. وكان يُشترَط له مبلغٌ في الاسم، فسقطت ثلاثة كشوف مؤرشفة من الجدول تماماً وظهر «أوراق الزيتون» في «لم يصل كشفه» وقد أرسله مرّتين.
- **أسطر الكشف تُحفَظ عند الأرشفة لا عند المطابقة** — كان النموذج يستخرجها ثمّ تُرمى، ويُحفَظ الرصيد الختاميّ وحده. فبقيت `statement_lines` فارغةً في أحد عشر كشفاً، والمطابقة مستحيلةٌ لأنّ ما يُطابَق غير موجود — ثمّ تقول البوّابة «لم يُطابَق منها واحد» كأنّ التقصير من صاحب المقهى.
- **رصيد الكشف المجهول ليس صفراً** — كان العمودان `NOT NULL DEFAULT 0`، فكشفٌ لم يُقرأ رصيدُه يُحفَظ «لا يطالبنا بشيء»، ثمّ تُحسَب المعادلة على ذلك الصفر فيُقال «حساب المورّد لا يستقيم» — ويُتَّهم المورّد بخطأٍ مصدره أنّنا لم نقرأ. صارا يقبلان `NULL` في `018`.
- **الشهر الجاري يُقارَن بمثله** — كانت الصفحة تجمع سبتمبر حتّى اليوم وتقارنه بأغسطس كاملاً، فتقول في السادس من كلّ شهر «▼ ٩٨٪ أنفقتَ أقلّ». والنقص يومٌ لا سلوك؛ والإنذار الكاذب الدوريّ يُفقد الثقة بما عداه. فيُقصّ السابق عند اليوم نفسه ويُعلَن الأساس: «عن أوّل ٦ أيّامٍ من أغسطس».
- **عدد الهجرات يُقرأ من المجلّد لا يُكتَب رقماً** — كان `EXPECTED_MIGRATIONS = 17` في البوّابة، فأوّل هجرةٍ تُضاف تقول «١٨ من ١٧ — فشل»: تُعاقِب على التقدّم ثمّ يُسكِتها من يزيد الرقم بيده، فتصير رقماً يُجامَل لا فحصاً يجري. والمقارنة بالأسماء تكشف الاتّجاهين — ومنهما هجرةٌ طُبّقت وملفّها غائب، وهي الأخطر.
- **الهويّة تُمسَك من النمط حين لا اسم ولا حساب.** كانت الذاكرة تُمسك الحركة من اسم المستفيد أو رقم حسابه، وحركاتُ أحمد المجهولة الخمس والثمانون **كلّها** بلا اسمٍ وبلا حساب: سدادٌ حكوميّ عبر «سداد»، ورسومُ شبكة، وأوصافٌ فارغة. فالمفتاح يخرج `null` لكلّها، و«يؤكّد مرّةً فيعمّ» لم يكن يعمّ على شيءٍ ممّا يحتاجه فعلاً. فصار الوصفُ بعد إسقاط أرقامه دليلاً — ظنّيّاً كالاسم: إن اشترك فيه اثنان سقط.
- **الاسم يُقرأ بالصيغة التي يُكتب بها.** كان يُحفَظ موحَّداً (`normalizeText`) ويُقرأ خاماً في `merchantKey`، فاسمٌ فيه همزةٌ أو تاء مربوطة لا يلتقي بنفسه — وهو أكثر الأسماء العربية. والتوحيد الآن في `identitiesOf` وحدها، يكتب بها المؤكِّد ويقرأ بها المصنِّف.
- **السؤال يُطرَح على المجموعة لا على الحركة.** كشفٌ فيه ستّون حركة وخمسَ عشرة منها لمورّدٍ واحد كان يعني خمسة عشر سؤالاً عن شيءٍ واحد — فيُترَك الطابور. وقياساً على معلَّقات أحمد: ١٥٣ حركة صارت ٤٧ سؤالاً. والخادم يُعيد اشتقاق المجموعة ولا يصدّق المتصفّح في أنّها متشابهة.
- **التأكيد يعمّ على القاعدة كلّها لا على الكشف القادم.** بعد الحفظ تُقرأ الذاكرة من جديد ويمرّ **المصنِّف نفسه** الذي يمرّ عليه الاستيراد على ما بقي. ولا يُمَسّ ما صنّفه إنسان.
- **`db:reclassify` كان يمحو التعلّم.** يستدعي `classify` بلا ذاكرة، فيُلغي في كل تشغيل كلَّ ما أكّده صاحب العمل — والمسارُ الذي يُفترَض أن ينشر التعلّم كان يُبطله. ويكتب `category` وحده تاركاً `classification_source` يقول شيئاً غير ما وقع.
- **تغييرُ دالّة الهوية يُبطل بأثرٍ رجعيّ كلَّ ما حُفظ بها.** خرج رقم الحساب من `transactionIdentity`، فصار للحركة الواحدة بصمتان — واحدة قبل التغيير وأخرى بعده — فلم يرَ الاستيراد الثاني الأوّل، ودخل كشفٌ كامل مرّة ثانية: ١٤١٣ حركة، ٩١٢٬٧١١ ريالاً. **والمنع صار على المفتاح الطبيعيّ نفسه** (حساب · تاريخ · مبلغ · اتجاه · وصف موحَّد · ترتيب) لا على بصمةٍ تُشتقّ منه: المفتاح الطبيعيّ ليس محسوباً فلا «نسخة» له تتغيّر. ويُثبته `db:verify` بمحاولة إدخالٍ تُرَدّ.
- **العدد الحقّ لحركةٍ هو أكثر ما قاله ملفٌّ واحد** — لا واحد، ولا مجموع الملفّين. فالكشف يذكر الحركة بعدد ما وقعت؛ فإن قال كشفان «مرّتين» فهي مرّتان لا أربع، وإن قال أحدهما «مرّة» فذاك نقصُ تغطيةٍ لا نفيُ وقوع. وبه يُحذف ما تكرّر بالاستيراد ويبقى ما تكرّر في الواقع — و**في كشف أحمد ٢٣ مجموعةً متكرّرة حقيقيّة**، لولا هذا التمييز لابتُلعت.
- **والحذف يُسبَق بدمج** — النسختان ليستا متطابقتين: الباقي (الأقدم) بلا «نوع العملية» في ١٤٣٢ حركة، والمحذوف يحمله. فحذفٌ بلا دمج يشتري إزالة التكرار بثمنِ حقلٍ لا يُستعاد إلّا بإعادة استيراد الكشف.
- **`occurrence` جزءٌ من المفتاح فلا تبقى فيه ثغرة** — صفٌّ باقٍ ترتيبُه ١ وقد حُذف صاحبُ الصفر يعني أنّ استيراداً قادماً يكتب الصفر فلا يصطدم بشيء، فتعود الحركة مرّتين. فالترقيم يُعاد بعد كل حذف، على مرحلتين لأنّ الفهرس فريد.
- **المقدار دليلٌ حين يصمت الوصف** — الصادر الأقلّ من عشرين ريالاً وليس ضريبةً رسمُ بنك. وموضعها بعد الكلمات مقصود: زكاةُ خمسة ريالات تبقى زكاة. ولها مصدرها `AMOUNT`، ولم تُدسّ تحت `KEYWORD` لأنّها ليست كلمة: من يقرأ «كيف عُرفت هذه؟» بعد شهر يجب أن يقرأ الحقّ.
- **ضريبةُ الرسم بابٌ غير باب الرسم.** وصفُ رسم القناة الرقمية ووصفُ ضريبته واحد — «CITY:Digital Channel» — ولا يفرّقهما إلّا «نوع العملية». فمن فحص الرسم أوّلاً نسب إلى البنك ما ذهب إلى الدولة (٣٥ حركة). والنظام يفصل أصلاً `POS_FEE` عن `POS_VAT`، فصار `BANK_VAT` هو الفصل نفسه في رسوم البنك. **والضريبة تُفحص قبل الرسم دائماً.**
- **صيغة الضريبة مضيَّقة عمداً**: «Tax» وحدها تقع في وصف زاتكا (`Zakat, Tax and Customs`) وهو سدادٌ حكوميّ بأربعة عشر ألف ريال — فلو أُخذت لصار سدادُ الدولة «ضريبةَ رسمٍ بنكيّ».
- **«نوع العملية» يصنّف حين يصمت الوصف.** `81140155-260626-POS 0` لا كلمة فيه، ونوعُه يقول «ضريبة عملية نقاط بيع فوري»؛ و`PoSMonthlyFeeSep…` لا يشبه صيغة الدفعات. وكان النوع يُقرأ للمطابقة ثمّ يُطرح من التصنيف — فبقيت حركاتٌ «غير مصنَّفة» وما يصنّفها مكتوبٌ في عمودٍ مجاور. **ويدخل النوعُ التعرّفَ ولا يدخل النمط**: هناك خاصّيةُ تصديرٍ تفرّق نمط الحركة الواحدة بين ملفّين، وهنا دليلٌ صريح على بابها.
- **القاعدة المقيَّدة بأوّل النصّ وآخره هشّة.** كان `^CITY:Digital Channel$` يُطابَق على `searchText` وهو يجمع الوصف والنوع والمستفيد — فالرسم الذي يُعرَف حين يغيب نوعه خرج مجهولاً حين حضر (بعد أن استعاده الدمج).
- **استعادةُ حقلٍ تكشف خطأً كان مستوراً.** لمّا أعاد الدمجُ «نوع العملية» إلى ١٤٠٩ حركات، خرجت ٢٥ حركة من «رسوم بنكية» إلى «غير مصنَّفة» — لا لأنّ شيئاً فسد، بل لأنّ تصنيفها كان صحيحاً بالمصادفة: قاعدةٌ هشّة طابقت نصّاً ناقصاً. **والانحدارُ الظاهر بعد إصلاحٍ يُفحَص، فقد يكون كشفاً لا عطباً.**
- **الطابور يقرأ الطبقة لا القرار القديم.** كان شرطُ «ما يحتاج قرارك» هو `match_disposition in ('SUGGEST','REVIEW')` — وهو عمودٌ عن مطابقة **فاتورة** لا عن معرفة الجهة. فمن عرّف حركةً وحدّث الصفحة وجدها تُسأل عنه ثانيةً: التعريف كُتب فعلاً، والعمود الآخر بقي كما هو، والطابور يقرأه. ثلاثٌ وأربعون حركة أكّدها أحمد بيده كانت تعود إليه كلّ مرّة، وأربعٌ وسبعون رسماً صُنّف تلقائياً كان يُسأل عنه. **والشرط صار الطبقةَ والمصدر**: ما قرّره إنسان لا يُسأل عنه، وما ليس سداد مورّد لا قرار فيه أصلاً. (٥٢ → ٥).
- **والقرار القديم يُطوى مع الجواب** — من قال «هذه رسمٌ بنكيّ» قال ضمناً إنّها ليست سداد فاتورة، فتُطوى `match_disposition`. ويطويها `db:reclassify` عن كلّ بابٍ ليس سداد مورّد.
- **مرجع العمليّة هويّةٌ فوق المفتاح الطبيعيّ.** المفتاح يمنع دخول الكشف مرّتين، ولا يمنع أن **يذكر الكشفُ الواحد الحوالةَ مرّتين** — يحميهما `occurrence` بوصفهما حقيقيّتين، وهو صحيحٌ في الرسوم (١٦ مجموعة `CITY:Digital Channel` متكرّرة في اليوم حقيقيّة) خطأٌ في الحوالات. والحوالة تحمل رقم عمليّة لا يتكرّر. **والقياس قبل القيد**: ١٢٤ حركة لها مرجع، والمكرَّر منها خمسة، كلّها بنفس المبلغ، ولا مرجعَ واحدٌ يخدم مبلغين — فالقيد الفريد لا يردّ حركةً حقيقيّة. وحركات الشبكة تُستثنى: «REFERENCE : 81140155 MC26 0811» رقمُ طرفيّةٍ لا عمليّة.
- **الموقوف يبقى بلا مرجع، ولا يُدَّعى له.** نسختان لعمليّةٍ واحدة كلتاهما مرتبطة بدفعة لا يقبلهما القيد، وكتابةُ المرجع لإحداهما ادّعاءٌ بأنّ الأخرى عمليّةٌ ثانية. فيبقى العمود فارغاً حتى يحسم إنسان — والفراغ يقول «لم يُحسَم».
- **نصٌّ يُشغَّل مرّتين فيعطي نتيجتين ليس نصَّ إصلاح.** `db:dedupe` كان يأكل دليلَه: بعد أوّل تنظيف تتوزّع النجاةُ على الملفّين، فيقرأ الشوطُ الثاني «أكثر ما قاله ملفٌّ واحد = ١» ويحذف ما أبقاه بالأمس — أربعةَ عشر صفّاً حقيقيّاً. فصارت الحقيقة أكبرَ الاثنين: ما يقوله ملفّ، وما قُبل من قبل (`max(occurrence)+1`).
- **اسم المستفيد يُقرأ من نصّ البنك، لا من عمودٍ يكتبه نظامُنا.** بُني التجميعُ على `beneficiary_raw` — وهو ملوَّث في **٣٧ صفّاً من ٤٦**: يحمل اسم المورّد الذي طابقه الاستيراد القديم لا المستفيد. فجُمعت أربع حوالات لأربع جهات — «أطوار العقارية» و«top taste» و«مؤسسة عمار» و«PURE BEVERAGE» — تحت اسمٍ واحد، **وقرارٌ واحد عليها كان سيُخطئ في أربع**. والقاعدة كانت مكتوبةً في هذا الملف وخُولفت. فصار `beneficiaryFrom` يقرأ الاسم من الوصف: بعد رمز الحوالة وقبل `BENBK`/`BEN ID`/اسمِ بنك المستفيد. ونصُّ البنك يسبق العمود ولا يُلغيه — والتلوّث يُعالَج في مصدره (`db:reclassify` يعيد كتابة العمود ويُفرّغ ما لم يقله البنك) لا بالالتفاف حوله في كل قراءة.
- **ولا يدخل العمودُ النمطَ أيضاً** — الاسم المُقحَم يتصدّر الكلمات الاثنتي عشرة فيطغى على ما يفرّق الحوالات.
- **ولا يُشتقّ اسمٌ من كلّ وصف** — «CITY:Digital Channel» رسمٌ لا مستفيد له، واشتقاقُ اسمٍ منه يصنع جهةً وهميّة تُجمَع تحتها حركات. فيُشترَط دليلٌ على وجود مستفيد: `BEN ID` أو `BENBK` أو «حوالات تحت الطلب» أو «رقم السداد».
- **التعميم يضاعف الخطأ كما يضاعف الصواب** — وهو أخطر ما في «عرّفه مرّة فيعمّ». فكلّ ما يُجمَع به يجب أن يكون خبراً من البنك لا استنتاجاً من عندنا.
- **عميلٌ في جدول الموردين.** «سبعة جرة — عميل» (واسمه الحقيقيّ «سابع جار») مسجَّل مورّداً، وأسماؤه البديلة فيها `Sales - Sabea Jar (فاتورة صادرة)` — أي أنّه يشتري من المقهى لا يبيع له. ولا جدولَ للعملاء بعد. فُصلت عنه أربع حركات نُسبت إليه خطأً، وبقي هو معلَّقاً لقرار أحمد.
- **هويّةٌ قُيِّدت بمعرفةٍ تتحسّن مع الوقت ليست هويّة — وهذا الدرس وقع ثلاث مرّات.** أوّلاً في دالّة البصمة (خرج منها رقم الحساب فبطل ما حُفظ بها)، ثمّ في نطاق الفرادة نفسه: فرادةُ الحركة مقيَّدة بحسابها (`014`) وهو صحيح — لكنّ الحساب **يُقرأ من الملفّ**. فالكشوف الأولى استُوردت ولا يُقرأ منها الرقم فحُفظت بحسابٍ فارغ، ثمّ صار القارئ يقرأه، فبُحث عن السابق **في نطاق الحساب الجديد** ولم يوجد شيء — لأنّ القديم كلّه في نطاق «المجهول». فدخل الكشف كلّه ثانيةً: ١٤٣٦ حركة، وصار الصادر ٧٣٦ ألفاً بدل ٣٦٨.
- **والفارغ ليس «حساباً آخر»، هو «لم نكن نعرف».** فالبحث عن السابق لا يُقيَّد بالحساب، ثمّ **يُتبنّى ما طابق**: يُكتَب له الحساب فينتقل من نطاق المجهول إلى نطاقه، ويحرسه القيدُ في القاعدة بعدها. ولا يُتبنّى إلّا ما طابق مفتاحاً في الكشف — تلك وحدها التي ثبت أنّها منه.
- **والإصلاح يُقابل واحدةً بواحدة لا يُبقي واحداً** — `db:repair-scope` كان سيأكل تسعةَ عشر صفّاً حقيقيّاً لو أبقى من كل مجموعةٍ صفّاً: المجموعة قد تحمل تكراراً حقيقيّاً (رسمُ القناة الرقمية مرّتين في اليوم). والدمج قبل الحذف كذلك: النسخة الجديدة قد تحمل عملَ إنسانٍ وقع عليها بعد رفعها — دفعةً رُبطت بها، أو تصنيفاً أكّده — فيُنقَل إلى الباقي قبل أن تُحذف.
- **الاستيراد صار مزامنة: «أهي عندنا؟» قبل «ما معناها؟».** كان المسار يحسب لثلاثمئة حركة مرشّحيها ودرجاتِها ويستدعي الحَكَم، ثمّ يكتشف أنّ مئتين وتسعين منها مقيَّدة. فصار الترتيب: اقرأ ← عرّف الحساب ← اعرف الهويّات ← افصل المعروف ← **سوِّ الجديد وحده**.
- **هويّة الحركة طبقتان، والوصف ليس أُولاهما.** المرجعُ الذي يعطيه البنك للعمليّة هو هويّتها؛ ووصفُها يتغيّر بين تصديرٍ وآخر والعمليّة واحدة. فمن جعل الوصف هويّةً أدخلها مرّتين. والوقائع (تاريخ · مبلغ · اتجاه · وصف موحَّد) طبقةٌ ثانية لما لا مرجع له.
- **والمراجع تُقابَل مجموعةً لا نصّاً واحداً** — كشفٌ يكتب «مرجع…» ورمزَ الحوالة معاً، وآخر يكتب المرجع وحده. فلو رُبطا في نصٍّ واحد لاختلف النصّان لحوالةٍ واحدة. ومرجعٌ واحد مشترك يكفي.
- **«رقم السداد» ليس مرجع عمليّة** — هو رقم المشترك عند الجهة (عدّاد الكهرباء، رقم الهاتف، الرقم الضريبيّ)، ويتكرّر في فاتورة كلّ شهر. فلو عُدّ هويّةً لابتُلعت فاتورةُ الشهر الثاني بوصفها «مقيَّدة»، ومالٌ خرج فعلاً لا أثر له. **كشفه الفحص**: ثلاث حركات سدادٍ حكوميّ تغيّر تصنيفُها بمجرّد قلب ترتيب الصفوف — والترتيب لا يغيّر حقيقة.
- **المفتاح لا يحمل الحساب، والنطاق يقع في اختيار ما يُقابَل** — لو دخل الحسابُ المفتاحَ لتغيّر المفتاح بتغيّر ما نعرفه عن الحساب. والوارد المجهولُ حسابُه يُقابَل بالكلّ: لا نعرف أنّه من حسابٍ آخر، والادّعاء بذلك يُضاعف المال.
- **والترتيب آخرُ شيء لا أوّلُه** — `occurrence` فاصلٌ بين متطابقتين لا مرجع لهما، لا هويّة. وبه تنجو الحركتان المتطابقتان الحقيقيّتان (١٨ مجموعة في كشف أحمد)، ولا يُعتمَد عليه في تمييز حركةٍ عن أخرى.
- **الالتباس يُعرَض ولا يُحسَم** — واردٌ بلا مرجع تطابق وقائعُه حركةً لها مرجع: أهي هي صُدِّرت بلا رقمها، أم عمليّةٌ ثانية تشبهها؟ لا تُدسّ في المقيَّد، ولا تُضاف، ولا يُحذَف طرف. تُعرَض في المعاينة ليقرّر إنسان.
- **ومرجعان مختلفان ليسا التباساً** — البنك يعطي كلّ عمليّةٍ رقمها، فاختلافُ الرقمين اختلافُ عمليّتين.
- **`db:identity` يعرض ولا يحذف** — مكرَّرٌ قاطع، ومحتمل، وتكرارٌ مشروع، وفريدٌ مؤكَّد. وتنظيفُ المال قرارُ إنسان: يُعرَض بدليله ويُنتظَر فيه.
- **المورّد محور المستحقّات، لا الفاتورة.** كان النظام يسأل عن كل حوالة «أيّ فاتورة تفسّرها؟» فإن لم يجد مبلغاً مطابقاً وقف — ولا يعطي فعلاً. وبعض مورّدي أحمد لا يعطون فاتورةً أصلاً: كشفُ حسابٍ أو ورقةٌ باليد. فالحوالة **سدادٌ لحساب المورّد**، والفواتير تفصيلٌ داخله: تُقيَّد الدفعة، وتُوزَّع على المفتوح، وما بقي يبقى «غير مخصَّص» — وهي حالٌ صحيحة لا نقص.
- **وسياسة التخصيص معلَنة: الأقدم أوّلاً.** لا يكسرها إلّا رقمُ فاتورةٍ في الحوالة أو توزيعٌ يكتبه صاحب العمل بيده — ولا يخترعها المحسِّن ليرفع درجة. ولا تُسدَّد فاتورةٌ لم تكن قد وُجدت: النافذة سبعةُ أيّامٍ بعد الحوالة (الفاتورة تُكتب بتاريخ التسليم وتصل بعده)، وما جاوزها سدادٌ مقدَّم يقرّره إنسان.
- **والخادم يُعيد الحساب لحظةَ السداد** — لا يأخذ من المتصفّح إلّا معرّف الحركة. لأنّ ما رُجّح لحظةَ الاستيراد قد تكون فاتورتُه سُدّدت بعده من دفعةٍ أخرى.
- **المجموعة تُسدَّد في معاملةٍ واحدة لا في خمسةَ عشر طلباً.** كانت الشاشة ترسل حركةً حركة، فإن نجح ثمانية وفشل التاسع بقيت المجموعة نصفَ مقيَّدة ولا أحد يعرف أين وقفت. والمستحقّ يتناقص بين حركةٍ وأختها **داخل المعاملة نفسها**، فلا تُخصَّص فاتورةٌ واحدة لدفعتين.
- **وسباقُ دفعتين على فاتورةٍ واحدة محسومٌ في القاعدة لا في الترتيب** — مؤثِّرُ `007` يرفض تجاوز إجمالي الفاتورة. ويثبته `db:verify` بمحاولة إدخالٍ تُرَدّ: لولاه لصارت الفاتورة مسدَّدة مرّتين ولخرج ستّة آلاف عن ثلاثة.
- **السياسة تُقال للمستخدم قبل الضغط** — «لا رقمَ فاتورةٍ في الحوالة، فتُوزَّع بالأقدم أوّلاً»، ومعها ما سيُخصَّص وما سيبقى على الحساب. والسياسة المخفيّة تُنتج ثقةً بلا فهم.
- **والذاكرة تُختبَر على المستقبل لا على الكشف نفسه** — «أعرّفه مرّة» تعني أن يأتي كشفُ الشهر القادم فيُعرَف بلا سؤال، لا أن يختفي التنبيه من هذه الشاشة.
- **خوارزميّةُ هويّةٍ واحدة في النظام.** بقيت في `identity.ts` خوارزميّةٌ ثانية كاملة (`transactionIdentity` · `assignIdentities` · `naturalKey` · `countByNaturalKey` · `unseenRows`) لا يستعملها إلّا اختباراتها. وهذا أخطر أشكال الدَّين: محرّكان، أحدهما موصول والآخر **يبدو** موصولاً — ومراجعٌ خارجيّ قرأ الملفّ فحكم بأنّ العطب لم يُصلَح أصلاً. **فحُذفت.** والملفّ صار ٨٧ سطراً: بصمةُ الملفّ ومراجعُ العمليّة.
- **`external_id` يحمل الهويّة نفسها لا مفتاح الصفّ.** كنتُ أكتب فيه `row-5-4` — وعليه فهرسٌ فريد مقيَّد بالحساب. فاستيرادُ ملفٍّ ثانٍ تتكرّر فيه أرقامُ الصفوف **تُرَدّ صفوفُه الجديدة صامتةً**: حركاتٌ حقيقيّة تختفي بلا شكوى. عطبٌ أدخلتُه أنا، وكشفه تتبُّعُ بندٍ في مراجعةٍ خارجية.
- **والمتساهل لا يُستعمَل إلّا بدليلٍ على أنّهما عمليّة واحدة** — يُسقط الأرقام كلّها، فـ«TRF ABC 123456» و«TRF ABC 654321» يتساويان فيه. فالشرط أن يحمل أحدُ الطرفين مرجع عمليّة؛ وإن خلا الطرفان فلا دليل، والوارد جديد. **والنقص أسوأ من التكرار: التكرار يُرى في الرصيد والنقص لا يُرى.**
- **ومرجعُ العمليّة فريدٌ داخل الحساب لا في النظام** — وبعضُ القيود يُثبَت بالقبول لا بالرفض: `db:verify` يثبت أنّ المرجع نفسه في حسابٍ آخر **يُقبَل**. والقيد الذي يرفض كلّ شيء «يعمل» بالمعنى الساذج ويمنع الواقع.
- **الوقوف بمهلةٍ معلَنة خيرٌ من قتلٍ صامت.** مزامنة الدرايف كانت تتجاوز الستّين ثانية فيقتلها المزوّد ويردّ صفحةً نصّية، فتنفجر الشاشة بـ«Unexpected token 'A'» — رسالةٌ عن المحلّل لا عن العطب. فصار المشي يقف عند أربعين ثانية ويقول أيّ الأشهر بقيت، والشاشة تستأنف وحدها. **وكلُّ واجهةٍ تقرأ ردّاً يجب أن تقرأه نصّاً قبل أن تدّعي أنّه JSON.**
- **والفحص عند رأس الوحدة لا داخلها** — الشهر يُتمّ أو يُؤجَّل كاملاً، فلا يبقى نصفُ شهرٍ لا يعرف أحدٌ أين وقف.
- **لا يُقترَح اسمٌ إلّا لملفٍّ لا يُقرأ اسمُه.** أوّل صياغةٍ كانت تقارن الاسم بالمبنيّ حرفاً بحرف، فتقترح على «Ganache-AGK_Statement_May» أن يصير «Ganache_Statement» — فتمحو «AGK» و«May»، وهما تفصيلٌ كتبه إنسان. والقياس على الأرشيف كشفه: ثمانية اقتراحاتٍ كلُّها تخسر معلومة. **فالشرط أن يعجز القارئ، لا أن يختلف المبنيّ** — وصار المقترَح ملفّين اثنين، وهما اللذان اشتكى منهما أحمد.
- **السؤال يقع حيث تُعرَف إجابته.** المزامنة تكتشف الملفّ الجديد وتقرأ محتواه فتعرف مورّده وتاريخه وإجماليّه — **في تلك اللحظة**. وكان سؤال «أأوحّد اسمَه؟» في شاشةٍ أخرى تنظر إلى **المسجَّل**، والجديد لم يُسجَّل بعد: فيرى أحمد في المزامنة ستّة أسماء غلط، ويرى الفحصُ «لا شيء». خيطان لا يلتقيان، والعمل بينهما يضيع. فصارت المزامنة تقترح التسمية لما سجّلته للتوّ.
- **لا يُحاسَب القديم بما لم يكن موجوداً** — `decision_history` وُلد في `013`، فحركةٌ قُيّدت قبله لا أثر لها فيه: تاريخٌ لا عطب. والفحص يقتصر على ما بعد أوّل سجلٍّ فيه.
- **الشاشة التي تَعُدّ العمل ولا تُنقصه أسوأ من غيابها.** كان طابور المراجعة ثلاث قوائم بلا زرٍّ واحد — «يُراجَع» توصف بأنّها «تحتاج عينك» ولا تُعطي العين ما تفعله، و«يُحسَم» توصف بأنّ «تعريفَها يسري على أمثالها» ولا موضع فيها للتعريف. ‏٣٩٢ بنداً بقيمة ٩٨٣ ألفاً معروضةً للنظر. فصار **لكلّ بندٍ فعلُه**، والقائمة تُوسَّع أربعين أربعين بدل أن تُقصّ عند الأربعين بلا سبيلٍ إلى ما بعدها.
- **عددٌ واحد للعمل الباقي.** كان له ثلاثة أجوبة في ثلاث شاشات — ٨٥ و١٩٤ و٤٠٠ — تحت عناوين مترادفة في العربية. وقد تكون الثلاثة صحيحةً وتقيس أشياء مختلفة، لكنّ **العدد الذي يتغيّر بتغيّر الصفحة يفقد صفته عدداً**، ومعه تسقط الثقة بما عداه. فصار `countPendingWork()` مصدراً واحداً يُعرَض في القشرة، وسُمّي ما عداه باسمه الدقيق.
- **الخطّ يُقاس ولا يُفترَض.** تعليق `.nums` كان يَعِد بأنّ «الأرقام بعرض ثابت حتى تصطفّ الخانات»، وخطّ ثمانية لا يحمل الأرقام الجدوليّة أصلاً: «11» بعرض ‎9.08px‎ و«88» بعرض ‎15.31px‎ — فالخاصيّة تُحسَب ولا أثر لها. **والتعليق الذي يصف نيّةً غير محقَّقة أسوأ من غيابه**، لأنّ من يقرؤه يحسِب المسألة محلولةً فلا يفحصها. فطُلب خطُّ الواجهة النظاميّ للأرقام وحدها — بلا تبعيّةٍ جديدة، وبمقاسٍ لا يبين فرقُه.
- **`align: end` في العربية هو اليسار** — فكانت أعمدة المال تلتصق بحافّتها اليسرى وتتراقص فواصلها. والمال يُصفّ على آخر خانةٍ منه لا على جهةٍ من الجدول: `nums-col` تعطيه `direction: ltr` مع `text-align: right`.
- **فعلٌ واحد باسمٍ واحد.** كان الإنسان يوافق بأربعة أفعال — إقرار وختم وتأكيد واعتماد — ثلاثةٌ منها في بطاقةٍ واحدة. والمستحقُّ للمورّدين بستّ تسميات، منها الجملة نفسها بشدّةٍ وبغيرها. فمن ينتقل بين شاشتين يظنّ أنّه انتقل بين مفهومين.
- **مقدّمةُ الصفحة تقول ما هي، لا لماذا صُمّمت هكذا.** كانت خانة `intro` تخلط ثلاثة أعمال: وصفَ الصفحة، والدفاعَ عن قرارٍ لم يعترض عليه أحد، وتفصيلَ التنفيذ («مفروضاً بمشغّلات في القاعدة»). والأخيران يخاطبان من بنى النظام — **ومكانهما التعليق في الشيفرة، وهو مكتوبٌ فيها بأجودَ ممّا في الواجهة**.
- **الرسالة التي يقرؤها المحجوب تُكتب له لا لمن حجبه.** خمسٌ من سبع رسائل دخولٍ كانت تقول «راجع متغيّرات جوجل في الخادم» و«راجع Client ID» — أوامرُ لا يملك قارئها تنفيذها، وهو بالتحديد **من لا يستطيع الدخول ليصلح**. فتفصيلُ الإصلاح إلى سجلّ الخادم، ويبقى للقارئ خبرٌ وطريقٌ ورمزٌ ينقله.
- **أضعفُ نصٍّ في الشاشة كان يحمل أكثر ما يُقرأ فيها.** `--muted` عند ‎3.38:1‎ فاتحاً و‎3.08:1‎ داكناً — وعليه تسميةُ كل بطاقة رقم، ورأسُ كل عمودٍ في كل جدول، وتسميةُ كل لسانٍ في شريط الجوّال. وأسوأ حالةٍ كانت مجموعَ كلّ قسمٍ في الطابور: المالُ نفسه.
- **الفشل لا يكون أهدأ من النجاح.** رسالة فشل الإقرار الجماعيّ كانت تطلب `border-bad/40 bg-bad-bg` — ولا رمز اسمه `bad` في نظام الألوان، فتخرج شفّافةً بحدٍّ أسود بينما النجاح أخضر. **والصنف اللونيّ الذي لا رمز له لا يُولَّد ولا يُنبَّه عليه**، فيمرّ في المراجعة ويُكتشَف عند أوّل فشلٍ حقيقيّ.
- **للحركة سؤالان، وجوابُ الأوّل لا يطوي الثاني.** «من هذه الجهة؟» يجيبه التصنيف، و«أيّ فاتورة تسدّدها؟» يجيبه قيدُ الدفعة. وكان شرطُ الطابور يطويهما معاً — وذلك صحيحٌ في كلّ بابٍ إلّا سدادَ المورّد: من قال «هذه رسمٌ بنكيّ» أنهى أمرَها، ومن قال «هذه لأوراق الزيتون» **بدأ** عملَها. فخرجت من الطابور **عشرون حوالةً بـ٧٢٬٩٠٤٫٦٨ ريالاً** في اللحظة التي عرّفها أحمد فيها، ولا دفعةَ لواحدة: تقول صفحةُ «مصنع الكوب الذهبي» إنّ عليه ٢٥٬٥٠٩٫٨٨ وقد سدّدها على أربع حوالات. **والعملُ الذي يختفي لا يُنجَز.**
- **وزرٌّ لا يعمل أسوأ من زرٍّ غائب.** كان «أكّد» يُعرَض على حركةٍ ليست اقتراحاً فيردّها الخادمُ بحقّ. والفعلُ الصحيح — «قيّدها على حسابه» — كان في شاشة البنك وحدها، وشاشةُ المراجعة هي التي تُسمّى طابور المراجعة. فصار الزرّ يتبع ما تحتاجه الحركة، ومعه السياسةُ مكتوبةً قبل الضغط.
- **الريال الواحد يأتي من بابين فيُقيَّد مرّتين.** إيصالُ السداد في الدرايف يُنشئ دفعة، وحركةُ الكشف نفسُها تُنشئ أخرى، ولا فاحصَ بينهما. وفي بيانات أحمد أربع: **١١٬٠٨١٫٥٢ ريالاً** محسوبةً مرّتين، تُظهر المورّد مأخوذاً منه ضعفَ ما أخذ فيُطالَب بردٍّ لا يستحقّه. فصار `findPaymentTwin` يُسأل قبل الإنشاء: **الإيصالُ دليلٌ على دفعة لا دفعةٌ ثانية**، والكشفُ يتبنّى ما قُيّد من إيصاله ولا ينسخه.
- **والتوأمة كشفٌ لا قيد.** مورّدٌ يُدفَع له مرّتين في اليوم بنفس المبلغ حقيقةً — بيكوف يُسدَّد بمئةٍ وخمسين لكلّ فاتورة. فلا يُمنَع الإنشاء؛ يُرَدّ ما وُجد بحاله (أله حركةُ بنك؟ أخُصّص منه شيء؟) ويقرّر المستدعي. ولا يُتبنّى إلّا ما لا حركةَ له ولا تخصيص.
- **والدفعة بلا أصلٍ تُعرَض ولا تُطوى.** لا مستندَ لها ولا حركةَ بنك: قد تكون وسماً يدويّاً صحيحاً، وقد تكون نصفَ دمجٍ قديم. وطيُّها يُعيد الفاتورة مستحقّةً بلا سببٍ ظاهر فتُدفَع مرّتين.
- **مالٌ خرج مرّتين غيرُ صفٍّ دخل مرّتين.** الأوّل يُطالَب به الجهةُ ويُسترَدّ، والثاني يُصلَح في قاعدتنا. ويفرّق بينهما مرجعُ السداد: مرجعان مختلفان عمليّتان قطعاً. وفي كشف أحمد **٢٬٣٥٠٫٧٧ ريالاً** سُدّدت مرّتين في ١٥ يوليو — كهرباءُ عدّادَين واتصالات — لم يرها النظام لأنّ `findDuplicatePayments` كانت تتخطّى كلّ بابٍ غير «مورّد»، **وبابا المرافق والحكومة هما بالضبط حيث يقع السدادُ المزدوج**: السدادُ فيهما بضغطةٍ تُعاد حين لا يظهر التأكيد.
- **والصفرُ المكتوب بيدٍ أسوأ من الفراغ.** `duplicatePayments` في «ما يحتاج انتباهك» كان `0` ثابتاً وتعليقاً يحيل إلى صفحة البنك — وصفحةُ البنك لا تحسبه أيضاً. فلم يظهر البندُ مرّةً واحدة، والشاشةُ تقول ضمناً «لا سداد مزدوج» وهي دعوى لم تُفحَص. **والقيمة الثابتة تُقرأ جواباً.**
- **«لمن دفعنا بلا فاتورة؟» سؤالٌ يستحقّ موضعاً.** كان أحمد يراجعه بيده كلّ أسبوع في دفتره، ولا موضع له في النظام. والدفعة غير المخصَّصة حالٌ صحيحة لا خطأ — لكنّها عملٌ باقٍ: إمّا أن تصل الفاتورة، وإمّا أن يُعلَن أنّ المورّد لا يصدرها فيُطلَب عقدُ توريد. وبلا فاتورةٍ ضريبية لا يُخصَم مدخلُ الضريبة. والمقدَّمةُ المعلَنة تخرج منه: صاحبُها قال ما هي.
- **الأهليّ يكسر الكلمة بفراغ ويقتطع الاسم عند حدّه.** التصدير يلفّ السطر عند عرضٍ ثابت **داخل الكلمة**: «المحد ودة» و«التجا رية» و«والتغل يف» و«TRA DING»، ثمّ يقصّ الخانة عند ثلاثين حرفاً: «PURE BEVERAGE INDUSTRY CO CLOS». فكان الاسمُ البديل المكتوب صواباً لا يلتقي بنفسه — **أحدَ عشر مستفيداً من تسعةَ عشر** لا يعرفهم النظام، وفيهم من له بديلٌ مكتوبٌ عندنا منذ التأسيس. فصار الاسم يُقارَن بلا فراغاته، والاحتواءُ يُسأل في الجهتين (الكشف أقصرُ أحياناً وأطولُ أحياناً)، والبدائلُ تدخل الاحتواء لا المساواةَ وحدها.
- **DeepSeek لا يقبل PDF، وأرشيف المقهى كلّه PDF.** ١٥٧ من ١٥٨ مستنداً. والأنواع المقبولة أربعُ صور. فالمسار الحرج في الهجرة قراءةُ الـPDF لا تبديلُ المزوّد. والحلّ طبقتان: **النصّ أوّلاً** — الرقم المكتوب في الملفّ مقروءٌ يقيناً والمرسوم في صورةٍ مقروءٌ ظنّاً، فحين يوجد النصّ تكون قراءتُه نقلاً لا قراءة — ثمّ انتزاعُ JPEG المضمَّن للممسوح. ولا حزمة رسمٍ جديدة: الماسح وضع صورته في الملفّ كما هي، فالمطلوب مقصٌّ لا رسّام.
- **ولا مخطّط صارم عند المزوّد.** `json_schema` يُردّ بـ٤٠٠. وجيميني كان يفرض `responseSchema` وكلود `zodOutputFormat` — فالانتقال **تراجعٌ في الضمان** يُقال ولا يُغطّى. وأثرُه أنّ التحقّق بعد القراءة صار حاملاً للصحّة: يُوصَف المخطّط في الموجِّه، ويُتحقَّق بـzod، ويُعاد السؤال موجَّهاً إن اختلّ الحساب.
- **وشكلُ الجواب يُطلَب في المزوّد لا في المستدعي.** جُرّب الحَكَم بلا وصفٍ للشكل فاختار المرشّح **الصحيح** وأجابه `{"answer":"a"}` — حكمٌ سليم في غلافٍ لا يقرؤه المخطّط، فسقط كأنّه فشل. والخسارة مضاعفة: كلفةُ نداءٍ صحيح، ثمّ حركةٌ تُرفَع إلى مراجعةٍ بلا حاجة.
- **ورموز التفكير تُحسَب من `max_tokens`.** سقفُ ٨٠٠٠ كان يكفي الجواب ولا يكفي التفكير قبله، فانقطع مخرَجُ فاتورةٍ من ثلاثة بنود. **والانقطاع لا يُرى في الجواب** — يُرى في `finish_reason`، ومن لم يفحصه قبِل نصفَ JSON. ونصفُ الرقم في مستندٍ ماليّ أسوأ من لا رقم.
- **ترتيبُ كلمات الصفّ يتبع لغته لا جهةً واحدة.** أُعيد استعمال `groupIntoRows` (وهي لكشوف البنك، ترتّب من اليمين دائماً) فخرج اسم المورّد مقلوباً: «Est. Trading Leaves Olive». **والمبالغ سلمت لأنّ الرقم لا يُقلَب** — فكان العطب صامتاً في الأسماء وحدها، وهي ما تقوم عليه مطابقة المورّدين. وقلبُ الاتجاه يُصلح طرفاً ويُفسد الآخر، والصفّ الواحد يحمل الاثنين. فالترتيب بالموضع ثمّ **عكسُ السلاسل العربية وحدها**. ولم تُمَسّ `groupIntoRows`: تغييرُ دالّةٍ يعتمد عليها مسارٌ قائم يُبطل بأثرٍ رجعيّ ما بُني بها.
- **وأشكالُ العرض تُوحَّد.** كثيرٌ من ملفّات PDF يخزّن العربية بـ«أشكال العرض» (ﻣﺆﺳﺴﺔ) لا بحروفها (مؤسسة) — نصّان مختلفان بايتاً وإن تطابقا في العين، فلا يلتقي اسمٌ باسمه المسجَّل أبداً.
- **«تعذّر الاتصال» تُقال حين لا يصل الطلب، لا حين يردّ الخادم بخطأ** — وإلّا أُرسل صاحب العمل يفحص شبكةً سليمة. فما لم يصل عطبُ شبكة، وما ردّ عليه الخادمُ عطبٌ عنده ومعه رمزُ الحالة.

### الجرد الأسبوعيّ (سبتمبر ٢٠٢٦) — `docs/review/INVENTORY-RECONCILIATION-DESIGN.md`

- **الفرقُ ليس فاقداً، ولا يُسمّى به.** الفرقُ الواحد قد يكون وصفةً خاطئة،
  أو جرعةً زائدة، أو ميزاناً غير معاير، أو فاتورةَ شراءٍ لم تصل، أو نقلاً
  لم يُسجَّل، أو عدّاً مستعجلاً. فاسمُه **«فرق الجرد»** و**«فاقد غير
  مفسَّر»**، ولا ينفصل عنه «هدرٌ مسجَّل» إلّا بعد أن يكتبه إنسان في
  `waste_records`. وتسميةُ الفرق فاقداً دعوى سببٍ بلا دليل — ثمّ يُحاسَب
  عليها أحد.
- **الكمّيّة عددٌ صحيح بالمِلّي، كما أنّ المال هللات.** حسابُ الجرد يضرب
  ويجمع آلافَ المرّات في الأسبوع، والكسرُ العائم يجعل مجموعَ ألفِ سطرٍ
  يختلف عن مجموع الألفِ نفسِه بترتيبٍ آخر — **ورقمٌ يتغيّر بترتيب جمعه
  ليس رقماً**. والمِلّي من أصغر وحدةٍ في العائلة: مِلّي‑جرام ومِلّي‑مليلتر
  ومِلّي‑حبّة، فـ١٠٠ × ٢٠ جراماً = ٢ كجم بالضبط.
- **ولا جسر بين وزنٍ وحجم، ولا بين حبّةٍ وعبوة.** و«لا يُحوَّل» تعني أنّ
  الاستهلاك **مجهول** لا صفر: لو قُرئ صفراً لصار المتوقَّعُ هو الافتتاحيَّ
  زائدَ المشتريات كلِّها، فيُعلَن فرقٌ هائل سببُه خطأٌ في وحدةٍ لا في مخزون.
- **الفرقُ لا يُحسَب بالمبيعات وحدها.** افتتاحيٌّ ٥ ومشترياتٌ ٢٠ واستهلاكٌ
  ١٢ ← المتوقَّع ١٣. ولو أُسقطت المشترياتُ لصار المتوقَّع «سالب ٧» والجردُ
  ١٠٫٥، فيُعلَن «فائضٌ ١٧٫٥ كجم» — رقمٌ بلا معنى وأسوأُ من لا رقم.
- **ولا تُشتقّ كمّيّةٌ من ريال.** «دفعتُ ٤٥٠ ريالاً لمحمصة» لا يقول كم كيلو،
  وقسمةُ المبلغ على آخر سعرٍ معروف تُنتج كمّيّةً تبدو دقيقة وهي مخترَعة.
  فما لم تُعرَف مواصفةُ عبوته يُعلَن ويُستبعَد، **ويُعرَض عددُه ومبلغُه** كي
  يُعرَف حجمُ ما خرج من الحساب.
- **وبندُ شراءٍ واحدٌ مجهولُ الكمّيّة يُخرج مشترياتِ صنفه كلَّها إلى «مجهول»** —
  ولا تُحسَب ناقصةً. وإلّا أُعلن فرقٌ سببُه شراءٌ وقع ولم يُقَس.
- **نسخةُ الوصفة تُختار بتاريخ البيعة لا بالأحدث.** جرعةٌ تغيّرت من ٢٠ جراماً
  إلى ١٨ في ٨ سبتمبر: حسابُ الأسبوع الأوّل بالأحدث يقول إنّ المقهى استهلك
  ١٨٠٠ جرامٍ وقد استهلك ٢٠٠٠. وفي الأسبوع الواحد قد تعمل نسختان. **ولا
  تُعدَّل نسخةٌ في مكانها أبداً** — التعديلُ نسخةٌ بتاريخٍ جديد، وتُغلَق
  السابقةُ في اليوم الذي قبله. ويمنع التداخلَ مؤثِّرٌ في القاعدة لا اتّفاقٌ
  برمجيّ: الكتابةُ تأتي من مسارين.
- **الملغى والمرتجَع والمجانيّ ثلاثةٌ لا واحد.** الملغى يُستبعَد كلُّه (لم
  يُصنَع)، والمرتجَع يُنقص الاستهلاك، والمجانيّ يُستهلَك فعلاً ويُعرَض على
  حدة. ومن جمعها في رقمٍ واحد أخطأ في ثلاثة.
- **التغطيةُ تُحسَب قبل النتيجة وتُعرَض فوقها.** رقمُ فرقٍ يبدو دقيقاً وقد
  سقط منه ثلثُ المبيعات صامتاً **أسوأ من ألّا يوجد رقم**: الأوّل يُتَّخذ
  عليه قرارٌ خاطئ، والثاني يدفع إلى إكمال البيانات. والحكمُ ثلاثة:
  `READY` · `PARTIAL` · `BLOCKED` — والجزئيُّ يُعلَن ومعه **كم يمثّل من
  المبيعات**، فـ«٩٦٪ من الوصفات مكتملة» لا تقول إنّ الأربعة الباقية أكثرُ
  الأصناف مبيعاً.
- **والصفَّ الذي لا يُقرأ يُفرَّق فيه الفارغُ من المعطوب.** سطرُ المجموع في
  تقرير فودكس خانةُ كمّيّته فارغة، فلو عُدّ خطأً لخرج من كلّ استيرادٍ خطآن
  أو ثلاثة لا شيءَ فيها — ومن يرى خطأً كلَّ مرّة يتعلّم ألّا ينظر، فيمرّ
  الخطأُ الحقيقيّ معها.
- **مرادفاتُ الترويسة تُطبَّع بما تُطبَّع به الترويسة.** كُتبت «الكمية» بتائها
  المربوطة و`normaliseHeader` تحوّلها هاءً، فلم يلتقيا — **ولم يُفهَم عمودُ
  الكمّيّة في أيّ ملفٍّ عربيّ**، فسقط تقريرُ مزيج الأصناف كلُّه بحجّة «لم
  يُفهَم الملفّ». والعلاجُ أن يمرّ الطرفان بالدالّة نفسها لا أن تُكتب
  الصيغةُ المطبَّعة بالحدس.
- **والفرعُ الفارغ في البيعة «لم نكن نعرف» لا «فرعٌ آخر».** الترشيحُ
  بالمساواة وحدها كان يُسقط كلَّ بيعةٍ لم يُقرأ فرعُها من الملفّ، فيخرج
  الاستهلاك **صفراً بلا شكوى** ويظهر كلُّ ما اشتُري «فرقاً». وهو الدرسُ
  نفسه الذي كلّف `014` كشفاً كاملاً مرّتين.
- **والعدُّ يُكتب على سطرٍ موجود، فالأسطرُ تُهيَّأ قبله.** كان `saveActualCounts`
  يُحدِّث بشرطٍ لا يطابق شيئاً إن لم يُحسَب الجرد مرّةً — **فتنجح الكتابةُ
  ولا تكتب**. والترتيبُ كان مضموناً في الواجهة وحدها، وذلك اتّفاقٌ لا حارس.
  ويُعَدّ أثرُ الكتابة الآن، وصنفٌ ليس في الجرد يُقال باسمه.
- **الثباتُ التاريخيّ مفروضٌ في القاعدة لا في الشيفرة.** أرقامُ الجرد المقفَل
  مجمَّدةٌ في أسطرها، ومؤثِّرٌ يرفض الكتابةَ فيها (كما يفعل `028` بالشهر
  المقفل)، ولقطتُه تحفظ **بم حُسب**: نسخُ الوصفات، وأسطرُ الفواتير،
  ومعرّفاتُ الاستيرادات، ونسخةُ المحرّك. فالتقريرُ يُقرأ ولا يُعاد حسابُه
  — وذاك الفرقُ بين تقريرٍ تاريخيّ واستعلامٍ عن الماضي.
- **والاستيرادُ يُمنَع من التكرار على المفتاح الطبيعيّ لا على البصمة وحدها.**
  البصمةُ تمنع رفعَ الملفّ عينِه، ولا تمنع ملفّاً آخر يغطّي الطلبات نفسها.
  فرقمُ الطلب عند فودكس هو الهويّة؛ وتقريرُ «مزيج الأصناف» ملخّصُ يومٍ لا
  عمليّة، فهويّتُه `PMIX:{الفرع}:{التاريخ}` — وملفٌّ أحدثُ عنه **يُعيد
  بيانَه** ولا يضيفه.
- **والترتيبُ آخرُ ما يدخل هويّة السطر.** كوبان متطابقان في الطلب الواحد
  واقعتان لا يفرّق بينهما إلّا الترتيب؛ وصنفٌ مذكورٌ مرّتين في تقرير اليوم
  شيءٌ واحد يُجمَع. فلو أُدخل الترتيبُ في الحالين لخرج من اليوم الواحد
  سطران لصنفٍ واحد.
- **والميزانُ غيرُ الدفتر.** `inventory:count` صلاحيةُ عدٍّ لا بابٌ خلفيّ
  إلى المال: مديرُ المشتريات يعدّ الرفَّ ولا يرى كلفةَ الفرق ولا مبيعاتِ
  الأسبوع. فالمبالغُ تُخفى في الشاشة، والصفحتان اللتان جوابُهما بالريال —
  سجلُّ الجرد والاتّجاه — تشترطان `amounts:view`. وإعادةُ فتح جردٍ مقفَل
  للمالك وحده بـ`month:reopen`، كإعادة فتح الشهر.

### إعادةُ تصميم التجربة (سبتمبر ٢٠٢٦) — `docs/review/UX-REDESIGN-DESKTOP.md`

- **الرقم الواحد كان له أربعة أجوبة.** «دفعات بلا فاتورة»: عشرٌ في التنبيه، وثلاثَ عشرةَ في البنك، ومبلغٌ ثالث في المشتريات. والفرقُ وحده `issues_invoices` — يستثنيه أحدُهما ولا يستثنيه الآخر، فثلاثةُ آلافِ ريالٍ تظهر في شاشةٍ وتغيب في أخرى بلا أن يُقال لِمَ. ومالٌ خرج بلا فاتورة هو كذلك أصدر المورّدُ فواتير أو لم يصدر: يدخل الكلّ، و`issuesInvoices` يتبعه **صياغةُ الطلب** وحدها.
- **عدّادُ القشرة كان يقول صفراً والعملُ تسعةُ بنود.** `countPendingWork()` يعدّ حركاتِ البنك التي لم يُعرَف بابُها، وهي صفرٌ في بيانات المقهى. فالعدّادُ الوحيد الدائم في الشاشة يقول «لا شيء ينتظرك» بينما مالٌ خرج مرّتين ينتظر. **والصفرُ يُقرأ جواباً.** فصار العدد عددَ بنود `/attention` نفسِها.
- **الحارسُ الناقصُ يُطمئن ولا يحرس.** اختبار «لا تنبيه يُرسل إلى صفحة جامعة» كان يمرّ وهو يكذب: قائمةُ الصفحات الجامعة فيه لم تشمل `/analysis`، وبندُ ارتفاع الأسعار يفتحها وجدولُ الأسعار في `/performance`.
- **وضعُ المعاينة كان متعذّراً على Vercel أصلاً.** الحارس `VERCEL_ENV === "production" || NODE_ENV === "production"`، وNext.js يبني **كلّ** نشرٍ بـ`NODE_ENV=production`. واختبارٌ اسمُه «يعمل في بيئة معاينة Vercel» كان يمرّ لأنّه يمرّر `NODE_ENV: "development"` — تركيبةٌ لا تقع في نشرٍ حقيقيّ. فصار `VERCEL_ENV` هو الحاكم حيث يوجد.
- **سياسةُ مستندات المورّد يكتبها الإنسان ولا تُشتقّ** (`035`). «لم يصل منه مستند» لا يعني «ليس له مستند»: فمن لا يحتاج عقداً يُعلَن، ومن فواتيرُه ورقيّةٌ يُعلَن — وفاتورتُه موجودةٌ حقّاً ومطلبُها رفعُ الورقة لا عقدُ توريد. **والتنبيهُ الذي لا يُسكَت ولا يُفعَل فيه شيء يُعلّم صاحبَه تجاهلَ ما عداه.**
- **السببُ يُشتقّ ولا يُخزَّن.** «ينقصها ركن» كان حكماً من ثلاث كلمات، والسببُ محسوبٌ وقتَ الأرشفة ثمّ يُرمى. ولو خُزّن لصار له مصدران يفترقان متى صُحّح حقلٌ أو تغيّرت سياسةُ المورّد. **والتشخيصُ الذي لا يُعرَض لا يُعالَج.**
- **القراءةُ تُخطئ، فيُصحَّح ما أخطأت فيه لا يُرفَض المستند الصحيح.** كانت الشاشة تقول «لا يوجد رقم فاتورة» والرقمُ على الورقة، ولا موضعَ يُكتب فيه: لا اعتمادٌ ينفع ولا رفضٌ يصحّ. و`/api/invoice-fields` يصحّح، **والخادمُ يعيد اشتقاق حال الضريبة** — فمن يصحّح رقماً لا يقرّر معه أنّ الفاتورة صارت صالحة.
- **`<a>` داخل `<a>` يُسقط الترطيب فيتوقّف تفاعلُ الصفحة كلّها.** بطاقةُ الصفّ على الجوّال كانت رابطاً يلفّ خلاياها؛ فلمّا صارت شارةُ الضريبة تفتح سببَها تعطّلت الصفحة. فصار الرابطُ طبقةً تحت المحتوى، وما كان تفاعليّاً يُرفَع فوقها.
- **إخفاء المبالغ بالتنقيط لا بالطمس** — `blur` يترك طولَ الرقم، وطولُه وحده يقول «هذا بالآلاف». وكثيرٌ من المبالغ يُبنى في الخادم **داخل جملة**، فكانت تبقى ظاهرةً والزرُّ مضغوط: **ومن يظنّ أنّه أخفى يُري ما لا يريد.** فـ`Prose` يلفّ ما يشبه المبلغ عند العرض.

- **«عليك ١٨ ألفاً» كان خطأً في المنهج لا في الأرقام.** كلُّ فاتورةٍ حُسب ما بقي عليها، ومالٌ دُفع للمورّد ولم يُخصم من فاتورةٍ بعينها لا يُنقص شيئاً — ولوريفا حُوِّل لها قبل وصول فواتيرها بيومين فبقيت فواتيرها «مستحقّة». فصار الرقم **بالمورّد**: فواتيره المفتوحة ناقصَ رصيدنا عنده (`supplier-balances.ts`)، ويُخصم الرصيد آلياً عند وصول الفاتورة في الرفع والمزامنة (سبعة أيّام، وما جاوزها قرار إنسان).
- **سدادٌ من حساب المالك ليس حوالةً ولا نقداً — طريقةٌ باسمها (`OWNER_ACCOUNT`).** فاتورة مايو للكوب الذهبي سُدّدت من حساب أحمد الشخصيّ فلا يراها كشف المقهى، فوزّع النظام حوالات يوليو «بالأقدم أوّلاً» على مايو وبقيت يوليو (١٢٬٠٠٣٫١٣) مفتوحةً وقد سُدّدت. فالقيد من حساب المالك **يفكّ حوالات المقهى عن الفاتورة وينقلها إلى فواتير المورّد الأخرى**، ويُعايَن قبل الإقرار لأنّه ينقل مالاً بين فواتير.
- **دفعة أوّل الشهر تخصم رصيد المورّد قبل أن تقترح تحويلاً** — وإلّا دُفع الريال مرّتين لمورّدٍ عنده لنا مال.
- **تحليل الذكاء يشرح ويقترح، والخادم يحسب ويتحقّق، والإنسان يُقرّ.** الأرقام (المفتوح، الرصيد، الفرق مع الكشف عند تاريخه، المكرَّر) تُحسب قبل النموذج وتُعطى له؛ وجوابه يشير بمراجع قصيرة (F1، P2) من قوائم حُسبت، وما لا يُطابَق يُسقَط، **ومبلغ كلّ اقتراح يُعاد حسابه في الخادم**. والإقرار يمرّ بالخدمات نفسها التي يمرّ بها العمل اليدويّ، ويحتاج صلاحية اعتماد السداد إن كتب مالاً.
- **والتحليل يتحسّن بالقرار لا بالتدريب** — ما رفضه أحمد وملاحظته يُعطيان للنموذج في التحليل القادم: «لا تقترحه ثانيةً إلّا بدليلٍ جديد تذكره».
- **النموذج يُطلب بمهلةٍ تنقضي قبل عمر المسار** (`AbortSignal.timeout` تحت الستّين) — ومورّدٌ واحد في كلّ طلب، و«حلّل الكلّ» يمرّ عليهم من المتصفّح واحداً واحداً. فلا يُقتل طلبٌ طويل صامتاً.

- **الكتابة داخل المعاملة بمقبضها `t` — لا بـ`db` أبداً.** على Vercel في المجمَّع اتّصالٌ واحد تحجزه المعاملة، فينتظر `db` عشر ثوانٍ ثمّ يسقط؛ ومحلّياً (عشرة اتّصالات) يسقط بالمفتاح الأجنبيّ لأنّ الصفّ الأب لم يُودَع. فلم يُحفَظ تعريفُ جهةٍ من الواجهة خمسة أيّام، ولم يكشفه اختبار. و`recordAudit(entry, writer)` يأخذ المقبض كي لا يبقى في السجلّ «تعلّمٌ» أُلغي. ويحرسه `code-guards.test.ts`.
- **المزامنة لا تسمّي.** تُقيِّد الملفّ باسمه وتقترح، والتسمية في `/api/drive-rename` وحده باختيارٍ ملفّاً ملفّاً (القائمة تبدأ فارغة) وقيدٍ بالاسمين. وكانت تسمّي قبل التقييد بلا أثر — خرقاً للقيد الأوّل.
- **التوأم يُسأل في `createPayment` نفسها** لا في المستدعين — كان يُسأل في مسارين من ثمانية فبقيت لافا ٩٤٥ دفعتين. ومن قيّد من حركة بنك يمرّ بـ`recordBankPayment`: المقيَّد بلا حركة يُتبنّى مخصَّصاً كان أو لا.
- **الحركة تُربط بدفعتها بشرطٍ يُعدّ أثره** (`claimBankTransaction`): الشرط في `where` بلا فحص عدد الصفوف لا يمنع ضغطتين.
- **الإقفال يمنع بلا كشف وبلا معادلة، والشهر المقفل لا يُكتب فيه من أيّ باب** — في الخدمات وفي القاعدة (٠٢٨). وإعادة الفتح للمالك وحده.
- **`db:verify` يطابق القيد الذي يسمّيه** — أيُّ خطأٍ كان يُعدّ رفضاً، ففحصٌ ماليّ كان يمرّ بقيد فرادةٍ لا علاقة له.
- **المصروف المقيَّد يتبع تصنيف حركته** (`resyncBankExpenses`) — والحذف لصفٍّ مشتقّ، بنصّه في التدقيق.
- **`.nums` على الرقم وحده، وأهداف اللمس ٤٤ بكسل على الجوّال** (`buttonClass` يحملها)، ومنطقة الرفع `label` تلفّ حقلاً `sr-only` لا `hidden` — `display:none` لا يُركَّز عليه فلا يرفع من لا يستعمل الفأرة.
- **لوح الإقرار يُغلق عند النجاح وحده** — `onConfirm` يُرجع `false` إن فشل.
- **لا وسمَ جماعيّ بالسداد.** «أعلن سدادها يدوياً» كان يسِم كلّ المفتوح بضغطتين؛ و`mark-paid` صار يقبل الفواتير بأعيانها.

### مراجعةُ التجربة الثانية (سبتمبر ٢٠٢٦) — `docs/review/UX-VALIDATION-V2.md`

- **الرقم الواحد باسمين يهدم الثقة بالاثنين.** كانت صفحةُ الفواتير تقول
  «ما بقي عليك ١١٬٩١١٫٢٤» وصفحةُ المورّدين «عليك للمورّدين ١٠٬٥٠٢٫٤٩»،
  والفرقُ ١٬٤٠٨٫٧٥ رصيدٌ لنا عند مورّدٍ خُصم في إحداهما. ومن قرأهما لا
  يملك ما يرجّح به. فصار لكلٍّ اسمُه: «بقي على هذه الفواتير» بالفاتورة،
  و«عليك للمورّدين» وحدَها بالمورّد — والمصدر `supplier-balances.ts`
  كما هو.
- **العملُ المعروض أربع مرّاتٍ يُقرأ أربعةَ أعمال.** «دفعات لم تُنسب
  إلى فاتورة» كانت بطاقةَ أرقامٍ في الرئيسية و«يحتاج قرارك»
  و«المورّدين» و«حركة البنك». وهو **عملٌ باقٍ** لا حالُ حساب، فموضعُه
  الطابور وحده، ومن غيره سطرُ إحالة.
- **الرقمان اللذان لا يُفهمان إلّا بفقرةٍ بينهما لا يُعرضان معاً.** كان
  تحت بطاقتَي «رصيدٌ لك» و«لم تُنسب» نصٌّ يشرح لِمَ تختلفان. والفقرة
  صحيحة، **ووجودُها هو العطب**.
- **ما يُقاس يُصدَّق: الطيّ عند ٧٦٨.** كانت الرئيسية تُظهر زرَّ المهمّة
  الأولى عند ٧٣٣ والثانيةَ عند ٩٦٧ — مهمّةٌ واحدةٌ من ثماني على أشيعِ
  مقاسِ حاسوب. فصار شريطُ الحال سطراً (٩٢ بكسلاً بدل ٢٣٣) والمهمّةُ
  صفّاً (٦٥ بدل ٢٤٩)، فخمسُ مهمّاتٍ فوق الطيّ. و«يحتاج قرارك» رُفعت من
  فوقه أربعُ بطاقاتٍ وفقرةُ تحذيرٍ من جمعها، فظهرت بنودُه الثمانية في
  شاشةٍ واحدة.
- **الحالُ يُقرأ من الحال، لا من عمودٍ رجّح يوماً.** كانت شارةُ الحركة
  تُشتقّ من `match_disposition` متى لم تكن مقيَّدةً بدفعة — وهو عمودٌ عن
  **آخر ما رجّحه المحرّك** لا عن حالها اليوم. فحوالةٌ **واردة** بـ١٬١٠٠
  ريالاً حالُها `IGNORED` (أُعلن أنّها ليست سداداً وانتهى أمرُها) كانت
  تُعرَض «تنتظر مراجعتك» بالأحمر. ونجا منها ٢١ صفّاً بالمصادفة لا بحارس:
  لأنّ لها دفعةً مطابَقة. **والشارةُ التي تُري عملاً مقضيّاً تُفقد الثقةَ
  بالطوابير كلّها** — يبحث صاحبُ المقهى عمّا وعدته به فلا يجده. فصارت
  تُشتقّ بترتيب ما تصير به الحركة إلى حالها: متجاهَلةٌ ← مقيَّدةٌ ←
  حُسمت بلا مال ← ترجيحٌ ينتظر. ولم يُمَسّ محرّك المطابقة.
- **الحارسُ يمسك الحالَ لا سبباً من أسبابه.** كان يسأل عن
  `match_outcome = 'NOT_A_PAYMENT'`، ونتيجةُ تلك الحركة `UNKNOWN_ENTITY`
  — وكلتاهما `IGNORED`. فسببٌ واحدٌ من أسباب التجاهل لا يقوم مقام
  التجاهل نفسه.
- **عدّادُ البند هو عدّادُ اللوح الذي يفتحه.** كان بند «حركات بنكية»
  يعدّ `category='UNKNOWN'` واللوحُ تحته يقرأ `pendingDecision()` —
  شرطان لشيءٍ واحد، وتقاطعُهما قد يكون صفراً: حركةٌ تنتظر ولا بندَ يدلّ
  عليها، أو بندٌ يُفتَح على لوحٍ فارغ. فصار الشرط واحداً يُستدعى ولا
  يُنسَخ. وأُضيفت إليه بندُ «رجّح ولم يُبتّ» لكلّ بابٍ واتّجاه، بحرّاسه
  الثلاثة (لا دفعة · لم يبلغ الحسم · لم يصنّفه إنسان) كي لا يعود ما
  فُرغ منه.
- **لا يُخرَج من الطابور إلّا لعملٍ لا يقع إلّا خارجه.** كان ستّةٌ من
  ثمانية بنودٍ زرُّها ينقل إلى صفحةٍ أخرى لعملٍ صغير — رسالةُ واتساب،
  ومستندٌ يُعتمَد، وسياسةُ مورّدٍ تُعلَن. فصارت في `attention-workspaces.tsx`
  ألواحاً داخل البند. وبقي بندان يخرجان بحقّ: ٦٨ فاتورةً ناقصة (لكلٍّ
  سببُها)، والمتأخّراتُ (فعلُها بناءُ ملفّ تحويلات).
- **والدليلُ لا يُعرَض فوق لوحٍ يعرضه ومعه فعلُه** — صارت الأسماءُ
  مكتوبةً مرّتين في شاشةٍ واحدة: ثمانيةً بلا فعل ثمّ ثلاثةَ عشر بفعلها.
- **القاعدةُ الواحدة تُحقَن ولا تُنسَخ** (`supplier-policy-rules.ts`) —
  «من يحتاج عقد توريد» كان محسوباً في ثلاثة مواضع، فافترق. والحقنُ في
  SQL يسمّي أعمدته باسم الجدول، **فلقبٌ للجدول يكسره**: `from suppliers s`
  مع الحقن أخرج «invalid reference to FROM-clause entry» — صفحةَ عطبٍ
  حمراء لا يراها المترجم ولا الاختباراتُ النقيّة. ويحرسه اختباره.
- **اسمُ الهيكل هو اسمُ صفحته.** تسعُ صفحاتٍ كانت تعرض عنواناً ريثما
  تُبنى ثمّ عنواناً آخر حين تُبنى («ما يحتاج انتباهك» ← «يحتاج قرارك»،
  «كشوف المورّدين» ← «الكشوف»). ويحرسه `nav.test.ts`.
- **العددُ المحسوبُ بشرطٍ قديم في شاشةٍ جانبيّة ينقض الشرطَ الجديد في
  شاشتين.** «يحتاجون عقد توريد» في `/upload` كان `!issuesInvoices`
  وحده، فيقول ٣ بينما المورّدون و«يحتاج قرارك» يقولان ٢ — لأنّهما
  يقرآن الهجرة ٠٣٥ (`contract_required` · `paper_invoices` ·
  `contract_on_file`). وحُذف اللوحُ كلُّه: فيه أيضاً اسمُ النموذج
  القارئ، وهو سؤالُ من بنى النظام.
- **الصفحةُ التي جوابُها «لا يمكن الحساب» لا تأخذ لساناً في التنقّل.**
  «المصروفات» بلا مصروفاتٍ متكرّرةٍ مسجّلة: المتوقَّع ٠٫٠٠ والفرق «لا
  يمكن الحساب» وجدولُ المقارنة عمودان يقولان «لم يُتوقَّع». فخرجت إلى
  `owns`، ومدخلُها من «أين ذهب» — وبقي فيها الفعليُّ وحده حتّى يُسجَّل
  متوقَّع.
- **اسمُ الفعل لا اسمُ الوجهة.** كان ثلاثةَ عشر من سبعةَ عشر تنبيهاً
  زرُّها يبدأ بـ«افتح…». فصارت: اطلب الفواتير · اعتمدها أو ارفضها ·
  طالِب بالردّ · احذف الزائد · اقرأ تفصيلها · أعلِن سياسة مستنداتهم.
- **نصُّ البنك الخام ليس اسماً.** كانت `/bank` تعرض خمسةً وعشرين صفّاً
  أوّلُ سطرٍ في كلٍّ منها `حوالات تحت الطلب20260825S ANCBKNCBK6B8241…`
  — ولوحُ الحركة المختارة **فوقها في الصفحة نفسها** يستخرج المستفيد
  بـ`toCanonical`. فاختلافُ الشاشتين في الشيء الواحد سهوٌ لا قرار.
- **اسمٌ واحدٌ لضابطين في شاشةٍ واحدة.** في صفحة المورّد كان «كشوفه»
  زرّاً في الترويسة ولساناً في المتن — وجهتان بالاسم نفسه. فصار الزرّ
  باسم فعله «طابِق كشوفه»، وسقط «فواتيره» لأنّ لسانَه تحته يعرضها.

## المصائد المعروفة

- **الأسماء القديمة `deepseek-v4-flash` و`deepseek-v4-flash-vision-exp` سُحبت** (١٠ سبتمبر ٢٠٢٦) وتُوجَّه إلى `deepseek-flash` مؤقّتاً. ومتغيّرات `DEEPSEEK_*_MODEL` في Vercel كانت تحملها فتغلب الافتراضيّ بصمت — أُزيلت، والافتراضيّ في `models.ts` هو المصدر.
- **ما قرأه النموذج لا يُؤخذ من المتصفّح** — `/api/analyze` يحفظه في `extraction_cache` ببصمة الملفّ، و`/api/archive` يقرؤه منها. من يضيف حقلاً يُبنى من القراءة يقرؤه من هناك.
- **جيميني ما زال في الشيفرة، خاملاً** — ولا يُختار إلّا بـ`EXTRACTION_ALLOW_ALT_PROVIDER=true` فوق `EXTRACTION_PROVIDER`. لم يُحذَف لأنّ حذفه مشروطٌ باجتياز القياس على مستندات حقيقية، ولم يُقَس بعد. ولا احتياط إليه: المتغيّرات كلّها `deepseek`.
- **حصّة Gemini المجانية = ٢٠ طلباً/اليوم لكل نموذج** (لا في الدقيقة). لذلك تدوير سبعة نماذج.
- **الطبقة المجانية تتدرّب على الفواتير ويراجعها بشر.** التبديل سطر: `EXTRACTION_PROVIDER=claude`.
- **`AUTH_BYPASS`** يفتح كل الأبواب في التطوير، ولا يعمل في الإنتاج مهما فُعِّل — الحارس مزدوج في `src/lib/preview-mode.ts`.
- **وضع المعاينة كان يقرأ ولا يكتب.** `trial-user` معرّفٌ مخترَع لا وجود له في `users`، وكلّ قيدٍ يحمل `created_by_id` مقيَّدٌ بمفتاحٍ أجنبيّ إليه — فيسقط بـ500 برسالةٍ لا تقول السبب. أي أنّ الوضع المبنيّ لتجربة النظام كان يمنع تجربة نصفه. صار `currentUser()` يستعير معرّف أوّل مستخدمٍ قائم ويُبقي الاسم المعروض «وضع التجربة».
- **مساعد `now()` يسمّي كل عمود `created_at`** مهما كان اسم الحقل في TypeScript. فـ`documents.uploadedAt` عمودها `created_at` — ومن يكتب SQL خاماً يقع فيها.
- **`transaction_type`** كان ناقصاً في ألفٍ وأربعمئة حركة — استعاده الدمجُ في `db:dedupe` من نسختها المحذوفة، وصار كاملاً في ١٤٤٥ حركة. ومن يحذف مكرَّراً بلا دمج يفقده ثانيةً.
- **مفاتيح تحتاج تدويراً بيد أحمد:** `GEMINI_API_KEY` و`GOOGLE_CLIENT_SECRET` (ظهرا في محادثة).
- **اسمُ الشهرة أنفع من الاسم النظاميّ في البدائل البنكية.** الاسمُ الكامل لا يلتقي بخانةٍ مقتطعة أبداً؛ واسمُ الشهرة يلتقي بها احتواءً. فـ«الرعاية المتناهية» تعمل و«شركة الرعاية المتناهية المحدودة» لا تعمل.
- **`sql` داخل `.select()` تُكتَب بالمرجع لا بالعمود.** ‏`${payments.id}` في قالبٍ خام يُصيّر العمود مجرّداً فتصمت الاستعلاماتُ الفرعيّة وتُرجع صفراً — لا خطأً. كُتبت `findPaymentTwin` بها فقالت «لا تخصيص» عن دفعةٍ مخصَّصة بالكامل، وكشفه سيناريو الشهادة لا المترجم. والصواب `${payments}.id`.
- **الاستعلام الفرعيّ المرتبط يُكتب `${table}.id` لا `${table.id}`** — الثاني يُصيّر العمود مجرّداً فيصمت ويُرجع صفراً **لا خطأً**. وقع في `/api/match-confirm` فبدت كلّ فاتورةٍ مسدَّدة مفتوحةً، ورُدَّ السدادُ المزدوج من القاعدة لا من الشيفرة. ويحرسه `src/lib/allocation-sql.test.ts` بقراءة الملفّ نصّاً — إذ لا يراه المترجم ولا يرميه التشغيل.
- **الكتابة على مئات الصفوف تُجمَع دفعاتٍ** — `/api/counterparty` كان يحدّث صفّاً صفّاً داخل معاملة، فنفدت مهلة الاتصال (`timeout exceeded when trying to connect`) وخرج ٥٠٠.
- **النصوص الكاتبة ترفض بلا `--i-know-this-is-production`** (`scripts/lib/guard-write.ts`) — `db:demo` و`db:seed` و`db:expenses` و`db:products` و`try:archive --upload`. والتأسيس `db:bootstrap` يرفض قاعدةً فيها جداول؛ وأمرُ دفع المخطّط بلا هجرة حُذف من package.json.
- **ما قرأه النموذج في المزامنة «ينتظر المراجعة»، وملفّ التحويلات يحجزه** (`NEEDS_REVIEW` في `payment-run.ts`) — لا يدخل مالٌ إلى البنك على قراءةٍ لم يرها إنسان. والتأكيد «أكّده» في `/documents` عبر `/api/document-status`.
- **رمز الدرايف يُحدَّث في كلّ دخول بجوجل** (`events.signIn` في `src/auth.ts`) — Auth.js لا يكتب الرموز إلّا عند ربط الحساب أوّل مرّة، فلمّا انتهى الرمز (`invalid_grant`) لم يُصلحه الخروج والدخول. **ومشيُ الأرشيف لا يبتلع إلّا ٤٠٤**: كان يبتلع كلّ خطأ عند مجلّد السنة فتقول المزامنة «٠ ملفّات جديدة» والدرايف لم يُقرأ. وإن كانت شاشة موافقة جوجل في وضع «Testing» فرمز التجديد ينتهي كلّ سبعة أيّام.
- **بيئة المعاينة كانت تشارك قاعدة الإنتاج فعلاً** — تُحُقِّق منه في سبتمبر ٢٠٢٦: لم يكن في مشروع Neon إلّا فرعٌ واحد. فصار للمعاينة فرعٌ خاصّ (`ux-redesign`) و`DATABASE_URL` الافتراضيّ للمعاينة يشير إليه — فلا نشرٍ تجريبيّ يكتب في مالٍ حقيقيّ. **ولا يُثبَت العزلُ بـ`systemIdentifier`**: فروعُ Neon نسخٌ على مستوى الملفّات فترثه من أصلها، فلا يفرّق `/api/ops/db-identity` الفرعَ من أبيه. والإثباتُ القاطع أن يُكتَب علَمٌ في الفرع ويُتحقَّق أنّ الإنتاج لا يراه.
- **الامتدادُ حروفٌ لا أرقام — والمبلغُ ليس امتداداً.** أسماءُ الأرشيف تنتهي بالمبلغ («‏…_SAR996.19»)، وكان كلُّ ما بعد آخر نقطة يُقرأ امتداداً: فقُرئت «19» امتداداً، فبُني الاسمُ القياسيّ على قاعدةٍ تنتهي بالمبلغ ثمّ أُلحق بها — «‏…_SAR996.19.19». و**وقع في الدرايف لا في الشيفرة وحدها**: أربعةُ ملفّاتٍ حقيقيّة يوم ١٤ سبتمبر ٢٠٢٦، بنصّها في سجلّ `DRIVE_FILE_RENAMED`. وأخطرُ منه أنّ الاسم المُفسَد **يُقرأ صحيحاً**: `parseFileName` تقبل «71» امتداداً فيستقيم الأساس، فتقول `canonicalName` «على الصيغة» ولا يُقترَح إصلاحُه أبداً — عطبٌ يُخفي نفسه، ولولا مقابلةُ الأرشيف بالقيد ما ظهر. والحدّ الآن `looksLikeExtension` في `naming.ts`: حروفٌ من حرفين إلى أربعة، وما لا امتداد له يُؤخَذ من `mime_type` المقيَّد. **ولم يُقصَر على قائمةٍ معدودة عمداً** — امتدادٌ صحيح خارجها (`xlsx`، `csv`) يصير «بلا امتداد» فيُقترَح له امتدادٌ آخر، وذاك كسرُ الملفّ الذي نتفاداه. والأربعةُ المُفسَدة تُقترَح للإصلاح الآن، والتسميةُ فعلُ إنسانٍ ملفّاً ملفّاً بإذن أحمد.

## الأوامر

`npm test` (٢١٦٣ اختباراً · ١٢٣ ملفاً) · `npm run test:db` · `npm run typecheck` · `npm run lint`
`npm run db:migrate` · `db:verify` · `db:dedupe` · `db:rematch` · `db:reclassify` · `db:learn` · `db:link` · `db:split-check`
`npm run db:expenses` · `db:audit` · `db:measure` · `db:repair` · `db:products` · `db:merge` · `db:reprice` · `db:repair-rules` · `db:repair-scope` · `db:identity` · `db:unpaid`
`npm run drive:auth` · `drive:inventory` · `drive:backfill` · `drive:diagnose`

### بوّابة الإنتاج — لا يُقال «جاهز» بلا دليل

| الأمر | ماذا يفعل |
|---|---|
| `npm run ops:gate` | ستّة عشر بنداً: `PASS` أو `FAIL` أو **`?` لم يُفحَص** — والمجهول يمنع كما يمنع الفشل |
| `npm run ops:certify` | أحدَ عشر سيناريو ماليّاً على المخطّط الحقيقيّ، **كلٌّ يُلغى أثرُه** فلا يُكتَب شيء |
| `npm run ops:truth` | الأصل في الدرايف مقابل القيد: مطابق · يحتاج تصحيحاً · مكرَّر · مفقود · لا يُقطَع فيه |
| `npm run ops:isolation` | بصمةُ القاعدة — و`systemIdentifier` هو الحاكم، لا اسمُ المضيف |

وما لا يُفحَص آلياً — تدويرُ مفتاح، وأخذُ نسخة — يُقَرّ في `ops-attestation.json`
(انظر المثال). ولا يُكتَب `PASS` بالنيّة.

## مبنيٌّ ولا يصل إليه أحد — لا عمداً

هذه ليست «لم تُبنَ»؛ هي بُنيت واختُبرت ووُثّقت، ثمّ لم تُوصَل بشاشة.
والفرق مهمّ: ما لم يُبنَ يُعرَف أنّه ناقص، وما بُني ولم يُوصَل **يُظنّ
عاملاً** — في الوثيقة وفي الذهن — فيُبنى عليه.

| الوحدة | ماذا يضيع بغيابها |
|---|---|
| `src/lib/credit-notes.ts` | موصولةٌ نصفَ وصل: `supplier-account.ts` تستدعيها ولا يُمرَّر لها إشعارٌ واحد — لا إشعارات في البيانات اليوم |
| `src/lib/bank/reversal.ts` | الردّ لا يُكشَف آلياً — حالةٌ واحدة محتملة في البيانات |
| `src/lib/extraction/benchmark.ts` | لا يُقاس أيّ نموذجٍ أدقّ |
| `branches` | مخطَّطٌ بلا شيفرة: `branchId` لا يرد في استعلامٍ واحد. (أمّا `bank_accounts` فممتلئٌ في كلّ حركة، و`reconciliation_periods` يكتبه الاستيراد ونموذج الإقفال.) |

وما كان ميّتاً بلا شاشةٍ ولا مستدعٍ **حُذف بدل أن يُسرَد** (مراجعة سبتمبر): `insights.ts` و`bank/parsers/types.ts` و`matchBankTransactions` ونصُّ تجربته، و`/api/supplier-alias`.

**قبل أن تُضيف وحدةً جديدة: أوصِل واحدةً من هذه.**

## ما ليس مبنيّاً — عمداً

تكلفة المبيعات والهامش · التنبّؤ · «المدير المالي» الذكي · تصدير محاسبي · الإشعارات.

**وواجهةُ فودكس البرمجيّة ليست مبنيّة — والإكسل هو الطريق اليوم.** ودخول
المبيعات يقع عبر `sales-import.service.ts` إلى `sales`/`sale_lines`
نفسِها، والموصلُ `notConnected` ما زال يرمي `NotConnectedError` ولا
يُرجع أصفاراً. **ولا تُملأ جداولُ المبيعات ببياناتٍ وهميّة** — تُملأ
من ملفٍّ حقيقيّ أو تبقى فارغة.

وسجلُّ الهدر **جدولُه مبنيّ وشاشتُه داخل الجرد**، ولا منتجَ إدارةِ هدرٍ
فوقه: لا تقارير هدر، ولا موافقات، ولا أسبابٌ تُدار. يُسجَّل السطر
فيخرج من «الفرق غير المفسَّر»، وذاك كلُّ ما يلزم اليوم.
