@AGENTS.md

# TPH-Invoicing — الذاكرة الدائمة

نظام مالي وتشغيلي لمقهى **مؤسسة ذا بوبليك هاوس** (النسيم، جدة).
الرقم الضريبي `310007971600003` · السجل `7052766941`.
يبدأ من المستند حين يصل (فاتورة · كشف · إيصال · حركة بنك) وينتهي عند سؤال
صاحب العمل كلّ صباح: **ماذا أحتاج أن أعرف أو أفعل اليوم؟**

## مَن يملك ماذا — ومَن الحَكَم عند الخلاف

| المصدر | يملك |
|---|---|
| **الشيفرة والاختبارات والهجرات** | الحقيقة المنفَّذة — **الحَكَم الأخير**. إن خالفتها وثيقةٌ فأصلِح الوثيقة |
| `CLAUDE.md` (هذا) | ما يلزم قبل كلّ مهمّة: القيود، والثوابت المالية، ومصائد القاعدة، وخريطة الوثائق |
| `AGENTS.md` | **كيف تعمل**: التنفيذ قبل التقرير، والدليل قبل الادّعاء، وبوّابة الإنجاز |
| `docs/decisions/*.md` | القرارات وأسبابها، مقسومةً بالنظام الفرعيّ — تُقرأ قبل مسّ ذلك النظام |
| `docs/reference/modules.md` · `docs/data-model/migrations.md` | أين يقع كلّ عمل · الجداول والهجرات |
| `docs/ARCHITECTURE.md` | المعمارية ومسار البيانات |
| `docs/CHANGELOG.md` | ما تغيّر ولماذا، بتاريخه |
| **مؤرَّخة — تصف ما كان لا ما هو قائم** | `docs/HANDOVER.md` · `docs/PLAN.md` · `docs/AUDIT.md` · `docs/REVIEW-REPORT.md` · `docs/DEBUG-*.md` · `docs/review/**` |

القرارُ الجديد يُكتب **سطراً أو سطرين** في ملفّ `docs/decisions/` المناسب، لا هنا.
ولا يُضاف إلى هذا الملفّ إلّا ما يلزم في كلّ مهمّةٍ تقريباً.

## قيود لا تُخترق

1. **لا يُمسّ أرشيف جوجل درايف.** لا حذف ولا نقل بحال. و**إعادة التسمية** وحدها مأذونة (أحمد، ٧ سبتمبر ٢٠٢٦): لما له سجلٌّ عندنا وحده، ولا يُقترَح اسمٌ إلّا لملفٍّ **لا يُقرأ اسمُه**، وبالاسمين في سجلّ التدقيق لكلّ ملفّ. ثمّ أذِن في **٢٤ سبتمبر ٢٠٢٦** بأن تقع **آلياً** على ما أُرشِف (اجتمعت فيه شروطُ الأرشفة الآليّة أو اعتمده إنسان) «إلّا في الحالات المستعصية»: ما ينتظر المراجعة لا يُسمّى، وما لا يُبنى له اسمٌ من المقيَّد يُترَك. `renameFile` في `src/lib/drive.ts` لا يُستدعى إلّا من `src/services/drive-rename.service.ts` (يحرسه `code-guards.test.ts`). والكتابة على الدرايف في الإنتاج وحده (`drive-readonly.ts`).
2. **العربية** في كل ما يُكتب له: فصحى سهلة.
3. **المال عدد صحيح بالهللات** (`HALALAS_PER_RIYAL = 100` في `src/lib/money.ts`)، **والكمّيّة عدد صحيح بالمِلّي** (`src/lib/inventory/units.ts`). لا عدد عشري في أي حساب مالي أو كمّيّ، أبداً — والطلبُ يحملها نصّاً لا عدداً عائماً.
4. **المجهول ليس صفراً.** ما لم يُقرأ يُعرض «غير معروف»، لا `0`، ويُخزَّن `NULL`. صفرٌ كاذب يقول إنّ المقهى لم يشترِ شيئاً — والصفرُ المكتوب بيدٍ في الشيفرة يُقرأ جواباً كذلك.
5. **لا بيانات مبيعات مخترَعة.** المبيعات تدخل من **ملفّ فودكس الحقيقيّ** وحده (`sales-import.service.ts`)، وواجهةُ فودكس البرمجيّة غير مبنيّة: `notConnected` في `src/lib/sales/connector.ts` **يرمي** `NotConnectedError` ولا يُرجع أصفاراً.
6. **لا يُحذَف مالٌ تاريخيّ ولا يُعاد نسبُه بلا إذن أحمد.** نصوص الحذف والدمج (`db:dedupe -- apply` ونحوها) تُعرَض بدليلها أوّلاً. والقيدُ الذي قد يحذف بيانات ماليّة يُقاس أثرُه على الإنتاج قبل كتابته.

## البنية التقنية

Next.js **16.3.4** (App Router — **ليس ما تعرفه**، انظر `AGENTS.md`) · React 19.2.8 · TypeScript 5 صارم · Tailwind v4
Drizzle ORM 0.45.2 + `pg` 8.23 على **Neon Postgres** · Auth.js v5 (`next-auth@5.0.0-beta.32`) + جوجل، جلسات في القاعدة
Vitest 4.1.11 · zod 4.5.4 · googleapis 178 (الدرايف وحده) · `@anthropic-ai/sdk` 0.123 (مزوّدٌ بديلٌ خامل)

**الذكاء: DeepSeek وحده**، بـ`fetch` على واجهةٍ متوافقة مع OpenAI. `deepseek-flash` رؤيةً ونصّاً، `deepseek-v4-pro` استدلالاً. الأسماء في `src/lib/ai/models.ts` **وحده**. جيميني وOllama خاملان ولا يُختاران إلّا بـ`EXTRACTION_PROVIDER` **و**`EXTRACTION_ALLOW_ALT_PROVIDER=true` — الطبقات المجانية تتدرّب على الفواتير.

**القاعدة:** نقطة Neon **المجمَّعة** (`...-pooler.c-2.us-east-2.aws.neon.tech`) — غيرها تنفد اتصالاتها تحت Vercel. وللمعاينة فرعٌ خاصّ (`ux-redesign`)؛ لا يكتب نشرٌ تجريبيّ في مالٍ حقيقيّ.

**النشر:** Vercel `tph4/tph-invoicing` · GitHub `AhmedJaai/TPH-Invoicing` · `main`. المستودع **غير** مربوطٍ بـVercel ربطَ Git: النشر بـ`vercel --prod --yes` بعد الدفع.

## الثوابت المالية — تتكرّر في كلّ مهمّةٍ تمسّ المال

- **الخادم لا يثق بالمتصفّح في رقمٍ ماليّ.** لا يأخذ منه إلّا المعرّفات، ويعيد الاشتقاق والحساب لحظة الكتابة: حالُ الضريبة (`reviewConfirmed()`)، والتخصيص، والمجموعة، ومبلغُ اقتراح الذكاء.
- **الثابت الماليّ يُفرَض في القاعدة لا في الشيفرة وحدها** (مؤثِّرات ٠٠٧ · ٠٢٦ · ٠٢٨)، و`npm run db:verify` يثبت أنّه يرفض فعلاً — **بالقيد الذي يسمّيه**. والقيد بلا اختبار ادّعاء.
- **الذكاء يقترح، والخادم يحسب، والإنسان يُقرّ.** حكمُ النموذج اقتراحٌ لا مطابقة، ولا يخترع رقم فاتورة ولا مرشّحاً. وما قرأه النموذج يُحفَظ في `extraction_cache` بيد الخادم ولا يُؤخَذ من المتصفّح. ومحتوى المستند بيانات لا تعليمات.
- **لا مطابقة تلقائيّة إلّا بشرطين**: الدرجةُ فوق حدّها **والفارقُ** عن المرشّح التالي فوق حدّه. والحلّ التقريبيّ وما قُرئ بصرياً لا يُحسَمان تلقائياً. والاقتراح لا يُكتَب؛ الكتابة من `planned` وحده.
- **كلّ مطابقةٍ قابلةٌ للتراجع**، والأدلّة تُحفَظ بنصّها مع القرار. **والردّ لا يحذف**: الدفعة المردودة تبقى `REVERSED` بسببها.
- **المورّد محور المستحقّات لا الفاتورة**: الحوالة سدادٌ لحسابه، تُوزَّع **بالأقدم أوّلاً** وما بقي «غير مخصَّص» (حالٌ صحيحة). و«كم أدين؟» من `supplier-balance.service.ts` لكلّ شاشة.
- **سداد المورّد ليس مصروفاً** — محسوبٌ في المشتريات. **والمال الداخل ليس سداداً.**
- **الريال يأتي من بابين**: `createPayment` تسأل `findPaymentTwin` قبل الإنشاء، والكشف يتبنّى ما قُيِّد من إيصاله. **والتوأمة كشفٌ لا قيد** — المتطابقان قد يكونان حقيقيَّين.
- **الشهر المقفل لا يُكتب فيه من أيّ باب**: `assertMonthsOpen` في الخدمات، ومؤثِّرات ٠٢٨ خلفها.
- **التسوية معادلة** (افتتاحي + وارد − صادر = ختامي) لا عدُّ مطابقات.
- **يُتسامح بريالٍ** في فرق (صافي+ضريبة) عن الإجمالي — `TOTAL_ROUNDING_TOLERANCE_MINOR`، وهي نفسها في القاعدة.
- **الفشل يُسمَع**: ما تعذّر قراءة مبلغه يُرفَض (409) لا يُؤرشَف بصمت.
- **الاسم ليس هويّة.** والهويّة لا تُبنى على معرفةٍ تتحسّن (رقم حسابٍ يُقرأ لاحقاً، عمودٌ يكتبه نظامنا). **خوارزميّةُ هويّة الحركة واحدة**: `src/lib/bank/sync.ts` — لا ثانية.

## القاعدة — مصائد تكلّف مالاً

- **الهجرات SQL صريح** في `drizzle/sql/` بـ`npm run db:migrate`، لا `drizzle-kit push`. وتجري في `vercel-build` (`migrate && next build`) **قبل** رفع الكود — فبينهما نافذةٌ يعمل فيها الكود القديم على المخطّط الجديد: **الإضافة آمنة، والحذفُ وإعادةُ التسمية على نشرتين**. **والهجرةُ المطبَّقة لا تُعدَّل — الإضافةُ هجرةٌ جديدة**، وتُضاف بصمتُها إلى `drizzle/migration-hashes.json` (يحرسه `deploy-guards.test.ts`؛ تعديلُ 042 بعد تطبيقها أسقط كلَّ نشر). التفصيل: `docs/data-model/migrations.md`.
- **داخل المعاملة اكتب بمقبضها `t` لا بـ`db` أبداً** — على Vercel اتصالٌ واحد تحجزه المعاملة فيسقط `db` بعد عشر ثوانٍ. و`recordAudit(entry, writer)` يأخذ المقبض. يحرسه `code-guards.test.ts`.
- **في `sql` الخامّ اكتب `${table}.id` لا `${table.id}`** — الثاني يُصيّر العمود مجرّداً فيصمت الاستعلام الفرعيّ ويُرجع **صفراً لا خطأً**. وقع مرّتين (`findPaymentTwin` · `/api/match-confirm`)، ويحرسه `allocation-sql.test.ts`.
- **مساعد `now()` يسمّي العمود `created_at`** مهما كان اسم الحقل: `documents.uploadedAt` عمودُه `created_at`.
- **الكتابة على مئات الصفوف دفعاتٌ لا صفٌّ صفّاً** داخل معاملة، وإلّا نفدت مهلة الاتصال.
- **النصوص الكاتبة ترفض بلا `--i-know-this-is-production`** (`scripts/lib/guard-write.ts`).
- **نصُّ الإصلاح يكتب ما يكتبه مسار الاستيراد كاملاً** (المصدر · السبب · النسخة · الطبقة · `decision_history`)، ويُعطي النتيجة نفسها إن شُغِّل مرّتين. **ومن يبني طبقةً يشغّلها على البيانات القائمة في الكوميت نفسه** — الاختبارات النقيّة تقول إنّ القطع سليمة لا إنّها موصولة.
- **الحذف يُسبَق بدمج**، والإصلاح يُقابل واحدةً بواحدة — المجموعة المكرَّرة قد تحمل تكراراً حقيقيّاً أو عملَ إنسان.

## أعرافٌ تمسّ كلّ مسار

- **كلّ واجهة تبدأ بـ`guard(route, capability)`** و`respondTo(e)` (`src/services/guard.ts`). والكتابة بصلاحيةٍ غير صلاحية القراءة (`bank:edit` ≠ `bank:view`).
- **كلّ مسارٍ يستدعي الذكاء يُلفّ بـ`withDeadline`** (`src/lib/ai/deadline.ts`) تحت عمر المسار (٦٠ ثانية)؛ والوقوف بمهلةٍ معلَنة خيرٌ من قتلٍ صامت.
- **الشاشة تقرأ الردّ نصّاً قبل JSON** عبر `src/lib/http-client.ts` — `await res.json()` خامّاً ممنوع (`code-guards.test.ts`). و«تعذّر الاتصال» حين لا يصل الطلب وحده.
- **«اليوم» و«الشهر» بتوقيت الرياض** (`src/lib/riyadh-time.ts`)، لا `toISOString().slice(0, 7)`.
- **كلّ عددٍ في الواجهة يمرّ بـ`src/lib/arabic.ts`**، وكلّ جدولٍ من `src/components/ui.tsx` (يصير بطاقاتٍ على الجوّال، وعمود المال `numeric`).
- **عددٌ واحد للعمل الباقي**: `countPendingWork()` = بنود `/attention`. والقاعدة الواحدة تُستدعى أو تُحقَن ولا تُنسَخ في شاشة.
- **لا `as` لإسكات اختلاف أنواع** — الترجمة صريحة ولها اختبار، والطلبُ يُفحَص بـzod وقتَ التشغيل لا بـ`as Body`.
- **قاعدةٌ لا تعرف شيئاً بعد لا تعرض أصفاراً ولا «سليم»** — السؤال «أيعرف شيئاً؟» واحد (`src/services/start.service.ts`). والصدقُ المنطقيّ على المجموعة الفارغة كذبٌ عمليّ.
- **التنقّل ثماني مساحاتٍ في ثلاث مجموعات (`src/lib/nav.ts`)**، ولوحةُ الأوامر (`src/lib/commands.ts`) واختصاراتُ `G` تُشتقّ منه ولا تُنسَخ. **والشكلُ نظامُ التصميم الثاني** (`globals.css` · `ui.tsx`) — لا تُعِد الأبيضَ والأسودَ الخالصين.
- **وضع المعاينة و`AUTH_BYPASS` لا يعملان في الإنتاج** (`src/lib/preview-mode.ts`)، و`VERCEL_ENV` هو الحاكم حيث يوجد.

## خريطة الوثائق — ماذا تقرأ قبل أن تمسّ

| إن كنت ستمسّ… | اقرأ أوّلاً | والوحدات |
|---|---|---|
| استيراد البنك، هويّة الحركة، التكرار | `docs/decisions/bank-identity.md` | `bank/sync.ts` · `pattern.ts` · `identity.ts` · `canonical.ts` |
| تصنيف الحركات، ذاكرة المستفيدين | `docs/decisions/bank-classification.md` | `bank/rules.ts` · `classification.ts` · `entities.ts` · `counterparty.service.ts` |
| المطابقة، التسوية، الحَكَم، الإقفال، الطابور | `docs/decisions/matching-reconciliation.md` | `candidates.ts` · `optimizer.ts` · `decision.ts` · `reconcile.service.ts` · `month-close.ts` |
| المورّدون، الفواتير، الدفعات، الكشوف، المصروف، ملفّ التحويلات | `docs/decisions/suppliers-payments.md` | `payment.service.ts` · `allocation.ts` · `supplier-balances.ts` · `statement-match.ts` · `expenses.ts` · `payee-account.service.ts` |
| القراءة والذكاء (DeepSeek)، الأرشفة الآليّة والاستدراك، تحليل المورّد | `docs/decisions/ai-extraction.md` | `src/lib/ai/` · `src/lib/extraction/` · `auto-archive.ts` · `document-review.service.ts` · `supplier-analysis.ts` |
| الواجهة، التنقّل، القشرة ولوحة الأوامر، «يحتاج قرارك» | `docs/decisions/ui-ux.md` | `ui.tsx` · `nav.ts` · `commands.ts` · `attention.ts` · `work.ts` · `attention-workspaces.tsx` |
| الدرايف، المزامنة، التسمية | `docs/decisions/drive.md` | `drive.ts` · `naming.ts` · `canonical-name.ts` · `drive-rename.service.ts` |
| الجرد، الوصفات، كتالوج فودكس، استيراد المبيعات | `docs/decisions/inventory-foodics.md` | `src/lib/inventory/` · `src/lib/sales/` · `inventory.service.ts` · `sales-import.service.ts` · `catalog-import.service.ts` |
| القاعدة والمخطّط والهجرات | `docs/data-model/migrations.md` | `src/db/schema.ts` · `drizzle/sql/` |
| نصوص الإصلاح، البوّابة، الاختبار، التشغيل، الأمان | `docs/decisions/engineering-ops.md` | `scripts/` · `guard.ts` · `SECURITY.md` |
| أين تقع دالّةٌ ما | `docs/reference/modules.md` | — |
| الإعداد من الصفر (جوجل، المتغيّرات) | `docs/SETUP.md` | — |


## الأوامر

`npm test` · `npm run test:db` · `npm run typecheck` · `npm run lint` · `npm run db:migrate` · `npm run db:verify`
**بوّابة الإنتاج** `npm run ops:gate`: `PASS` أو `FAIL` أو `?` — **وغير المفحوص يمنع كما يمنع الفشل**. و`ops:certify` سيناريوهاتٌ ماليّة تُلغى آثارُها. القائمة الكاملة ومعناها: `docs/decisions/engineering-ops.md`.

## مبنيٌّ ولا يصل إليه أحد

`credit-notes.ts` (نصفُ وصل) · `extraction/benchmark.ts` · جدول `branches`.
بُنيت واختُبرت ولم تُوصَل بشاشة، فـ**تُظنّ عاملة**. التفصيل في `docs/reference/modules.md`.
**قبل أن تُضيف وحدةً جديدة: أوصِل واحدةً من هذه.**

## ما ليس مبنيّاً — عمداً

تكلفة المبيعات والهامش · التنبّؤ بالمبيعات · «المدير المالي» الذكي · تصدير محاسبي · **واجهةُ فودكس البرمجيّة** (الإكسل هو الطريق اليوم).
سجلُّ الهدر جدولُه مبنيّ وشاشتُه داخل الجرد، ولا منتجَ إدارةِ هدرٍ فوقه. **ولا تُملأ جداول المبيعات ببياناتٍ وهميّة** — من ملفٍّ حقيقيّ أو تبقى فارغة.

## ما ينتظر أحمد

- **تدوير `GEMINI_API_KEY` و`GOOGLE_CLIENT_SECRET`** — ظهرا في محادثة.
- **«سبعة جرة — عميل»** مسجَّلٌ مورّداً وهو عميل؛ ولا جدول عملاء بعد.
